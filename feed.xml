<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://westyang.github.io/blog-page/feed.xml" rel="self" type="application/atom+xml" /><link href="https://westyang.github.io/blog-page/" rel="alternate" type="text/html" /><updated>2026-04-17T05:19:49+00:00</updated><id>https://westyang.github.io/blog-page/feed.xml</id><title type="html">个人博客</title><subtitle>我的个人技术博客</subtitle><author><name>博主</name><email>your-email@example.com</email></author><entry><title type="html">V1.35.0 Centos Binary Install Ipv6 Ipv4 Three Masters Two Slaves Offline</title><link href="https://westyang.github.io/blog-page/2026/04/17/v1.35.0-CentOS-binary-install-IPv6-IPv4-Three-Masters-Two-Slaves-Offline.html" rel="alternate" type="text/html" title="V1.35.0 Centos Binary Install Ipv6 Ipv4 Three Masters Two Slaves Offline" /><published>2026-04-17T00:00:00+00:00</published><updated>2026-04-17T00:00:00+00:00</updated><id>https://westyang.github.io/blog-page/2026/04/17/v1.35.0-CentOS-binary-install-IPv6-IPv4-Three-Masters-Two-Slaves-Offline</id><content type="html" xml:base="https://westyang.github.io/blog-page/2026/04/17/v1.35.0-CentOS-binary-install-IPv6-IPv4-Three-Masters-Two-Slaves-Offline.html"><![CDATA[<h1 id="二进制安装kubernetesk8sv1350">二进制安装Kubernetes（k8s）v1.35.0</h1>

<h1 id="介绍">介绍</h1>

<p><a href="https://github.com/cby-chen/Kubernetes">https://github.com/cby-chen/Kubernetes</a> 开源不易，帮忙点个star，谢谢了</p>

<h2 id="kubernetesk8s二进制高可用安装部署支持ipv4ipv6双栈">kubernetes（k8s）二进制高可用安装部署，支持IPv4+IPv6双栈。</h2>

<h2 id="强烈建议在github上查看文档-">强烈建议在Github上查看文档 ！！！</h2>

<h2 id="github出问题会更新文档并且后续尽可能第一时间更新新版本文档-">Github出问题会更新文档，并且后续尽可能第一时间更新新版本文档 ！！！</h2>

<h2 id="手动项目地址httpsgithubcomcby-chenkubernetes">手动项目地址：https://github.com/cby-chen/Kubernetes</h2>

<h1 id="1环境">1.环境</h1>

<table>
  <thead>
    <tr>
      <th>主机名称</th>
      <th>IP地址</th>
      <th>说明</th>
      <th>软件</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td> </td>
      <td>192.168.1.60</td>
      <td>外网节点</td>
      <td>下载各种所需安装包</td>
    </tr>
    <tr>
      <td>Master01</td>
      <td>192.168.1.31</td>
      <td>master节点</td>
      <td>kube-apiserver、kube-controller-manager、kube-scheduler、etcd、<br />kubelet、kube-proxy、nfs-client、haproxy、keepalived、nginx</td>
    </tr>
    <tr>
      <td>Master02</td>
      <td>192.168.1.32</td>
      <td>master节点</td>
      <td>kube-apiserver、kube-controller-manager、kube-scheduler、etcd、<br />kubelet、kube-proxy、nfs-client、haproxy、keepalived、nginx</td>
    </tr>
    <tr>
      <td>Master03</td>
      <td>192.168.1.33</td>
      <td>master节点</td>
      <td>kube-apiserver、kube-controller-manager、kube-scheduler、etcd、<br />kubelet、kube-proxy、nfs-client、haproxy、keepalived、nginx</td>
    </tr>
    <tr>
      <td>Node01</td>
      <td>192.168.1.34</td>
      <td>node节点</td>
      <td>kubelet、kube-proxy、nfs-client、nginx</td>
    </tr>
    <tr>
      <td>Node02</td>
      <td>192.168.1.35</td>
      <td>node节点</td>
      <td>kubelet、kube-proxy、nfs-client、nginx</td>
    </tr>
    <tr>
      <td> </td>
      <td>192.168.1.36</td>
      <td>VIP</td>
      <td> </td>
    </tr>
  </tbody>
</table>

<p>详细版本
| 软件 | 版本 |
| — | —| 
| cni_plugins_version | v1.9.0 |
| cri_containerd_cni_version | 2.2.1 |
| crictl_version | v1.35.0 |
| cri_dockerd_version | 0.3.21 |
| etcd_version | v3.6.7 |
| cfssl_version | 1.6.5 |
| kubernetes_server_version | 1.35.0 |
| docker_version | 29.1.3 |
| runc_version | 1.4.0 |
| kernel_version | 6.16.4 |
| helm_version | 4.0.4 |
| nginx_version | 1.29.4 |</p>

<p>网段  <br /></p>

<p>IPv4   <br />
物理主机：192.168.1.0/24  <br />
service：10.96.0.0/12  <br />
pod：172.16.0.0/12 <br /></p>

<p>IPv6   <br />
物理主机：2408:822a:732:5ce1::1001/64  <br />
物理主机：fc00::31/8  <br />
service：fd00:1111::/112  <br />
pod：fc00:2222::/112</p>

<p>安装包已经整理好：https://github.com/cby-chen/Kubernetes/releases/download/v1.35.0/kubernetes-v1.35.0.tar</p>

<h2 id="11k8s基础系统环境配置">1.1.k8s基础系统环境配置</h2>

<h3 id="12配置ip">1.2.配置IP</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 注意！</span>
<span class="c"># 若虚拟机是进行克隆的那么网卡的UUID和MachineID会重复</span>
<span class="c"># 需要重新生成新的UUIDUUID和MachineID</span>
<span class="c"># UUID和MachineID重复无法DHCP获取到IPV6地址</span>
ssh root@192.168.1.195 <span class="s2">"rm -rf /etc/machine-id; systemd-machine-id-setup;reboot"</span>
ssh root@192.168.1.196 <span class="s2">"rm -rf /etc/machine-id; systemd-machine-id-setup;reboot"</span>
ssh root@192.168.1.197 <span class="s2">"rm -rf /etc/machine-id; systemd-machine-id-setup;reboot"</span>
ssh root@192.168.1.198 <span class="s2">"rm -rf /etc/machine-id; systemd-machine-id-setup;reboot"</span>
ssh root@192.168.1.199 <span class="s2">"rm -rf /etc/machine-id; systemd-machine-id-setup;reboot"</span>
<span class="c"># </span>
<span class="c"># 查看当前的网卡列表和 UUID：</span>
<span class="c"># nmcli con show</span>
<span class="c"># 删除要更改 UUID 的网络连接：</span>
<span class="c"># nmcli con delete uuid &lt;原 UUID&gt;</span>
<span class="c"># 重新生成 UUID：</span>
<span class="c"># nmcli con add type ethernet ifname &lt;接口名称&gt; con-name &lt;新名称&gt;</span>
<span class="c"># 重新启用网络连接：</span>
<span class="c"># nmcli con up &lt;新名称&gt;</span>

<span class="c"># 更改网卡的UUID</span>
<span class="c"># 先配置静态IP之后使用ssh方式配置不断连</span>
ssh root@192.168.1.195 <span class="s2">"nmcli con delete uuid d1141403-18c6-3149-907c-ed5f09663a7f;nmcli con add type ethernet ifname ens160 con-name ens160;nmcli con up ens160"</span>
ssh root@192.168.1.196 <span class="s2">"nmcli con delete uuid d1141403-18c6-3149-907c-ed5f09663a7f;nmcli con add type ethernet ifname ens160 con-name ens160;nmcli con up ens160"</span>
ssh root@192.168.1.197 <span class="s2">"nmcli con delete uuid d1141403-18c6-3149-907c-ed5f09663a7f;nmcli con add type ethernet ifname ens160 con-name ens160;nmcli con up ens160"</span>
ssh root@192.168.1.198 <span class="s2">"nmcli con delete uuid d1141403-18c6-3149-907c-ed5f09663a7f;nmcli con add type ethernet ifname ens160 con-name ens160;nmcli con up ens160"</span>
ssh root@192.168.1.199 <span class="s2">"nmcli con delete uuid d1141403-18c6-3149-907c-ed5f09663a7f;nmcli con add type ethernet ifname ens160 con-name ens160;nmcli con up ens160"</span>

<span class="c"># 参数解释</span>
<span class="c"># </span>
<span class="c"># ssh ssh root@192.168.1.31</span>
<span class="c"># 使用SSH登录到IP为192.168.1.31的主机，使用root用户身份。</span>
<span class="c"># </span>
<span class="c"># nmcli con delete uuid 708a1497-2192-43a5-9f03-2ab936fb3c44</span>
<span class="c"># 删除 UUID 为 708a1497-2192-43a5-9f03-2ab936fb3c44 的网络连接，这是 NetworkManager 中一种特定网络配置的唯一标识符。</span>
<span class="c"># </span>
<span class="c"># nmcli con add type ethernet ifname ens160 con-name ens160</span>
<span class="c"># 添加一种以太网连接类型，并指定接口名为 ens160，连接名称也为 ens160。</span>
<span class="c"># </span>
<span class="c"># nmcli con up ens160</span>
<span class="c"># 开启 ens160 这个网络连接。</span>
<span class="c"># </span>
<span class="c"># 简单来说，这个命令的作用是删除一个特定的网络连接配置，并添加一个名为 ens160 的以太网连接，然后启用这个新的连接。</span>

<span class="c"># 修改静态的IPv4地址</span>
ssh root@192.168.1.195 <span class="s2">"nmcli con mod ens160 ipv4.addresses 192.168.1.31/24; nmcli con mod ens160 ipv4.gateway  192.168.1.1; nmcli con mod ens160 ipv4.method manual; nmcli con mod ens160 ipv4.dns "</span>8.8.8.8<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.196 <span class="s2">"nmcli con mod ens160 ipv4.addresses 192.168.1.32/24; nmcli con mod ens160 ipv4.gateway  192.168.1.1; nmcli con mod ens160 ipv4.method manual; nmcli con mod ens160 ipv4.dns "</span>8.8.8.8<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.197 <span class="s2">"nmcli con mod ens160 ipv4.addresses 192.168.1.33/24; nmcli con mod ens160 ipv4.gateway  192.168.1.1; nmcli con mod ens160 ipv4.method manual; nmcli con mod ens160 ipv4.dns "</span>8.8.8.8<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.198 <span class="s2">"nmcli con mod ens160 ipv4.addresses 192.168.1.34/24; nmcli con mod ens160 ipv4.gateway  192.168.1.1; nmcli con mod ens160 ipv4.method manual; nmcli con mod ens160 ipv4.dns "</span>8.8.8.8<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.199 <span class="s2">"nmcli con mod ens160 ipv4.addresses 192.168.1.35/24; nmcli con mod ens160 ipv4.gateway  192.168.1.1; nmcli con mod ens160 ipv4.method manual; nmcli con mod ens160 ipv4.dns "</span>8.8.8.8<span class="s2">"; nmcli con up ens160"</span>

<span class="c"># 参数解释</span>
<span class="c"># </span>
<span class="c"># ssh root@192.168.1.154</span>
<span class="c"># 使用SSH登录到IP为192.168.1.154的主机，使用root用户身份。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv4.addresses 192.168.1.31/24"</span>
<span class="c"># 修改ens160网络连接的IPv4地址为192.168.1.31，子网掩码为 24。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv4.gateway 192.168.1.1"</span>
<span class="c"># 修改ens160网络连接的IPv4网关为192.168.1.1。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv4.method manual"</span>
<span class="c"># 将ens160网络连接的IPv4配置方法设置为手动。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv4.dns "8.8.8.8"</span>
<span class="c"># 将ens160网络连接的IPv4 DNS服务器设置为 8.8.8.8。</span>
<span class="c"># </span>
<span class="c"># "nmcli con up ens160"</span>
<span class="c"># 启动ens160网络连接。</span>
<span class="c"># </span>
<span class="c"># 总体来说，这条命令是通过SSH远程登录到指定的主机，并使用网络管理命令 (nmcli) 修改ens160网络连接的配置，包括IP地址、网关、配置方法和DNS服务器，并启动该网络连接。</span>

<span class="c"># 我这里有公网的IPv6的地址，但是是DHCP动态的，无法固定，使用不方便</span>
<span class="c"># 所以我配置了内网的IPv6地址，可以实现固定的访问地址</span>

<span class="c"># 我使用的方式。只配置IPv6地址不配置网关DNS</span>
ssh root@192.168.1.31 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00::31/8; nmcli con up ens160"</span>
ssh root@192.168.1.32 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00::32/8; nmcli con up ens160"</span>
ssh root@192.168.1.33 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00::33/8; nmcli con up ens160"</span>
ssh root@192.168.1.34 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00::34/8; nmcli con up ens160"</span>
ssh root@192.168.1.35 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00::35/8; nmcli con up ens160"</span>

<span class="c"># IPv6地址路由DNS，样例</span>
ssh root@192.168.1.31 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00:43f4:1eea:1::10; nmcli con mod ens160 ipv6.gateway fc00:43f4:1eea:1::1; nmcli con mod ens160 ipv6.method manual; nmcli con mod ens160 ipv6.dns "</span>2400:3200::1<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.32 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00:43f4:1eea:1::20; nmcli con mod ens160 ipv6.gateway fc00:43f4:1eea:1::1; nmcli con mod ens160 ipv6.method manual; nmcli con mod ens160 ipv6.dns "</span>2400:3200::1<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.33 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00:43f4:1eea:1::30; nmcli con mod ens160 ipv6.gateway fc00:43f4:1eea:1::1; nmcli con mod ens160 ipv6.method manual; nmcli con mod ens160 ipv6.dns "</span>2400:3200::1<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.34 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00:43f4:1eea:1::40; nmcli con mod ens160 ipv6.gateway fc00:43f4:1eea:1::1; nmcli con mod ens160 ipv6.method manual; nmcli con mod ens160 ipv6.dns "</span>2400:3200::1<span class="s2">"; nmcli con up ens160"</span>
ssh root@192.168.1.35 <span class="s2">"nmcli con mod ens160 ipv6.addresses fc00:43f4:1eea:1::50; nmcli con mod ens160 ipv6.gateway fc00:43f4:1eea:1::1; nmcli con mod ens160 ipv6.method manual; nmcli con mod ens160 ipv6.dns "</span>2400:3200::1<span class="s2">"; nmcli con up ens160"</span>

<span class="c"># 参数解释</span>
<span class="c"># </span>
<span class="c"># ssh root@192.168.1.31</span>
<span class="c"># 通过SSH连接到IP地址为192.168.1.31的远程主机，使用root用户进行登录。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv6.addresses fc00:43f4:1eea:1::10"</span>
<span class="c"># 使用nmcli命令修改ens160接口的IPv6地址为fc00:43f4:1eea:1::10。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv6.gateway fc00:43f4:1eea:1::1"</span>
<span class="c"># 使用nmcli命令修改ens160接口的IPv6网关为fc00:43f4:1eea:1::1。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv6.method manual"</span>
<span class="c"># 使用nmcli命令将ens160接口的IPv6配置方法修改为手动配置。</span>
<span class="c"># </span>
<span class="c"># "nmcli con mod ens160 ipv6.dns "2400:3200::1"</span>
<span class="c"># 使用nmcli命令设置ens160接口的IPv6 DNS服务器为2400:3200::1。</span>
<span class="c"># </span>
<span class="c"># "nmcli con up ens160"</span>
<span class="c"># 使用nmcli命令启动ens160接口。</span>
<span class="c"># </span>
<span class="c"># 这个命令的目的是在远程主机上配置ens160接口的IPv6地址、网关、配置方法和DNS服务器，并启动ens160接口。</span>

<span class="c"># 查看网卡配置</span>
<span class="c"># nmcli device show ens160</span>
<span class="c"># nmcli con show ens160</span>
<span class="o">[</span>root@localhost ~]#  <span class="nb">cat</span> /etc/NetworkManager/system-connections/ens160.nmconnection 
<span class="o">[</span>connection]
<span class="nb">id</span><span class="o">=</span>ens160
<span class="nv">uuid</span><span class="o">=</span>8eba8c2b-1137-497b-8331-28357ec9eafc
<span class="nb">type</span><span class="o">=</span>ethernet
interface-name<span class="o">=</span>ens160
<span class="nv">timestamp</span><span class="o">=</span>1746419908

<span class="o">[</span>ethernet]

<span class="o">[</span>ipv4]
<span class="nv">address1</span><span class="o">=</span>192.168.1.31/24,192.168.1.1
<span class="nv">dns</span><span class="o">=</span>192.168.1.99<span class="p">;</span>
<span class="nv">method</span><span class="o">=</span>manual

<span class="o">[</span>ipv6]
addr-gen-mode<span class="o">=</span>default
<span class="nv">address1</span><span class="o">=</span>fc00::31/8
<span class="nv">method</span><span class="o">=</span>auto

<span class="o">[</span>proxy]

<span class="o">[</span>root@localhost ~]# 

<span class="c"># 参数解释</span>
<span class="c"># 1. `[connection]`:</span>
<span class="c">#    - `id`: 连接的唯一标识符，用于内部引用。</span>
<span class="c">#    - `uuid`: 连接的通用唯一标识符（UUID），确保在系统中的唯一性。</span>
<span class="c">#    - `type`: 指定连接的类型，本例中为以太网。</span>
<span class="c">#    - `interface-name`: 网络接口的名称（`ens160`），表示与此连接关联的物理或逻辑网络接口。</span>
<span class="c">#    - `timestamp`: 时间戳，指示连接配置上次修改的时间。</span>
<span class="c"># 2. `[ethernet]`:</span>
<span class="c">#    - 通常包含以太网特定的配置设置，如MAC地址或链路速度。</span>
<span class="c"># 3. `[ipv4]`:</span>
<span class="c">#    - `address1`: 以CIDR表示法指定IPv4地址和子网掩码（`192.168.1.31/24`）。还包括网关IP（`192.168.1.1`）。</span>
<span class="c">#    - `dns`: 指定要使用的DNS服务器（本例中为`8.8.8.8`），提供将域名转换为IP地址的手段。</span>
<span class="c">#    - `method`: 指定获取IPv4地址的方法。在本例中，设置为手动，表示IP地址是静态配置的。</span>
<span class="c"># 4. `[ipv6]`:</span>
<span class="c">#    - `addr-gen-mode`: 指定IPv6地址生成模式。设置为默认，通常意味着地址是根据接口的MAC地址生成的。</span>
<span class="c">#    - `method`: 指定获取IPv6地址的方法。在本例中，设置为自动，表示使用DHCPv6或SLAAC等协议进行自动配置。</span>
<span class="c"># 5. `[proxy]`:</span>
<span class="c">#    - 通常用于配置代理设置，如HTTP或SOCKS代理。</span>
</code></pre></div></div>

<h3 id="13设置主机名">1.3.设置主机名</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hostnamectl set-hostname k8s-master01
hostnamectl set-hostname k8s-master02
hostnamectl set-hostname k8s-master03
hostnamectl set-hostname k8s-node01
hostnamectl set-hostname k8s-node02

<span class="c"># 参数解释</span>
<span class="c"># </span>
<span class="c"># 参数: set-hostname</span>
<span class="c"># 解释: 这是hostnamectl命令的一个参数，用于设置系统的主机名。</span>
<span class="c"># </span>
<span class="c"># 参数: k8s-master01</span>
<span class="c"># 解释: 这是要设置的主机名，将系统的主机名设置为"k8s-master01"。</span>
</code></pre></div></div>

<h3 id="14配置yum源">1.4.配置yum源</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 其他系统的源地址</span>
<span class="c"># https://help.mirrors.cernet.edu.cn/</span>

<span class="c"># 对于私有仓库</span>
<span class="nb">sed</span> <span class="nt">-e</span> <span class="s1">'s|^mirrorlist=|#mirrorlist=|g'</span> <span class="nt">-e</span> <span class="s1">'s|^#baseurl=http://mirror.centos.org/\$contentdir|baseurl=http://192.168.1.123/centos|g'</span> <span class="nt">-i</span>.bak  /etc/yum.repos.d/CentOS-<span class="k">*</span>.repo

<span class="c"># 对于 Ubuntu</span>
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s1">'s/cn.archive.ubuntu.com/mirrors.ustc.edu.cn/g'</span> /etc/apt/sources.list

<span class="c"># epel扩展源</span>
<span class="nb">sudo </span>yum <span class="nb">install</span> <span class="nt">-y</span> epel-release
<span class="nb">sudo sed</span> <span class="nt">-e</span> <span class="s1">'s!^metalink=!#metalink=!g'</span> <span class="se">\</span>
    <span class="nt">-e</span> <span class="s1">'s!^#baseurl=!baseurl=!g'</span> <span class="se">\</span>
    <span class="nt">-e</span> <span class="s1">'s!https\?://download\.fedoraproject\.org/pub/epel!https://mirror.nju.edu.cn/epel!g'</span> <span class="se">\</span>
    <span class="nt">-e</span> <span class="s1">'s!https\?://download\.example/pub/epel!https://mirror.nju.edu.cn/epel!g'</span> <span class="se">\</span>
    <span class="nt">-i</span> /etc/yum.repos.d/epel<span class="o">{</span>,-testing<span class="o">}</span>.repo

<span class="c"># 对于 CentOS 7</span>
<span class="nb">sudo sed</span> <span class="nt">-e</span> <span class="s1">'s|^mirrorlist=|#mirrorlist=|g'</span> <span class="se">\</span>
         <span class="nt">-e</span> <span class="s1">'s|^#baseurl=http://mirror.centos.org/centos|baseurl=https://mirror.nju.edu.cn/centos|g'</span> <span class="se">\</span>
         <span class="nt">-i</span>.bak <span class="se">\</span>
         /etc/yum.repos.d/CentOS-<span class="k">*</span>.repo

<span class="c"># 对于 CentOS 8</span>
<span class="nb">sudo sed</span> <span class="nt">-e</span> <span class="s1">'s|^mirrorlist=|#mirrorlist=|g'</span> <span class="se">\</span>
         <span class="nt">-e</span> <span class="s1">'s|^#baseurl=http://mirror.centos.org/$contentdir|baseurl=https://mirror.nju.edu.cn/centos|g'</span> <span class="se">\</span>
         <span class="nt">-i</span>.bak <span class="se">\</span>
         /etc/yum.repos.d/CentOS-<span class="k">*</span>.repo

<span class="c"># 对于CentOS 9</span>
<span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="sh">'</span><span class="no">EOF</span><span class="sh">' &gt; /etc/yum.repos.d/centos.repo
[baseos]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - BaseOS
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/BaseOS/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-baseos-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=1

[baseos-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - BaseOS - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/BaseOS/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-baseos-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[baseos-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - BaseOS - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/BaseOS/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-baseos-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[appstream]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - AppStream
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/AppStream/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-appstream-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=1

[appstream-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - AppStream - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/AppStream/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-appstream-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[appstream-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - AppStream - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/AppStream/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-appstream-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[crb]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - CRB
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/CRB/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-crb-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=1

[crb-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - CRB - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/CRB/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-crb-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[crb-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - CRB - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/CRB/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-crb-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0
</span><span class="no">EOF

</span><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="sh">'</span><span class="no">EOF</span><span class="sh">' &gt; /etc/yum.repos.d/centos-addons.repo
[highavailability]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - HighAvailability
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/HighAvailability/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-highavailability-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=0

[highavailability-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - HighAvailability - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/HighAvailability/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-highavailability-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[highavailability-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - HighAvailability - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/HighAvailability/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-highavailability-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[nfv]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - NFV
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/NFV/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-nfv-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=0

[nfv-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - NFV - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/NFV/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-nfv-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[nfv-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - NFV - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/NFV/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-nfv-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[rt]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - RT
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/RT/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-rt-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=0

[rt-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - RT - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/RT/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-rt-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[rt-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - RT - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/RT/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-rt-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[resilientstorage]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - ResilientStorage
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/ResilientStorage/</span><span class="nv">$basearch</span><span class="sh">/os
# metalink=https://mirrors.centos.org/metalink?repo=centos-resilientstorage-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=0

[resilientstorage-debuginfo]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - ResilientStorage - Debug
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/ResilientStorage/</span><span class="nv">$basearch</span><span class="sh">/debug/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-resilientstorage-debug-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[resilientstorage-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - ResilientStorage - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/</span><span class="nv">$releasever</span><span class="sh">-stream/ResilientStorage/source/tree/
# metalink=https://mirrors.centos.org/metalink?repo=centos-resilientstorage-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-centosofficial
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0

[extras-common]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - Extras packages
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/SIGs/</span><span class="nv">$releasever</span><span class="sh">-stream/extras/</span><span class="nv">$basearch</span><span class="sh">/extras-common
# metalink=https://mirrors.centos.org/metalink?repo=centos-extras-sig-extras-common-</span><span class="nv">$stream</span><span class="sh">&amp;arch=</span><span class="nv">$basearch</span><span class="sh">&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-SIG-Extras-SHA512
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
countme=1
enabled=1

[extras-common-source]
name=CentOS Stream </span><span class="nv">$releasever</span><span class="sh"> - Extras packages - Source
baseurl=https://mirrors.tuna.tsinghua.edu.cn/centos-stream/SIGs/</span><span class="nv">$releasever</span><span class="sh">-stream/extras/source/extras-common
# metalink=https://mirrors.centos.org/metalink?repo=centos-extras-sig-extras-common-source-</span><span class="nv">$stream</span><span class="sh">&amp;arch=source&amp;protocol=https,http
gpgkey=file:///etc/pki/rpm-gpg/RPM-GPG-KEY-CentOS-SIG-Extras-SHA512
gpgcheck=1
repo_gpgcheck=0
metadata_expire=6h
enabled=0
</span><span class="no">EOF
</span></code></pre></div></div>

<h3 id="15安装一些必备工具">1.5.安装一些必备工具</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 对于 Ubuntu</span>
apt update <span class="o">&amp;&amp;</span> apt upgrade <span class="nt">-y</span> <span class="o">&amp;&amp;</span> apt <span class="nb">install</span> <span class="nt">-y</span> wget psmisc vim net-tools nfs-kernel-server telnet lvm2 git <span class="nb">tar </span>curl

<span class="c"># 对于 CentOS 7</span>
yum update <span class="nt">-y</span> <span class="o">&amp;&amp;</span> yum <span class="nt">-y</span> <span class="nb">install  </span>wget psmisc vim net-tools nfs-utils telnet yum-utils device-mapper-persistent-data lvm2 git <span class="nb">tar </span>curl

<span class="c"># 对于 CentOS 8</span>
yum update <span class="nt">-y</span> <span class="o">&amp;&amp;</span> yum <span class="nt">-y</span> <span class="nb">install </span>wget psmisc vim net-tools nfs-utils telnet yum-utils device-mapper-persistent-data lvm2 git network-scripts <span class="nb">tar </span>curl

<span class="c"># 对于 CentOS 9</span>
yum update <span class="nt">-y</span> <span class="o">&amp;&amp;</span> yum <span class="nt">-y</span> <span class="nb">install </span>wget psmisc vim net-tools nfs-utils telnet yum-utils device-mapper-persistent-data lvm2 git <span class="nb">tar </span>curl
</code></pre></div></div>

<h4 id="151-下载离线所需文件可选">1.5.1 下载离线所需文件(可选)</h4>

<p>在互联网服务器上安装一个一模一样的系统进行下载所需包</p>

<h5 id="centos7">CentOS7</h5>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载必要工具</span>
yum <span class="nt">-y</span> <span class="nb">install </span>createrepo yum-utils wget epel<span class="k">*</span>

<span class="c"># 下载全量依赖包</span>
repotrack createrepo wget psmisc vim net-tools nfs-utils telnet yum-utils device-mapper-persistent-data lvm2 git <span class="nb">tar </span>curl gcc keepalived haproxy bash-completion chrony sshpass ipvsadm ipset sysstat conntrack libseccomp

<span class="c"># 删除libseccomp</span>
<span class="nb">rm</span> <span class="nt">-rf</span> libseccomp-<span class="k">*</span>.rpm

<span class="c"># 下载libseccomp</span>
wget http://rpmfind.net/linux/centos/8-stream/BaseOS/x86_64/os/Packages/libseccomp-2.5.1-1.el8.x86_64.rpm

<span class="c"># 创建yum源信息</span>
createrepo <span class="nt">-u</span> <span class="nt">-d</span> /data/centos7/

<span class="c"># 拷贝包到内网机器上</span>
scp <span class="nt">-r</span> /data/centos7/ root@192.168.1.31:
scp <span class="nt">-r</span> /data/centos7/ root@192.168.1.32:
scp <span class="nt">-r</span> /data/centos7/ root@192.168.1.33:
scp <span class="nt">-r</span> /data/centos7/ root@192.168.1.34:
scp <span class="nt">-r</span> /data/centos7/ root@192.168.1.35:

<span class="c"># 在内网机器上创建repo配置文件</span>
<span class="nb">rm</span> <span class="nt">-rf</span> /etc/yum.repos.d/<span class="k">*</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/yum.repos.d/123.repo  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
[cby]
name=CentOS-</span><span class="nv">$releasever</span><span class="sh"> - Media
baseurl=file:///root/centos7/
gpgcheck=0
enabled=1
</span><span class="no">EOF

</span><span class="c"># 安装下载好的包</span>
yum clean all
yum makecache
yum <span class="nb">install</span> /root/centos7/<span class="k">*</span> <span class="nt">--skip-broken</span> <span class="nt">-y</span>

<span class="c">#### 备注 #####</span>
<span class="c"># 安装完成后，可能还会出现yum无法使用那么再次执行</span>
<span class="nb">rm</span> <span class="nt">-rf</span> /etc/yum.repos.d/<span class="k">*</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/yum.repos.d/123.repo  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
[cby]
name=CentOS-</span><span class="nv">$releasever</span><span class="sh"> - Media
baseurl=file:///root/centos7/
gpgcheck=0
enabled=1
</span><span class="no">EOF
</span>yum clean all
yum makecache
yum <span class="nb">install</span> /root/centos7/<span class="k">*</span>.rpm <span class="nt">--skip-broken</span> <span class="nt">-y</span>

<span class="c">#### 备注 #####</span>
<span class="c"># 安装 chrony 和 libseccomp</span>
<span class="c"># yum install /root/centos7/libseccomp-2.5.1*.rpm -y</span>
<span class="c"># yum install /root/centos7/chrony-*.rpm -y</span>
</code></pre></div></div>

<h5 id="centos8">CentOS8</h5>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载必要工具</span>
yum <span class="nt">-y</span> <span class="nb">install </span>createrepo yum-utils wget epel<span class="k">*</span>

<span class="c"># 下载全量依赖包</span>
repotrack wget psmisc vim net-tools nfs-utils telnet yum-utils device-mapper-persistent-data lvm2 git network-scripts <span class="nb">tar </span>curl gcc keepalived haproxy bash-completion chrony sshpass ipvsadm ipset sysstat conntrack libseccomp

<span class="c"># 创建yum源信息</span>
createrepo <span class="nt">-u</span> <span class="nt">-d</span> /data/centos8/

<span class="c"># 拷贝包到内网机器上</span>
scp <span class="nt">-r</span> centos8/ root@192.168.1.31:
scp <span class="nt">-r</span> centos8/ root@192.168.1.32:
scp <span class="nt">-r</span> centos8/ root@192.168.1.33:
scp <span class="nt">-r</span> centos8/ root@192.168.1.34:
scp <span class="nt">-r</span> centos8/ root@192.168.1.35:

<span class="c"># 在内网机器上创建repo配置文件</span>
<span class="nb">rm</span> <span class="nt">-rf</span> /etc/yum.repos.d/<span class="k">*</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/yum.repos.d/123.repo  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
[cby]
name=CentOS-</span><span class="nv">$releasever</span><span class="sh"> - Media
baseurl=file:///root/centos8/
gpgcheck=0
enabled=1
</span><span class="no">EOF

</span><span class="c"># 安装下载好的包</span>
yum clean all
yum makecache
yum <span class="nb">install</span> /root/centos8/<span class="k">*</span> <span class="nt">--skip-broken</span> <span class="nt">-y</span>

<span class="c">#### 备注 #####</span>
<span class="c"># 安装完成后，可能还会出现yum无法使用那么再次执行</span>
<span class="nb">rm</span> <span class="nt">-rf</span> /etc/yum.repos.d/<span class="k">*</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/yum.repos.d/123.repo  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
[cby]
name=CentOS-</span><span class="nv">$releasever</span><span class="sh"> - Media
baseurl=file:///root/centos8/
gpgcheck=0
enabled=1
</span><span class="no">EOF
</span>yum clean all
yum makecache
yum <span class="nb">install</span> /root/centos8/<span class="k">*</span>.rpm <span class="nt">--skip-broken</span> <span class="nt">-y</span>
</code></pre></div></div>

<h5 id="centos9">CentOS9</h5>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载必要工具</span>
yum <span class="nt">-y</span> <span class="nb">install </span>createrepo yum-utils wget epel<span class="k">*</span>

<span class="c"># 下载全量依赖包</span>
repotrack wget psmisc vim net-tools nfs-utils telnet yum-utils device-mapper-persistent-data lvm2 git <span class="nb">tar </span>curl
<span class="c"># 创建yum源信息</span>
createrepo <span class="nt">-u</span> <span class="nt">-d</span> centos9/

<span class="c"># 拷贝包到内网机器上</span>
scp <span class="nt">-r</span> centos9/ root@192.168.1.31:
scp <span class="nt">-r</span> centos9/ root@192.168.1.32:
scp <span class="nt">-r</span> centos9/ root@192.168.1.33:
scp <span class="nt">-r</span> centos9/ root@192.168.1.34:
scp <span class="nt">-r</span> centos9/ root@192.168.1.35:

<span class="c"># 在内网机器上创建repo配置文件</span>
<span class="nb">rm</span> <span class="nt">-rf</span> /etc/yum.repos.d/<span class="k">*</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/yum.repos.d/123.repo  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
[cby]
name=CentOS-</span><span class="nv">$releasever</span><span class="sh"> - Media
baseurl=file:///root/centos9/
gpgcheck=0
enabled=1
</span><span class="no">EOF

</span><span class="c"># 安装下载好的包</span>
yum clean all
yum makecache
yum <span class="nb">install</span> /root/centos9/<span class="k">*</span>.rpm <span class="nt">--skip-broken</span> <span class="nt">-y</span>
</code></pre></div></div>

<h5 id="ubuntu-下载包和依赖">Ubuntu 下载包和依赖</h5>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="nv">logfile</span><span class="o">=</span>123.log
<span class="nv">ret</span><span class="o">=</span><span class="s2">""</span>
<span class="k">function </span>getDepends<span class="o">()</span>
<span class="o">{</span>
   <span class="nb">echo</span> <span class="s2">"fileName is"</span> <span class="nv">$1</span><span class="o">&gt;&gt;</span><span class="nv">$logfile</span>
   <span class="c"># use tr to del &lt; &gt;</span>
   <span class="nv">ret</span><span class="o">=</span><span class="sb">`</span>apt-cache depends <span class="nv">$1</span>|grep Depends |cut <span class="nt">-d</span>: <span class="nt">-f2</span> |tr <span class="nt">-d</span> <span class="s2">"&lt;&gt;"</span><span class="sb">`</span>
   <span class="nb">echo</span> <span class="nv">$ret</span>|tee  <span class="nt">-a</span> <span class="nv">$logfile</span>
<span class="o">}</span>
<span class="c"># 需要获取其所依赖包的包</span>
<span class="nv">libs</span><span class="o">=</span><span class="s2">"wget psmisc vim net-tools nfs-kernel-server telnet lvm2 git tar curl gcc keepalived haproxy bash-completion chrony sshpass ipvsadm ipset sysstat conntrack libseccomp"</span>

<span class="c"># download libs dependen. deep in 3</span>
<span class="nv">i</span><span class="o">=</span>0
<span class="k">while</span> <span class="o">[</span> <span class="nv">$i</span> <span class="nt">-lt</span> 3 <span class="o">]</span> <span class="p">;</span>
<span class="k">do
    </span><span class="nb">let </span>i++
    <span class="nb">echo</span> <span class="nv">$i</span>
    <span class="c"># download libs</span>
    <span class="nv">newlist</span><span class="o">=</span><span class="s2">" "</span>
    <span class="k">for </span>j <span class="k">in</span> <span class="nv">$libs</span>
    <span class="k">do
        </span><span class="nv">added</span><span class="o">=</span><span class="s2">"</span><span class="si">$(</span>getDepends <span class="nv">$j</span><span class="si">)</span><span class="s2">"</span>
        <span class="nv">newlist</span><span class="o">=</span><span class="s2">"</span><span class="nv">$newlist</span><span class="s2"> </span><span class="nv">$added</span><span class="s2">"</span>
        apt <span class="nb">install</span> <span class="nv">$added</span> <span class="nt">--reinstall</span> <span class="nt">-d</span> <span class="nt">-y</span>
    <span class="k">done

    </span><span class="nv">libs</span><span class="o">=</span><span class="nv">$newlist</span>
<span class="k">done</span>

<span class="c"># 创建源信息</span>
apt <span class="nb">install </span>dpkg-dev
<span class="nb">sudo cp</span> /var/cache/apt/archives/<span class="k">*</span>.deb /data/ubuntu/ <span class="nt">-r</span>
dpkg-scanpackages <span class="nb">.</span> /dev/null |gzip <span class="o">&gt;</span> /data/ubuntu/Packages.gz <span class="nt">-r</span>

<span class="c"># 拷贝包到内网机器上</span>
scp <span class="nt">-r</span> ubuntu/ root@192.168.1.31:
scp <span class="nt">-r</span> ubuntu/ root@192.168.1.32:
scp <span class="nt">-r</span> ubuntu/ root@192.168.1.33:
scp <span class="nt">-r</span> ubuntu/ root@192.168.1.34:
scp <span class="nt">-r</span> ubuntu/ root@192.168.1.35:

<span class="c"># 在内网机器上配置apt源</span>
vim /etc/apt/sources.list
<span class="nb">cat</span> /etc/apt/sources.list
deb file:////root/ ubuntu/

<span class="c"># 安装deb包</span>
apt <span class="nb">install</span> ./<span class="k">*</span>.deb
</code></pre></div></div>

<h3 id="16选择性下载需要工具">1.6.选择性下载需要工具</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="c"># 查看版本地址：</span>
<span class="c"># </span>
<span class="c"># https://github.com/containernetworking/plugins/releases/</span>
<span class="c"># https://github.com/containerd/containerd/releases/</span>
<span class="c"># https://github.com/kubernetes-sigs/cri-tools/releases/</span>
<span class="c"># https://github.com/Mirantis/cri-dockerd/releases/</span>
<span class="c"># https://github.com/etcd-io/etcd/releases/</span>
<span class="c"># https://github.com/cloudflare/cfssl/releases/</span>
<span class="c"># https://github.com/kubernetes/kubernetes/tree/master/CHANGELOG</span>
<span class="c"># https://download.docker.com/linux/static/stable/x86_64/</span>
<span class="c"># https://github.com/opencontainers/runc/releases/</span>
<span class="c"># https://github.com/helm/helm/tags</span>
<span class="c"># http://nginx.org/download/</span>

<span class="c"># Version numbers</span>
<span class="nv">cni_plugins_version</span><span class="o">=</span><span class="s1">'v1.9.0'</span>
<span class="nv">cri_containerd_cni_version</span><span class="o">=</span><span class="s1">'2.2.1'</span>
<span class="nv">crictl_version</span><span class="o">=</span><span class="s1">'v1.35.0'</span>
<span class="nv">cri_dockerd_version</span><span class="o">=</span><span class="s1">'0.3.21'</span>
<span class="nv">etcd_version</span><span class="o">=</span><span class="s1">'v3.6.7'</span>
<span class="nv">cfssl_version</span><span class="o">=</span><span class="s1">'1.6.5'</span>
<span class="nv">kubernetes_server_version</span><span class="o">=</span><span class="s1">'1.35.0'</span>
<span class="nv">docker_version</span><span class="o">=</span><span class="s1">'29.1.3'</span>
<span class="nv">runc_version</span><span class="o">=</span><span class="s1">'1.4.0'</span>
<span class="nv">kernel_version</span><span class="o">=</span><span class="s1">'5.4.278'</span>
<span class="nv">helm_version</span><span class="o">=</span><span class="s1">'4.0.4'</span>
<span class="nv">nginx_version</span><span class="o">=</span><span class="s1">'1.29.4'</span>

<span class="c"># URLs </span>
<span class="nv">base_url</span><span class="o">=</span><span class="s1">'https://github.com'</span>
<span class="nv">kernel_url</span><span class="o">=</span><span class="s2">"http://mirrors.tuna.tsinghua.edu.cn/elrepo/kernel/el7/x86_64/RPMS/kernel-lt-</span><span class="k">${</span><span class="nv">kernel_version</span><span class="k">}</span><span class="s2">-1.el7.elrepo.x86_64.rpm"</span>
<span class="nv">runc_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/opencontainers/runc/releases/download/v</span><span class="k">${</span><span class="nv">runc_version</span><span class="k">}</span><span class="s2">/runc.amd64"</span>
<span class="nv">docker_url</span><span class="o">=</span><span class="s2">"https://mirrors.ustc.edu.cn/docker-ce/linux/static/stable/x86_64/docker-</span><span class="k">${</span><span class="nv">docker_version</span><span class="k">}</span><span class="s2">.tgz"</span>
<span class="nv">cni_plugins_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/containernetworking/plugins/releases/download/</span><span class="k">${</span><span class="nv">cni_plugins_version</span><span class="k">}</span><span class="s2">/cni-plugins-linux-amd64-</span><span class="k">${</span><span class="nv">cni_plugins_version</span><span class="k">}</span><span class="s2">.tgz"</span>
<span class="nv">cri_containerd_cni_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/containerd/containerd/releases/download/v</span><span class="k">${</span><span class="nv">cri_containerd_cni_version</span><span class="k">}</span><span class="s2">/containerd-</span><span class="k">${</span><span class="nv">cri_containerd_cni_version</span><span class="k">}</span><span class="s2">-linux-amd64.tar.gz"</span>
<span class="nv">crictl_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/kubernetes-sigs/cri-tools/releases/download/</span><span class="k">${</span><span class="nv">crictl_version</span><span class="k">}</span><span class="s2">/crictl-</span><span class="k">${</span><span class="nv">crictl_version</span><span class="k">}</span><span class="s2">-linux-amd64.tar.gz"</span>
<span class="nv">cri_dockerd_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/Mirantis/cri-dockerd/releases/download/v</span><span class="k">${</span><span class="nv">cri_dockerd_version</span><span class="k">}</span><span class="s2">/cri-dockerd-</span><span class="k">${</span><span class="nv">cri_dockerd_version</span><span class="k">}</span><span class="s2">.amd64.tgz"</span>
<span class="nv">etcd_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/etcd-io/etcd/releases/download/</span><span class="k">${</span><span class="nv">etcd_version</span><span class="k">}</span><span class="s2">/etcd-</span><span class="k">${</span><span class="nv">etcd_version</span><span class="k">}</span><span class="s2">-linux-amd64.tar.gz"</span>
<span class="nv">cfssl_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/cloudflare/cfssl/releases/download/v</span><span class="k">${</span><span class="nv">cfssl_version</span><span class="k">}</span><span class="s2">/cfssl_</span><span class="k">${</span><span class="nv">cfssl_version</span><span class="k">}</span><span class="s2">_linux_amd64"</span>
<span class="nv">cfssljson_url</span><span class="o">=</span><span class="s2">"</span><span class="k">${</span><span class="nv">base_url</span><span class="k">}</span><span class="s2">/cloudflare/cfssl/releases/download/v</span><span class="k">${</span><span class="nv">cfssl_version</span><span class="k">}</span><span class="s2">/cfssljson_</span><span class="k">${</span><span class="nv">cfssl_version</span><span class="k">}</span><span class="s2">_linux_amd64"</span>
<span class="nv">helm_url</span><span class="o">=</span><span class="s2">"https://mirrors.huaweicloud.com/helm/v</span><span class="k">${</span><span class="nv">helm_version</span><span class="k">}</span><span class="s2">/helm-v</span><span class="k">${</span><span class="nv">helm_version</span><span class="k">}</span><span class="s2">-linux-amd64.tar.gz"</span>
<span class="nv">kubernetes_server_url</span><span class="o">=</span><span class="s2">"https://cdn.dl.k8s.io/release/v</span><span class="k">${</span><span class="nv">kubernetes_server_version</span><span class="k">}</span><span class="s2">/kubernetes-server-linux-amd64.tar.gz"</span>
<span class="nv">nginx_url</span><span class="o">=</span><span class="s2">"http://nginx.org/download/nginx-</span><span class="k">${</span><span class="nv">nginx_version</span><span class="k">}</span><span class="s2">.tar.gz"</span>

<span class="c"># Download packages</span>
<span class="nv">packages</span><span class="o">=(</span>
  <span class="c"># $kernel_url</span>
  <span class="nv">$runc_url</span>
  <span class="nv">$docker_url</span>
  <span class="nv">$cni_plugins_url</span>
  <span class="nv">$cri_containerd_cni_url</span>
  <span class="nv">$crictl_url</span>
  <span class="nv">$cri_dockerd_url</span>
  <span class="nv">$etcd_url</span>
  <span class="nv">$cfssl_url</span>
  <span class="nv">$cfssljson_url</span>
  <span class="nv">$helm_url</span>
  <span class="nv">$kubernetes_server_url</span>
  <span class="nv">$nginx_url</span>
<span class="o">)</span>

<span class="k">for </span>package_url <span class="k">in</span> <span class="s2">"</span><span class="k">${</span><span class="nv">packages</span><span class="p">[@]</span><span class="k">}</span><span class="s2">"</span><span class="p">;</span> <span class="k">do
  </span><span class="nv">filename</span><span class="o">=</span><span class="si">$(</span><span class="nb">basename</span> <span class="s2">"</span><span class="nv">$package_url</span><span class="s2">"</span><span class="si">)</span>
  <span class="k">if </span>curl <span class="nt">--parallel</span> <span class="nt">--parallel-immediate</span> <span class="nt">-k</span> <span class="nt">-L</span> <span class="nt">-C</span> - <span class="nt">-o</span> <span class="s2">"</span><span class="nv">$filename</span><span class="s2">"</span> <span class="s2">"</span><span class="nv">$package_url</span><span class="s2">"</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">echo</span> <span class="s2">"Downloaded </span><span class="nv">$filename</span><span class="s2">"</span>
  <span class="k">else
    </span><span class="nb">echo</span> <span class="s2">"Failed to download </span><span class="nv">$filename</span><span class="s2">"</span>
    <span class="nb">exit </span>1
  <span class="k">fi
done</span>
</code></pre></div></div>

<h3 id="17关闭防火墙">1.7.关闭防火墙</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Ubuntu忽略，CentOS执行</span>
systemctl disable <span class="nt">--now</span> firewalld
</code></pre></div></div>

<h3 id="18关闭selinux">1.8.关闭SELinux</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Ubuntu忽略，CentOS执行</span>
setenforce 0
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s1">'s#SELINUX=enforcing#SELINUX=disabled#g'</span> /etc/selinux/config

<span class="c"># 参数解释</span>
<span class="c"># </span>
<span class="c"># setenforce 0</span>
<span class="c"># 此命令用于设置 SELinux 的执行模式。0 表示关闭 SELinux。</span>
<span class="c"># </span>
<span class="c"># sed -i 's#SELINUX=enforcing#SELINUX=disabled#g' /etc/selinux/config</span>
<span class="c"># 该命令使用 sed 工具来编辑 /etc/selinux/config 文件。其中 '-i' 参数表示直接修改原文件，而不是输出到终端或另一个文件。's#SELINUX=enforcing#SELINUX=disabled#g' 是 sed 的替换命令，它将文件中所有的 "SELINUX=enforcing" 替换为 "SELINUX=disabled"。这里的 '#' 是分隔符，用于替代传统的 '/' 分隔符，以避免与路径中的 '/' 冲突。</span>
</code></pre></div></div>

<h3 id="19关闭交换分区">1.9.关闭交换分区</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sed</span> <span class="nt">-ri</span> <span class="s1">'s/.*swap.*/#&amp;/'</span> /etc/fstab
swapoff <span class="nt">-a</span> <span class="o">&amp;&amp;</span> sysctl <span class="nt">-w</span> vm.swappiness<span class="o">=</span>0

<span class="nb">cat</span> /etc/fstab
<span class="c"># /dev/mapper/centos-swap swap                    swap    defaults        0 0</span>


<span class="c"># 参数解释：</span>
<span class="c"># </span>
<span class="c"># -ri: 这个参数用于在原文件中替换匹配的模式。-r表示扩展正则表达式，-i允许直接修改文件。</span>
<span class="c"># 's/.*swap.*/#&amp;/': 这是一个sed命令，用于在文件/etc/fstab中找到包含swap的行，并在行首添加#来注释掉该行。</span>
<span class="c"># /etc/fstab: 这是一个文件路径，即/etc/fstab文件，用于存储文件系统表。</span>
<span class="c"># swapoff -a: 这个命令用于关闭所有启用的交换分区。</span>
<span class="c"># sysctl -w vm.swappiness=0: 这个命令用于修改vm.swappiness参数的值为0，表示系统在物理内存充足时更倾向于使用物理内存而非交换分区。</span>
</code></pre></div></div>

<h3 id="110网络配置俩种方式二选一">1.10.网络配置（俩种方式二选一）</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Ubuntu忽略，CentOS执行，CentOS9不支持方式一</span>

<span class="c"># 方式一</span>
<span class="c"># systemctl disable --now NetworkManager</span>
<span class="c"># systemctl start network &amp;&amp; systemctl enable network</span>

<span class="c"># 方式二</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/NetworkManager/conf.d/calico.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
[keyfile]
unmanaged-devices=interface-name:cali*;interface-name:tunl*
</span><span class="no">EOF
</span>systemctl restart NetworkManager

<span class="c"># 参数解释</span>
<span class="c">#</span>
<span class="c"># 这个参数用于指定不由 NetworkManager 管理的设备。它由以下两个部分组成</span>
<span class="c"># </span>
<span class="c"># interface-name:cali*</span>
<span class="c"># 表示以 "cali" 开头的接口名称被排除在 NetworkManager 管理之外。例如，"cali0", "cali1" 等接口不受 NetworkManager 管理。</span>
<span class="c"># </span>
<span class="c"># interface-name:tunl*</span>
<span class="c"># 表示以 "tunl" 开头的接口名称被排除在 NetworkManager 管理之外。例如，"tunl0", "tunl1" 等接口不受 NetworkManager 管理。</span>
<span class="c"># </span>
<span class="c"># 通过使用这个参数，可以将特定的接口排除在 NetworkManager 的管理范围之外，以便其他工具或进程可以独立地管理和配置这些接口。</span>
</code></pre></div></div>

<h3 id="111进行时间同步">1.11.进行时间同步</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 服务端</span>
<span class="c"># apt install chrony -y</span>
yum <span class="nb">install </span>chrony <span class="nt">-y</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/chrony.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
pool ntp.aliyun.com iburst
driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
allow 192.168.1.0/24
local stratum 10
keyfile /etc/chrony.keys
leapsectz right/UTC
logdir /var/log/chrony
</span><span class="no">EOF

</span>systemctl restart chronyd <span class="p">;</span> systemctl <span class="nb">enable </span>chronyd

<span class="c"># 客户端</span>
<span class="c"># apt install chrony -y</span>
yum <span class="nb">install </span>chrony <span class="nt">-y</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/chrony.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
pool 192.168.1.31 iburst
driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
keyfile /etc/chrony.keys
leapsectz right/UTC
logdir /var/log/chrony
</span><span class="no">EOF

</span>systemctl restart chronyd <span class="p">;</span> systemctl <span class="nb">enable </span>chronyd

<span class="c">#使用客户端进行验证</span>
chronyc sources <span class="nt">-v</span>

<span class="c"># 参数解释</span>
<span class="c">#</span>
<span class="c"># pool ntp.aliyun.com iburst</span>
<span class="c"># 指定使用ntp.aliyun.com作为时间服务器池，iburst选项表示在初始同步时会发送多个请求以加快同步速度。</span>
<span class="c"># </span>
<span class="c"># driftfile /var/lib/chrony/drift</span>
<span class="c"># 指定用于保存时钟漂移信息的文件路径。</span>
<span class="c"># </span>
<span class="c"># makestep 1.0 3</span>
<span class="c"># 设置当系统时间与服务器时间偏差大于1秒时，会以1秒的步长进行调整。如果偏差超过3秒，则立即进行时间调整。</span>
<span class="c"># </span>
<span class="c"># rtcsync</span>
<span class="c"># 启用硬件时钟同步功能，可以提高时钟的准确性。</span>
<span class="c"># </span>
<span class="c"># allow 192.168.0.0/24</span>
<span class="c"># 允许192.168.0.0/24网段范围内的主机与chrony进行时间同步。</span>
<span class="c"># </span>
<span class="c"># local stratum 10</span>
<span class="c"># 将本地时钟设为stratum 10，stratum值表示时钟的准确度，值越小表示准确度越高。</span>
<span class="c"># </span>
<span class="c"># keyfile /etc/chrony.keys</span>
<span class="c"># 指定使用的密钥文件路径，用于对时间同步进行身份验证。</span>
<span class="c"># </span>
<span class="c"># leapsectz right/UTC</span>
<span class="c"># 指定时区为UTC。</span>
<span class="c"># </span>
<span class="c"># logdir /var/log/chrony</span>
<span class="c"># 指定日志文件存放目录。</span>
</code></pre></div></div>

<h3 id="112配置ulimit">1.12.配置ulimit</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ulimit</span> <span class="nt">-SHn</span> 65535
<span class="nb">cat</span> <span class="o">&gt;&gt;</span> /etc/security/limits.conf <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
* soft nofile 655360
* hard nofile 131072
* soft nproc 655350
* hard nproc 655350
* seft memlock unlimited
* hard memlock unlimitedd
</span><span class="no">EOF

</span><span class="c"># 参数解释</span>
<span class="c">#</span>
<span class="c"># soft nofile 655360</span>
<span class="c"># soft表示软限制，nofile表示一个进程可打开的最大文件数，默认值为1024。这里的软限制设置为655360，即一个进程可打开的最大文件数为655360。</span>
<span class="c">#</span>
<span class="c"># hard nofile 131072</span>
<span class="c"># hard表示硬限制，即系统设置的最大值。nofile表示一个进程可打开的最大文件数，默认值为4096。这里的硬限制设置为131072，即系统设置的最大文件数为131072。</span>
<span class="c">#</span>
<span class="c"># soft nproc 655350</span>
<span class="c"># soft表示软限制，nproc表示一个用户可创建的最大进程数，默认值为30720。这里的软限制设置为655350，即一个用户可创建的最大进程数为655350。</span>
<span class="c">#</span>
<span class="c"># hard nproc 655350</span>
<span class="c"># hard表示硬限制，即系统设置的最大值。nproc表示一个用户可创建的最大进程数，默认值为4096。这里的硬限制设置为655350，即系统设置的最大进程数为655350。</span>
<span class="c">#</span>
<span class="c"># seft memlock unlimited</span>
<span class="c"># seft表示软限制，memlock表示一个进程可锁定在RAM中的最大内存，默认值为64 KB。这里的软限制设置为unlimited，即一个进程可锁定的最大内存为无限制。</span>
<span class="c">#</span>
<span class="c"># hard memlock unlimited</span>
<span class="c"># hard表示硬限制，即系统设置的最大值。memlock表示一个进程可锁定在RAM中的最大内存，默认值为64 KB。这里的硬限制设置为unlimited，即系统设置的最大内存锁定为无限制。</span>
</code></pre></div></div>

<h3 id="113配置免密登录">1.13.配置免密登录</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># apt install -y sshpass</span>
yum <span class="nb">install</span> <span class="nt">-y</span> sshpass
ssh-keygen <span class="nt">-f</span> /root/.ssh/id_rsa <span class="nt">-P</span> <span class="s1">''</span>
<span class="nb">export </span><span class="nv">IP</span><span class="o">=</span><span class="s2">"192.168.1.31 192.168.1.32 192.168.1.33 192.168.1.34 192.168.1.35"</span>
<span class="nb">export </span><span class="nv">SSHPASS</span><span class="o">=</span>123123
<span class="k">for </span>HOST <span class="k">in</span> <span class="nv">$IP</span><span class="p">;</span><span class="k">do
     </span>sshpass <span class="nt">-e</span> ssh-copy-id <span class="nt">-o</span> <span class="nv">StrictHostKeyChecking</span><span class="o">=</span>no <span class="nv">$HOST</span>
<span class="k">done</span>

<span class="c"># 这段脚本的作用是在一台机器上安装sshpass工具，并通过sshpass自动将本机的SSH公钥复制到多个远程主机上，以实现无需手动输入密码的SSH登录。</span>
<span class="c"># </span>
<span class="c"># 具体解释如下：</span>
<span class="c"># </span>
<span class="c"># 1. `apt install -y sshpass` 或 `yum install -y sshpass`：通过包管理器（apt或yum）安装sshpass工具，使得后续可以使用sshpass命令。</span>
<span class="c"># </span>
<span class="c"># 2. `ssh-keygen -f /root/.ssh/id_rsa -P ''`：生成SSH密钥对。该命令会在/root/.ssh目录下生成私钥文件id_rsa和公钥文件id_rsa.pub，同时不设置密码（即-P参数后面为空），方便后续通过ssh-copy-id命令自动复制公钥。</span>
<span class="c"># </span>
<span class="c"># 3. `export IP="192.168.1.31 192.168.1.32 192.168.1.33 192.168.1.34 192.168.1.35"`：设置一个包含多个远程主机IP地址的环境变量IP，用空格分隔开，表示要将SSH公钥复制到这些远程主机上。</span>
<span class="c"># </span>
<span class="c"># 4. `export SSHPASS=123123`：设置环境变量SSHPASS，将sshpass所需的SSH密码（在这里是"123123"）赋值给它，这样sshpass命令可以自动使用这个密码进行登录。</span>
<span class="c"># </span>
<span class="c"># 5. `for HOST in $IP;do`：遍历环境变量IP中的每个IP地址，并将当前IP地址赋值给变量HOST。</span>
<span class="c"># </span>
<span class="c"># 6. `sshpass -e ssh-copy-id -o StrictHostKeyChecking=no $HOST`：使用sshpass工具复制本机的SSH公钥到远程主机。其中，-e选项表示使用环境变量中的密码（即SSHPASS）进行登录，-o StrictHostKeyChecking=no选项表示连接时不检查远程主机的公钥，以避免交互式确认。</span>
<span class="c"># </span>
<span class="c"># 通过这段脚本，可以方便地将本机的SSH公钥复制到多个远程主机上，实现无需手动输入密码的SSH登录。</span>
</code></pre></div></div>

<h3 id="114添加启用源">1.14.添加启用源</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Ubuntu忽略，CentOS执行</span>

<span class="c"># 为 RHEL-9或 CentOS-9配置源</span>
yum <span class="nb">install </span>https://www.elrepo.org/elrepo-release-9.el9.elrepo.noarch.rpm <span class="nt">-y</span> 
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@mirrorlist@#mirrorlist@g"</span> /etc/yum.repos.d/elrepo.repo 
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@elrepo.org/linux@mirrors.tuna.tsinghua.edu.cn/elrepo@g"</span> /etc/yum.repos.d/elrepo.repo 

<span class="c"># 为 RHEL-8或 CentOS-8配置源</span>
yum <span class="nb">install </span>https://www.elrepo.org/elrepo-release-8.el8.elrepo.noarch.rpm <span class="nt">-y</span> 
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@mirrorlist@#mirrorlist@g"</span> /etc/yum.repos.d/elrepo.repo 
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@elrepo.org/linux@mirrors.tuna.tsinghua.edu.cn/elrepo@g"</span> /etc/yum.repos.d/elrepo.repo 

<span class="c"># 为 RHEL-7 SL-7 或 CentOS-7 安装 ELRepo </span>
yum <span class="nb">install </span>https://www.elrepo.org/elrepo-release-7.el7.elrepo.noarch.rpm <span class="nt">-y</span> 
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@mirrorlist@#mirrorlist@g"</span> /etc/yum.repos.d/elrepo.repo 
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@elrepo.org/linux@mirrors.tuna.tsinghua.edu.cn/elrepo@g"</span> /etc/yum.repos.d/elrepo.repo 

<span class="c"># 查看可用安装包</span>
yum  <span class="nt">--disablerepo</span><span class="o">=</span><span class="s2">"*"</span>  <span class="nt">--enablerepo</span><span class="o">=</span><span class="s2">"elrepo-kernel"</span>  list  available
</code></pre></div></div>

<h3 id="115升级内核至418版本以上">1.15.升级内核至4.18版本以上</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Ubuntu忽略，CentOS执行</span>

<span class="c"># 安装最新的内核</span>
<span class="c"># 我这里选择的是稳定版kernel-ml   如需更新长期维护版本kernel-lt  </span>
yum <span class="nt">-y</span> <span class="nt">--enablerepo</span><span class="o">=</span>elrepo-kernel  <span class="nb">install  </span>kernel-ml

<span class="c"># 查看已安装那些内核</span>
rpm <span class="nt">-qa</span> | <span class="nb">grep </span>kernel

<span class="c"># 查看默认内核</span>
grubby <span class="nt">--default-kernel</span>

<span class="c"># 若不是最新的使用命令设置</span>
grubby <span class="nt">--set-default</span> <span class="si">$(</span><span class="nb">ls</span> /boot/vmlinuz-<span class="k">*</span> | <span class="nb">grep </span>elrepo<span class="si">)</span>

<span class="c"># 重启生效</span>
reboot

<span class="c"># v8 整合命令为：</span>
yum <span class="nb">install </span>https://www.elrepo.org/elrepo-release-9.el9.elrepo.noarch.rpm <span class="nt">-y</span> <span class="p">;</span> <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@mirrorlist@#mirrorlist@g"</span> /etc/yum.repos.d/elrepo.repo <span class="p">;</span> <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@elrepo.org/linux@mirrors.tuna.tsinghua.edu.cn/elrepo@g"</span> /etc/yum.repos.d/elrepo.repo <span class="p">;</span> yum  <span class="nt">--disablerepo</span><span class="o">=</span><span class="s2">"*"</span>  <span class="nt">--enablerepo</span><span class="o">=</span><span class="s2">"elrepo-kernel"</span>  list  available <span class="nt">-y</span> <span class="p">;</span> yum  <span class="nt">--enablerepo</span><span class="o">=</span>elrepo-kernel  <span class="nb">install </span>kernel-lt <span class="nt">-y</span> <span class="p">;</span> grubby <span class="nt">--default-kernel</span> <span class="p">;</span> reboot 

<span class="c"># v8 整合命令为：</span>
yum <span class="nb">install </span>https://www.elrepo.org/elrepo-release-8.el8.elrepo.noarch.rpm <span class="nt">-y</span> <span class="p">;</span> <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@mirrorlist@#mirrorlist@g"</span> /etc/yum.repos.d/elrepo.repo <span class="p">;</span> <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@elrepo.org/linux@mirrors.tuna.tsinghua.edu.cn/elrepo@g"</span> /etc/yum.repos.d/elrepo.repo <span class="p">;</span> yum  <span class="nt">--disablerepo</span><span class="o">=</span><span class="s2">"*"</span>  <span class="nt">--enablerepo</span><span class="o">=</span><span class="s2">"elrepo-kernel"</span>  list  available <span class="nt">-y</span> <span class="p">;</span> yum  <span class="nt">--enablerepo</span><span class="o">=</span>elrepo-kernel  <span class="nb">install </span>kernel-lt <span class="nt">-y</span> <span class="p">;</span> grubby <span class="nt">--default-kernel</span> <span class="p">;</span> reboot 

<span class="c"># v7 整合命令为：</span>
yum <span class="nb">install </span>https://www.elrepo.org/elrepo-release-7.el7.elrepo.noarch.rpm <span class="nt">-y</span> <span class="p">;</span> <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@mirrorlist@#mirrorlist@g"</span> /etc/yum.repos.d/elrepo.repo <span class="p">;</span> <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s@elrepo.org/linux@mirrors.tuna.tsinghua.edu.cn/elrepo@g"</span> /etc/yum.repos.d/elrepo.repo <span class="p">;</span> yum  <span class="nt">--disablerepo</span><span class="o">=</span><span class="s2">"*"</span>  <span class="nt">--enablerepo</span><span class="o">=</span><span class="s2">"elrepo-kernel"</span>  list  available <span class="nt">-y</span> <span class="p">;</span> yum  <span class="nt">--enablerepo</span><span class="o">=</span>elrepo-kernel  <span class="nb">install  </span>kernel-lt <span class="nt">-y</span> <span class="p">;</span> grubby <span class="nt">--set-default</span> <span class="si">$(</span><span class="nb">ls</span> /boot/vmlinuz-<span class="k">*</span> | <span class="nb">grep </span>elrepo<span class="si">)</span> <span class="p">;</span> grubby <span class="nt">--default-kernel</span> <span class="p">;</span> reboot 

<span class="c"># 离线版本 </span>
yum <span class="nb">install</span> <span class="nt">-y</span> /root/cby/kernel-lt-<span class="k">*</span><span class="nt">-1</span>.el7.elrepo.x86_64.rpm <span class="p">;</span> grubby <span class="nt">--set-default</span> <span class="si">$(</span><span class="nb">ls</span> /boot/vmlinuz-<span class="k">*</span> | <span class="nb">grep </span>elrepo<span class="si">)</span> <span class="p">;</span> grubby <span class="nt">--default-kernel</span> <span class="p">;</span> reboot 
</code></pre></div></div>

<h3 id="116安装ipvsadm">1.16.安装ipvsadm</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 对于CentOS7离线安装</span>
<span class="c"># yum install /root/centos7/ipset-*.el7.x86_64.rpm /root/centos7/lm_sensors-libs-*.el7.x86_64.rpm  /root/centos7/ipset-libs-*.el7.x86_64.rpm /root/centos7/sysstat-*.el7_9.x86_64.rpm  /root/centos7/ipvsadm-*.el7.x86_64.rpm  -y</span>

<span class="c"># 对于 Ubuntu</span>
<span class="c"># apt install ipvsadm ipset sysstat conntrack -y</span>

<span class="c"># 对于 CentOS</span>
yum <span class="nb">install </span>ipvsadm ipset sysstat conntrack libseccomp <span class="nt">-y</span>
<span class="nb">cat</span> <span class="o">&gt;&gt;</span> /etc/modules-load.d/ipvs.conf <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> 
ip_vs
ip_vs_rr
ip_vs_wrr
ip_vs_sh
nf_conntrack
ip_tables
ip_set
xt_set
ipt_set
ipt_rpfilter
ipt_REJECT
ipip
</span><span class="no">EOF

</span>systemctl restart systemd-modules-load.service

lsmod | <span class="nb">grep</span> <span class="nt">-e</span> ip_vs <span class="nt">-e</span> nf_conntrack
ip_vs_sh               16384  0
ip_vs_wrr              16384  0
ip_vs_rr               16384  0
ip_vs                 237568  6 ip_vs_rr,ip_vs_sh,ip_vs_wrr
nf_conntrack          217088  3 nf_nat,nft_ct,ip_vs
nf_defrag_ipv6         24576  2 nf_conntrack,ip_vs
nf_defrag_ipv4         16384  1 nf_conntrack
libcrc32c              16384  5 nf_conntrack,nf_nat,nf_tables,xfs,ip_vs

<span class="c"># 参数解释</span>
<span class="c">#</span>
<span class="c"># ip_vs</span>
<span class="c"># IPVS 是 Linux 内核中的一个模块，用于实现负载均衡和高可用性。它通过在前端代理服务器上分发传入请求到后端实际服务器上，提供了高性能和可扩展的网络服务。</span>
<span class="c"># ip_vs_rr</span>
<span class="c"># IPVS 的一种调度算法之一，使用轮询方式分发请求到后端服务器，每个请求按顺序依次分发。</span>
<span class="c"># ip_vs_wrr</span>
<span class="c"># IPVS 的一种调度算法之一，使用加权轮询方式分发请求到后端服务器，每个请求按照指定的权重比例分发。</span>
<span class="c"># ip_vs_sh</span>
<span class="c"># IPVS 的一种调度算法之一，使用哈希方式根据源 IP 地址和目标 IP 地址来分发请求。</span>
<span class="c"># nf_conntrack</span>
<span class="c"># 这是一个内核模块，用于跟踪和管理网络连接，包括 TCP、UDP 和 ICMP 等协议。它是实现防火墙状态跟踪的基础。</span>
<span class="c"># ip_tables</span>
<span class="c"># 这是一个内核模块，提供了对 Linux 系统 IP 数据包过滤和网络地址转换（NAT）功能的支持。</span>
<span class="c"># ip_set</span>
<span class="c"># 这是一个内核模块，扩展了 iptables 的功能，支持更高效的 IP 地址集合操作。</span>
<span class="c"># xt_set</span>
<span class="c"># 这是一个内核模块，扩展了 iptables 的功能，支持更高效的数据包匹配和操作。</span>
<span class="c"># ipt_set</span>
<span class="c"># 这是一个用户空间工具，用于配置和管理 xt_set 内核模块。</span>
<span class="c"># ipt_rpfilter</span>
<span class="c"># 这是一个内核模块，用于实现反向路径过滤，用于防止 IP 欺骗和 DDoS 攻击。</span>
<span class="c"># ipt_REJECT</span>
<span class="c"># 这是一个 iptables 目标，用于拒绝 IP 数据包，并向发送方发送响应，指示数据包被拒绝。</span>
<span class="c"># ipip</span>
<span class="c"># 这是一个内核模块，用于实现 IP 封装在 IP（IP-over-IP）的隧道功能。它可以在不同网络之间创建虚拟隧道来传输 IP 数据包。</span>
</code></pre></div></div>

<h3 id="117修改内核参数">1.17.修改内核参数</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> &gt; /etc/sysctl.d/k8s.conf
net.ipv4.ip_forward = 1
net.bridge.bridge-nf-call-iptables = 1
fs.may_detach_mounts = 1
vm.overcommit_memory=1
vm.panic_on_oom=0
fs.inotify.max_user_watches=89100
fs.file-max=52706963
fs.nr_open=52706963
net.netfilter.nf_conntrack_max=2310720

net.ipv4.tcp_keepalive_time = 600
net.ipv4.tcp_keepalive_probes = 3
net.ipv4.tcp_keepalive_intvl =15
net.ipv4.tcp_max_tw_buckets = 36000
net.ipv4.tcp_tw_reuse = 1
net.ipv4.tcp_max_orphans = 327680
net.ipv4.tcp_orphan_retries = 3
net.ipv4.tcp_syncookies = 1
net.ipv4.tcp_max_syn_backlog = 16384
net.ipv4.ip_conntrack_max = 65536
net.ipv4.tcp_max_syn_backlog = 16384
net.ipv4.tcp_timestamps = 0
net.core.somaxconn = 16384

net.ipv6.conf.all.disable_ipv6 = 0
net.ipv6.conf.default.disable_ipv6 = 0
net.ipv6.conf.lo.disable_ipv6 = 0
net.ipv6.conf.all.forwarding = 1
</span><span class="no">EOF

</span>sysctl <span class="nt">--system</span>

<span class="c"># 这些是Linux系统的一些参数设置，用于配置和优化网络、文件系统和虚拟内存等方面的功能。以下是每个参数的详细解释：</span>
<span class="c"># </span>
<span class="c"># 1. net.ipv4.ip_forward = 1</span>
<span class="c">#    - 这个参数启用了IPv4的IP转发功能，允许服务器作为网络路由器转发数据包。</span>
<span class="c"># 2. net.bridge.bridge-nf-call-iptables = 1</span>
<span class="c">#    - 当使用网络桥接技术时，将数据包传递到iptables进行处理。</span>
<span class="c"># 3. fs.may_detach_mounts = 1</span>
<span class="c">#    - 允许在挂载文件系统时，允许被其他进程使用。</span>
<span class="c"># 4. vm.overcommit_memory=1</span>
<span class="c">#    - 该设置允许原始的内存过量分配策略，当系统的内存已经被完全使用时，系统仍然会分配额外的内存。</span>
<span class="c"># 5. vm.panic_on_oom=0</span>
<span class="c">#    - 当系统内存不足（OOM）时，禁用系统崩溃和重启。</span>
<span class="c"># 6. fs.inotify.max_user_watches=89100</span>
<span class="c">#    - 设置系统允许一个用户的inotify实例可以监控的文件数目的上限。</span>
<span class="c"># 7. fs.file-max=52706963</span>
<span class="c">#    - 设置系统同时打开的文件数的上限。</span>
<span class="c"># 8. fs.nr_open=52706963</span>
<span class="c">#    - 设置系统同时打开的文件描述符数的上限。</span>
<span class="c"># 9. net.netfilter.nf_conntrack_max=2310720</span>
<span class="c">#    - 设置系统可以创建的网络连接跟踪表项的最大数量。</span>
<span class="c"># 10. net.ipv4.tcp_keepalive_time = 600</span>
<span class="c">#     - 设置TCP套接字的空闲超时时间（秒），超过该时间没有活动数据时，内核会发送心跳包。</span>
<span class="c"># 11. net.ipv4.tcp_keepalive_probes = 3</span>
<span class="c">#     - 设置未收到响应的TCP心跳探测次数。</span>
<span class="c"># 12. net.ipv4.tcp_keepalive_intvl = 15</span>
<span class="c">#     - 设置TCP心跳探测的时间间隔（秒）。</span>
<span class="c"># 13. net.ipv4.tcp_max_tw_buckets = 36000</span>
<span class="c">#     - 设置系统可以使用的TIME_WAIT套接字的最大数量。</span>
<span class="c"># 14. net.ipv4.tcp_tw_reuse = 1</span>
<span class="c">#     - 启用TIME_WAIT套接字的重新利用，允许新的套接字使用旧的TIME_WAIT套接字。</span>
<span class="c"># 15. net.ipv4.tcp_max_orphans = 327680</span>
<span class="c">#     - 设置系统可以同时存在的TCP套接字垃圾回收包裹数的最大数量。</span>
<span class="c"># 16. net.ipv4.tcp_orphan_retries = 3</span>
<span class="c">#     - 设置系统对于孤立的TCP套接字的重试次数。</span>
<span class="c"># 17. net.ipv4.tcp_syncookies = 1</span>
<span class="c">#     - 启用TCP SYN cookies保护，用于防止SYN洪泛攻击。</span>
<span class="c"># 18. net.ipv4.tcp_max_syn_backlog = 16384</span>
<span class="c">#     - 设置新的TCP连接的半连接数（半连接队列）的最大长度。</span>
<span class="c"># 19. net.ipv4.ip_conntrack_max = 65536</span>
<span class="c">#     - 设置系统可以创建的网络连接跟踪表项的最大数量。</span>
<span class="c"># 20. net.ipv4.tcp_timestamps = 0</span>
<span class="c">#     - 关闭TCP时间戳功能，用于提供更好的安全性。</span>
<span class="c"># 21. net.core.somaxconn = 16384</span>
<span class="c">#     - 设置系统核心层的连接队列的最大值。</span>
<span class="c"># 22. net.ipv6.conf.all.disable_ipv6 = 0</span>
<span class="c">#     - 启用IPv6协议。</span>
<span class="c"># 23. net.ipv6.conf.default.disable_ipv6 = 0</span>
<span class="c">#     - 启用IPv6协议。</span>
<span class="c"># 24. net.ipv6.conf.lo.disable_ipv6 = 0</span>
<span class="c">#     - 启用IPv6协议。</span>
<span class="c"># 25. net.ipv6.conf.all.forwarding = 1</span>
<span class="c">#     - 允许IPv6数据包转发。</span>
</code></pre></div></div>

<h3 id="118所有节点配置hosts本地解析">1.18.所有节点配置hosts本地解析</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /etc/hosts <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6

192.168.1.31 k8s-master01
192.168.1.32 k8s-master02
192.168.1.33 k8s-master03
192.168.1.34 k8s-node01
192.168.1.35 k8s-node02
192.168.1.36 lb-vip

fc00::31 k8s-master01
fc00::32 k8s-master02
fc00::33 k8s-master03
fc00::34 k8s-node01
fc00::35 k8s-node02
</span><span class="no">EOF
</span></code></pre></div></div>

<h1 id="2k8s基本组件安装">2.k8s基本组件安装</h1>

<p><strong>注意 ： 2.1 和 2.2 二选其一即可</strong></p>

<h2 id="21安装containerd作为runtime">2.1.安装Containerd作为Runtime</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># https://github.com/containernetworking/plugins/releases/</span>
<span class="c"># wget https://github.com/containernetworking/plugins/releases/download/v1.7.1/cni-plugins-linux-amd64-v1.7.1.tgz</span>

<span class="nb">cd </span>cby/

<span class="c">#创建cni插件所需目录</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> /etc/cni/net.d /opt/cni/bin 
<span class="c">#解压cni二进制包</span>
<span class="nb">tar </span>xf cni-plugins-linux-amd64-v<span class="k">*</span>.tgz <span class="nt">-C</span> /opt/cni/bin/

<span class="c"># https://github.com/containerd/containerd/releases/</span>
<span class="c"># wget https://github.com/containerd/containerd/releases/download/v2.0.5/containerd-2.0.5-linux-amd64.tar.gz</span>

<span class="c">#解压</span>
<span class="nb">tar</span> <span class="nt">-xzf</span> containerd-<span class="k">*</span><span class="nt">-linux-amd64</span>.tar.gz <span class="nt">-C</span> /usr/local/

<span class="c">#创建服务启动文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/systemd/system/containerd.service <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=containerd container runtime
Documentation=https://containerd.io
After=network.target local-fs.target

[Service]
ExecStartPre=-/sbin/modprobe overlay
ExecStart=/usr/local/bin/containerd
Type=notify
Delegate=yes
KillMode=process
Restart=always
RestartSec=5
LimitNPROC=infinity
LimitCORE=infinity
LimitNOFILE=infinity
TasksMax=infinity
OOMScoreAdjust=-999

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF


</span><span class="c"># 参数解释：</span>
<span class="c">#</span>
<span class="c"># 这是一个用于启动containerd容器运行时的systemd unit文件。下面是对该文件不同部分的详细解释：</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># Description=containerd container runtime</span>
<span class="c"># 描述该unit的作用是作为containerd容器运行时。</span>
<span class="c"># </span>
<span class="c"># Documentation=https://containerd.io</span>
<span class="c"># 指向容器运行时的文档的URL。</span>
<span class="c"># </span>
<span class="c"># After=network.target local-fs.target</span>
<span class="c"># 定义了在哪些依赖项之后该unit应该被启动。在网络和本地文件系统加载完成后启动，确保了容器运行时在这些依赖项可用时才会启动。</span>
<span class="c"># </span>
<span class="c"># [Service]</span>
<span class="c"># ExecStartPre=-/sbin/modprobe overlay</span>
<span class="c"># 在启动containerd之前执行的命令。这里的命令是尝试加载内核的overlay模块，如果失败则忽略错误继续执行下面的命令。</span>
<span class="c"># </span>
<span class="c"># ExecStart=/usr/local/bin/containerd</span>
<span class="c"># 实际执行的命令，用于启动containerd容器运行时。</span>
<span class="c"># </span>
<span class="c"># Type=notify</span>
<span class="c"># 指定服务的通知类型。这里使用notify类型，表示当服务就绪时会通过通知的方式告知systemd。</span>
<span class="c"># </span>
<span class="c"># Delegate=yes</span>
<span class="c"># 允许systemd对此服务进行重启和停止操作。</span>
<span class="c"># </span>
<span class="c"># KillMode=process</span>
<span class="c"># 在终止容器运行时时使用的kill模式。这里使用process模式，表示通过终止进程来停止容器运行时。</span>
<span class="c"># </span>
<span class="c"># Restart=always</span>
<span class="c"># 定义了当容器运行时终止后的重启策略。这里设置为always，表示无论何时终止容器运行时，都会自动重新启动。</span>
<span class="c"># </span>
<span class="c"># RestartSec=5</span>
<span class="c"># 在容器运行时终止后重新启动之前等待的秒数。</span>
<span class="c"># </span>
<span class="c"># LimitNPROC=infinity</span>
<span class="c"># 指定容器运行时可以使用的最大进程数量。这里设置为无限制。</span>
<span class="c"># </span>
<span class="c"># LimitCORE=infinity</span>
<span class="c"># 指定容器运行时可以使用的最大CPU核心数量。这里设置为无限制。</span>
<span class="c"># </span>
<span class="c"># LimitNOFILE=infinity</span>
<span class="c"># 指定容器运行时可以打开的最大文件数。这里设置为无限制。</span>
<span class="c"># </span>
<span class="c"># TasksMax=infinity</span>
<span class="c"># 指定容器运行时可以创建的最大任务数。这里设置为无限制。</span>
<span class="c"># </span>
<span class="c"># OOMScoreAdjust=-999</span>
<span class="c"># 指定容器运行时的OOM（Out-Of-Memory）分数调整值。负数值表示容器运行时的优先级较高。</span>
<span class="c"># </span>
<span class="c"># [Install]</span>
<span class="c"># WantedBy=multi-user.target</span>
<span class="c"># 定义了服务的安装位置。这里指定为multi-user.target，表示将服务安装为多用户模式下的启动项。</span>
</code></pre></div></div>

<h3 id="211配置containerd所需的模块">2.1.1配置Containerd所需的模块</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/modules-load.d/containerd.conf
overlay
br_netfilter
</span><span class="no">EOF

</span><span class="c"># 参数解释：</span>
<span class="c">#</span>
<span class="c"># containerd是一个容器运行时，用于管理和运行容器。它支持多种不同的参数配置来自定义容器运行时的行为和功能。</span>
<span class="c"># </span>
<span class="c"># 1. overlay：overlay是容器d默认使用的存储驱动，它提供了一种轻量级的、可堆叠的、逐层增量的文件系统。它通过在现有文件系统上叠加文件系统层来创建容器的文件系统视图。每个容器可以有自己的一组文件系统层，这些层可以共享基础镜像中的文件，并在容器内部进行修改。使用overlay可以有效地使用磁盘空间，并使容器更加轻量级。</span>
<span class="c"># </span>
<span class="c"># 2. br_netfilter：br_netfilter是Linux内核提供的一个网络过滤器模块，用于在容器网络中进行网络过滤和NAT转发。当容器和主机之间的网络通信需要进行DNAT或者SNAT时，br_netfilter模块可以将IP地址进行转换。它还可以提供基于iptables规则的网络过滤功能，用于限制容器之间或容器与外部网络之间的通信。</span>
<span class="c"># </span>
<span class="c"># 这些参数可以在containerd的配置文件或者命令行中指定。例如，可以通过设置--storage-driver参数来选择使用overlay作为存储驱动，通过设置--iptables参数来启用或禁用br_netfilter模块。具体的使用方法和配置细节可以参考containerd的官方文档。</span>
</code></pre></div></div>

<h3 id="212加载模块">2.1.2加载模块</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl restart systemd-modules-load.service

<span class="c"># 参数解释：</span>
<span class="c"># - `systemctl`: 是Linux系统管理服务的命令行工具，可以管理systemd init系统。</span>
<span class="c"># - `restart`: 是systemctl命令的一个选项，用于重新启动服务。</span>
<span class="c"># - `systemd-modules-load.service`: 是一个系统服务，用于加载内核模块。</span>
<span class="c"># </span>
<span class="c"># 将上述参数结合在一起来解释`systemctl restart systemd-modules-load.service`的含义：</span>
<span class="c"># 这个命令用于重新启动系统服务`systemd-modules-load.service`，它是负责加载内核模块的服务。在重新启动该服务后，系统会重新加载所有的内核模块。</span>
</code></pre></div></div>

<h3 id="213配置containerd所需的内核">2.1.3配置Containerd所需的内核</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | sudo tee /etc/sysctl.d/99-kubernetes-cri.conf
net.bridge.bridge-nf-call-iptables  = 1
net.ipv4.ip_forward                 = 1
net.bridge.bridge-nf-call-ip6tables = 1
</span><span class="no">EOF

</span><span class="c"># 加载内核</span>
sysctl <span class="nt">--system</span>

<span class="c"># 参数解释：</span>
<span class="c"># </span>
<span class="c"># 这些参数是Linux操作系统中用于网络和网络桥接设置的参数。</span>
<span class="c"># </span>
<span class="c"># - net.bridge.bridge-nf-call-iptables：这个参数控制网络桥接设备是否调用iptables规则处理网络数据包。当该参数设置为1时，网络数据包将被传递到iptables进行处理；当该参数设置为0时，网络数据包将绕过iptables直接传递。默认情况下，这个参数的值是1，即启用iptables规则处理网络数据包。</span>
<span class="c"># </span>
<span class="c"># - net.ipv4.ip_forward：这个参数用于控制是否启用IP转发功能。IP转发使得操作系统可以将接收到的数据包从一个网络接口转发到另一个网络接口。当该参数设置为1时，启用IP转发功能；当该参数设置为0时，禁用IP转发功能。在网络环境中，通常需要启用IP转发功能来实现不同网络之间的通信。默认情况下，这个参数的值是0，即禁用IP转发功能。</span>
<span class="c"># </span>
<span class="c"># - net.bridge.bridge-nf-call-ip6tables：这个参数与net.bridge.bridge-nf-call-iptables类似，但是它用于IPv6数据包的处理。当该参数设置为1时，IPv6数据包将被传递到ip6tables进行处理；当该参数设置为0时，IPv6数据包将绕过ip6tables直接传递。默认情况下，这个参数的值是1，即启用ip6tables规则处理IPv6数据包。</span>
<span class="c"># </span>
<span class="c"># 这些参数的值可以通过修改操作系统的配置文件（通常是'/etc/sysctl.conf'）来进行设置。修改完成后，需要使用'sysctl -p'命令重载配置文件使参数生效。</span>
</code></pre></div></div>

<h3 id="214创建containerd的配置文件">2.1.4创建Containerd的配置文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 参数解释：</span>
<span class="c"># </span>
<span class="c"># 这段代码是用于修改并配置containerd的参数。</span>
<span class="c"># </span>
<span class="c"># 1. 首先使用命令`mkdir -p /etc/containerd`创建/etc/containerd目录，如果该目录已存在，则不进行任何操作。</span>
<span class="c"># 2. 使用命令`containerd config default | tee /etc/containerd/config.toml`创建默认配置文件，并将输出同时传递给/etc/containerd/config.toml文件。</span>
<span class="c"># 3. 使用sed命令修改/etc/containerd/config.toml文件，将SystemdCgroup参数的值从false改为true。-i参数表示直接在原文件中进行编辑。</span>
<span class="c"># 4. 使用cat命令结合grep命令查看/etc/containerd/config.toml文件中SystemdCgroup参数的值是否已修改为true。</span>
<span class="c"># 5. 使用sed命令修改/etc/containerd/config.toml文件，将registry.k8s.io的地址替换为m.daocloud.io/registry.k8s.io。-i参数表示直接在原文件中进行编辑。</span>
<span class="c"># 6. 使用cat命令结合grep命令查看/etc/containerd/config.toml文件中sandbox_image参数的值是否已修改为m.daocloud.io/registry.k8s.io。</span>
<span class="c"># 7. 使用sed命令修改/etc/containerd/config.toml文件，将config_path参数的值从""改为"/etc/containerd/certs.d"。-i参数表示直接在原文件中进行编辑。</span>
<span class="c"># 8. 使用cat命令结合grep命令查看/etc/containerd/config.toml文件中certs.d参数的值是否已修改为/etc/containerd/certs.d。</span>
<span class="c"># 9. 使用mkdir命令创建/etc/containerd/certs.d/docker.io目录，如果目录已存在，则不进行任何操作。-p参数表示创建目录时，如果父级目录不存在，则自动创建父级目录。</span>
<span class="c"># </span>
<span class="c"># 最后，使用cat重定向操作符将内容写入/etc/containerd/certs.d/docker.io/hosts.toml文件。该文件会配置加速器，其中server参数设置为"https://docker.io"，host参数设置为"https://hub-mirror.c.163.com"，并添加capabilities参数。</span>

<span class="c"># 创建默认配置文件</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> /etc/containerd
containerd config default | <span class="nb">tee</span> /etc/containerd/config.toml

<span class="c"># 修改Containerd的配置文件</span>

<span class="c"># sed -i "s#SystemdCgroup\ \=\ false#SystemdCgroup\ \=\ true#g" /etc/containerd/config.toml</span>
<span class="c"># cat /etc/containerd/config.toml | grep SystemdCgroup</span>

<span class="c"># 沙箱pause镜像</span>
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#registry.k8s.io#registry.aliyuncs.com/chenby#g"</span> /etc/containerd/config.toml
<span class="nb">cat</span> /etc/containerd/config.toml | <span class="nb">grep </span>sandbox

<span class="c"># 注意！</span>
<span class="c"># SystemdCgroup参数是containerd中的一个配置参数，用于设置containerd在运行过程中使用的Cgroup（控制组）路径。Containerd使用SystemdCgroup参数来指定应该使用哪个Cgroup来跟踪和管理容器的资源使用。</span>
<span class="c"># </span>
<span class="c"># Cgroup是Linux内核提供的一种资源隔离和管理机制，可以用于限制、分配和监控进程组的资源使用。使用Cgroup，可以将容器的资源限制和隔离，以防止容器之间的资源争用和不公平的竞争。</span>
<span class="c"># </span>
<span class="c"># 通过设置SystemdCgroup参数，可以确保containerd能够找到正确的Cgroup路径，并正确地限制和隔离容器的资源使用，确保容器可以按照预期的方式运行。如果未正确设置SystemdCgroup参数，可能会导致容器无法正确地使用资源，或者无法保证资源的公平分配和隔离。</span>
<span class="c"># </span>
<span class="c"># 总而言之，SystemdCgroup参数的作用是为了确保containerd能够正确地管理容器的资源使用，以实现资源的限制、隔离和公平分配。</span>


<span class="c"># 配置加速器</span>
<span class="o">[</span>root@k8s-master01 ~]# vim /etc/containerd/config.toml
<span class="o">[</span>root@k8s-master01 ~]# <span class="nb">cat</span> /etc/containerd/config.toml | <span class="nb">grep </span>certs.d <span class="nt">-C</span> 5

    <span class="o">[</span>plugins.<span class="s1">'io.containerd.cri.v1.images'</span>.pinned_images]
      sandbox <span class="o">=</span> <span class="s1">'registry.aliyuncs.com/chenby/pause:3.10'</span>

    <span class="o">[</span>plugins.<span class="s1">'io.containerd.cri.v1.images'</span>.registry]
      config_path <span class="o">=</span> <span class="s1">'/etc/containerd/certs.d'</span>

    <span class="o">[</span>plugins.<span class="s1">'io.containerd.cri.v1.images'</span>.image_decryption]
      key_model <span class="o">=</span> <span class="s1">'node'</span>

  <span class="o">[</span>plugins.<span class="s1">'io.containerd.cri.v1.runtime'</span><span class="o">]</span>
<span class="o">[</span>root@k8s-master01 ~]# 


<span class="nb">mkdir</span> /etc/containerd/certs.d/docker.io <span class="nt">-pv</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/containerd/certs.d/docker.io/hosts.toml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
server = "https://docker.io"
[host."https://jockerhub.com"]
  capabilities = ["pull", "resolve"]
</span><span class="no">EOF

</span><span class="c"># 配置 GFW 代理</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> /etc/systemd/system/containerd.service.d
<span class="nb">touch</span> /etc/systemd/system/containerd.service.d/http-proxy.conf
<span class="nb">tee</span> /etc/systemd/system/containerd.service.d/http-proxy.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Service]
Environment="HTTP_PROXY=http://IP:Port"
Environment="HTTPS_PROXY=http://IP:Port"
Environment="NO_PROXY=localhost,127.0.0.1,containerd"
</span><span class="no">EOF
</span></code></pre></div></div>

<h3 id="215启动并设置为开机启动">2.1.5启动并设置为开机启动</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> containerd.service
<span class="c"># 启用并立即启动docker.service单元。docker.service是Docker守护进程的systemd服务单元。</span>

systemctl stop containerd.service
<span class="c"># 停止运行中的docker.service单元，即停止Docker守护进程。</span>

systemctl start containerd.service
<span class="c"># 启动docker.service单元，即启动Docker守护进程。</span>

systemctl restart containerd.service
<span class="c"># 重启docker.service单元，即重新启动Docker守护进程。</span>

systemctl status containerd.service
<span class="c"># 显示docker.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h3 id="216配置crictl客户端连接的运行时位置">2.1.6配置crictl客户端连接的运行时位置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># https://github.com/kubernetes-sigs/cri-tools/releases/</span>
<span class="c"># wget https://github.com/kubernetes-sigs/cri-tools/releases/download/v1.35.0/crictl-v1.35.0-linux-amd64.tar.gz</span>

<span class="c">#解压</span>
<span class="nb">tar </span>xf crictl-v<span class="k">*</span><span class="nt">-linux-amd64</span>.tar.gz <span class="nt">-C</span> /usr/bin/
<span class="c">#生成配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/crictl.yaml <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
runtime-endpoint: unix:///run/containerd/containerd.sock
image-endpoint: unix:///run/containerd/containerd.sock
timeout: 10
debug: false
</span><span class="no">EOF

</span><span class="c">#测试</span>
systemctl restart  containerd
crictl info

<span class="c"># 注意！</span>
<span class="c"># 下面是参数`crictl`的详细解释</span>
<span class="c"># </span>
<span class="c"># `crictl`是一个用于与容器运行时通信的命令行工具。它是容器运行时接口（CRI）工具的一个实现，可以对容器运行时进行管理和操作。</span>
<span class="c"># </span>
<span class="c"># 1. `runtime-endpoint: unix:///run/containerd/containerd.sock`</span>
<span class="c"># 指定容器运行时的终端套接字地址。在这个例子中，指定的地址是`unix:///run/containerd/containerd.sock`，这是一个Unix域套接字地址。</span>
<span class="c"># </span>
<span class="c"># 2. `image-endpoint: unix:///run/containerd/containerd.sock`</span>
<span class="c"># 指定容器镜像服务的终端套接字地址。在这个例子中，指定的地址是`unix:///run/containerd/containerd.sock`，这是一个Unix域套接字地址。</span>
<span class="c"># </span>
<span class="c"># 3. `timeout: 10`</span>
<span class="c"># 设置与容器运行时通信的超时时间，单位是秒。在这个例子中，超时时间被设置为10秒。</span>
<span class="c"># </span>
<span class="c"># 4. `debug: false`</span>
<span class="c"># 指定是否开启调式模式。在这个例子中，调式模式被设置为关闭，即`false`。如果设置为`true`，则会输出更详细的调试信息。</span>
<span class="c"># </span>
<span class="c"># 这些参数可以根据需要进行修改，以便与容器运行时进行有效的通信和管理。</span>
</code></pre></div></div>

<h2 id="22-安装docker作为runtime">2.2 安装docker作为Runtime</h2>

<h3 id="221-解压docker程序">2.2.1 解压docker程序</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 二进制包下载地址：https://download.docker.com/linux/static/stable/x86_64/</span>
<span class="c"># wget https://mirrors.ustc.edu.cn/docker-ce/linux/static/stable/x86_64/docker-27.4.0.tgz</span>

<span class="c">#解压</span>
<span class="nb">tar </span>xf docker-<span class="k">*</span>.tgz 
<span class="c">#拷贝二进制文件</span>
<span class="nb">cp </span>docker/<span class="k">*</span> /usr/bin/
</code></pre></div></div>

<h3 id="222-创建containerd的service文件">2.2.2 创建containerd的service文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#创建containerd的service文件,并且启动</span>
<span class="nb">cat</span> <span class="o">&gt;</span>/etc/systemd/system/containerd.service <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=containerd container runtime
Documentation=https://containerd.io
After=network.target local-fs.target

[Service]
ExecStartPre=-/sbin/modprobe overlay
ExecStart=/usr/bin/containerd
Type=notify
Delegate=yes
KillMode=process
Restart=always
RestartSec=5
LimitNPROC=infinity
LimitCORE=infinity
LimitNOFILE=1048576
TasksMax=infinity
OOMScoreAdjust=-999

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF

</span><span class="c"># 参数解释：</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># - Description=containerd container runtime：指定服务的描述信息。</span>
<span class="c"># - Documentation=https://containerd.io：指定服务的文档链接。</span>
<span class="c"># - After=network.target local-fs.target：指定服务的启动顺序，在网络和本地文件系统启动之后再启动该服务。</span>
<span class="c"># </span>
<span class="c"># [Service]</span>
<span class="c"># - ExecStartPre=-/sbin/modprobe overlay：在启动服务之前执行的命令，使用`-`表示忽略错误。</span>
<span class="c"># - ExecStart=/usr/bin/containerd：指定服务的启动命令。</span>
<span class="c"># - Type=notify：指定服务的类型，`notify`表示服务会在启动完成后向systemd发送通知。</span>
<span class="c"># - Delegate=yes：允许服务代理其他服务的应答，例如收到关机命令后终止其他服务。</span>
<span class="c"># - KillMode=process：指定服务终止时的行为，`process`表示终止服务进程。</span>
<span class="c"># - Restart=always：指定服务终止后是否自动重启，`always`表示总是自动重启。</span>
<span class="c"># - RestartSec=5：指定服务重启的时间间隔，单位为秒。</span>
<span class="c"># - LimitNPROC=infinity：限制服务的最大进程数，`infinity`表示没有限制。</span>
<span class="c"># - LimitCORE=infinity：限制服务的最大核心数，`infinity`表示没有限制。</span>
<span class="c"># - LimitNOFILE=1048576：限制服务的最大文件数，指定为1048576。</span>
<span class="c"># - TasksMax=infinity：限制服务的最大任务数，`infinity`表示没有限制。</span>
<span class="c"># - OOMScoreAdjust=-999：指定服务的OOM（Out of Memory）得分，负数表示降低被终止的概率。</span>
<span class="c"># </span>
<span class="c"># [Install]</span>
<span class="c"># - WantedBy=multi-user.target：指定服务的安装方式，`multi-user.target`表示该服务在多用户模式下安装。</span>


<span class="c"># 设置开机自启</span>
systemctl <span class="nb">enable</span> <span class="nt">--now</span> containerd.service
</code></pre></div></div>

<h3 id="223-准备docker的service文件">2.2.3 准备docker的service文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#准备docker的service文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/systemd/system/docker.service <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=Docker Application Container Engine
Documentation=https://docs.docker.com
After=network-online.target firewalld.service containerd.service
Wants=network-online.target
Requires=containerd.service

[Service]
Type=notify
ExecStart=/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock
ExecReload=/bin/kill -s HUP </span><span class="nv">$MAINPID</span><span class="sh">
TimeoutSec=0
RestartSec=2
Restart=always
StartLimitBurst=3
StartLimitInterval=60s
LimitNOFILE=infinity
LimitNPROC=infinity
LimitCORE=infinity
TasksMax=infinity
Delegate=yes
KillMode=process
OOMScoreAdjust=-500

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF

</span><span class="c"># 参数解释：</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># - Description: 描述服务的作用，这里是Docker Application Container Engine，即Docker应用容器引擎。</span>
<span class="c"># - Documentation: 提供关于此服务的文档链接，这里是Docker官方文档链接。</span>
<span class="c"># - After: 说明该服务在哪些其他服务之后启动，这里是在网络在线、firewalld服务和containerd服务后启动。</span>
<span class="c"># - Wants: 说明该服务想要的其他服务，这里是网络在线服务。</span>
<span class="c"># - Requires: 说明该服务需要的其他服务，这里containerd.service。</span>
<span class="c"># </span>
<span class="c"># [Service]</span>
<span class="c"># - Type: 服务类型，这里是notify，表示服务在启动完成时发送通知。</span>
<span class="c"># - ExecStart: 命令，启动该服务时会执行的命令，这里是/usr/bin/dockerd -H fd:// --containerd=/run/containerd/containerd.sock，即启动dockerd并指定一些参数，其中-H指定dockerd的监听地址为fd://，--containerd指定containerd的sock文件位置。</span>
<span class="c"># - ExecReload: 重载命令，当接收到HUP信号时执行的命令，这里是/bin/kill -s HUP $MAINPID，即发送HUP信号给主进程ID。</span>
<span class="c"># - TimeoutSec: 服务超时时间，这里是0，表示没有超时限制。</span>
<span class="c"># - RestartSec: 重启间隔时间，这里是2秒，表示重启失败后等待2秒再重启。</span>
<span class="c"># - Restart: 重启策略，这里是always，表示总是重启。</span>
<span class="c"># - StartLimitBurst: 启动限制次数，这里是3，表示在启动失败后最多重试3次。</span>
<span class="c"># - StartLimitInterval: 启动限制时间间隔，这里是60秒，表示两次启动之间最少间隔60秒。</span>
<span class="c"># - LimitNOFILE: 文件描述符限制，这里是infinity，表示没有限制。</span>
<span class="c"># - LimitNPROC: 进程数限制，这里是infinity，表示没有限制。</span>
<span class="c"># - LimitCORE: 核心转储限制，这里是infinity，表示没有限制。</span>
<span class="c"># - TasksMax: 最大任务数，这里是infinity，表示没有限制。</span>
<span class="c"># - Delegate: 修改权限，这里是yes，表示启用权限修改。</span>
<span class="c"># - KillMode: 杀死模式，这里是process，表示杀死整个进程组。</span>
<span class="c"># - OOMScoreAdjust: 用于调整进程在系统内存紧张时的优先级调整，这里是-500，表示将OOM分数降低500。</span>
<span class="c"># </span>
<span class="c"># [Install]</span>
<span class="c"># - WantedBy: 安装目标，这里是multi-user.target，表示在多用户模式下安装。</span>
<span class="c">#      在WantedBy参数中，我们可以使用以下参数：</span>
<span class="c">#      1. multi-user.target：指定该服务应该在多用户模式下启动。</span>
<span class="c">#      2. graphical.target：指定该服务应该在图形化界面模式下启动。</span>
<span class="c">#      3. default.target：指定该服务应该在系统的默认目标（runlevel）下启动。</span>
<span class="c">#      4. rescue.target：指定该服务应该在系统救援模式下启动。</span>
<span class="c">#      5. poweroff.target：指定该服务应该在关机时启动。</span>
<span class="c">#      6. reboot.target：指定该服务应该在重启时启动。</span>
<span class="c">#      7. halt.target：指定该服务应该在停止时启动。</span>
<span class="c">#      8. shutdown.target：指定该服务应该在系统关闭时启动。</span>
<span class="c">#      这些参数可以根据需要选择一个或多个，以告知系统在何时启动该服务。</span>
</code></pre></div></div>

<h3 id="224-准备docker的socket文件">2.2.4 准备docker的socket文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#准备docker的socket文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/systemd/system/docker.socket <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=Docker Socket for the API

[Socket]
ListenStream=/var/run/docker.sock
SocketMode=0660
SocketUser=root
SocketGroup=docker

[Install]
WantedBy=sockets.target
</span><span class="no">EOF

</span><span class="c"># 这是一个用于Docker API的socket配置文件，包含了以下参数：</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># - Description：描述了该socket的作用，即为Docker API的socket。</span>
<span class="c"># </span>
<span class="c"># [Socket]</span>
<span class="c"># - ListenStream：指定了socket的监听地址，该socket会监听在/var/run/docker.sock上，即Docker守护程序使用的默认sock文件。</span>
<span class="c"># - SocketMode：指定了socket文件的权限模式，此处为0660，即用户和用户组有读写权限，其他用户无权限。</span>
<span class="c"># - SocketUser：指定了socket文件的所有者，此处为root用户。</span>
<span class="c"># - SocketGroup：指定了socket文件的所属用户组，此处为docker用户组。</span>
<span class="c"># </span>
<span class="c"># [Install]</span>
<span class="c"># - WantedBy：指定了该socket被启用时的目标，此处为sockets.target，表示当sockets.target启动时启用该socket。</span>
<span class="c"># </span>
<span class="c"># 该配置文件的作用是为Docker提供API访问的通道，它监听在/var/run/docker.sock上，具有root用户权限，但只接受docker用户组的成员的连接，并且其他用户无法访问。这样，只有docker用户组的成员可以通过该socket与Docker守护进程进行通信。</span>
</code></pre></div></div>

<h3 id="225-配置加速器">2.2.5 配置加速器</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 配置加速器</span>
<span class="nb">mkdir</span> /etc/docker/ <span class="nt">-pv</span>
<span class="nb">cat</span> <span class="o">&gt;</span>/etc/docker/daemon.json <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
{
  "exec-opts": ["native.cgroupdriver=systemd"],
  "registry-mirrors": [
    "https://jockerhub.com"
  ],
  "max-concurrent-downloads": 10,
  "log-driver": "json-file",
  "log-level": "warn",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
    },
  "data-root": "/var/lib/docker",
  "proxies": {
    "http-proxy": "http://192.168.1.100:7890",
    "https-proxy": "http://192.168.1.100:7890",
    "no-proxy": "localhost"
  }
}
</span><span class="no">EOF


</span><span class="c"># 该参数文件中包含以下参数：</span>
<span class="c"># </span>
<span class="c"># 1. exec-opts: 用于设置Docker守护进程的选项，native.cgroupdriver=systemd表示使用systemd作为Cgroup驱动程序。</span>
<span class="c"># 2. registry-mirrors: 用于指定Docker镜像的镜像注册服务器。在这里有三个镜像注册服务器：https://docker.m.daocloud.io、https://docker.mirrors.ustc.edu.cn和http://hub-mirror.c.163.com。</span>
<span class="c"># 3. max-concurrent-downloads: 用于设置同时下载镜像的最大数量，默认值为3，这里设置为10。</span>
<span class="c"># 4. log-driver: 用于设置Docker守护进程的日志驱动程序，这里设置为json-file。</span>
<span class="c"># 5. log-level: 用于设置日志的级别，这里设置为warn。</span>
<span class="c"># 6. log-opts: 用于设置日志驱动程序的选项，这里有两个选项：max-size和max-file。max-size表示每个日志文件的最大大小，这里设置为10m，max-file表示保存的最大日志文件数量，这里设置为3。</span>
<span class="c"># 7. data-root: 用于设置Docker守护进程的数据存储根目录，默认为/var/lib/docker，这里设置为/var/lib/docker。</span>
</code></pre></div></div>

<h3 id="226-启动docker">2.2.6 启动docker</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>groupadd docker
<span class="c">#创建docker组</span>

systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> docker.service
<span class="c"># 启用并立即启动docker.service单元。docker.service是Docker守护进程的systemd服务单元。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> docker.socket
<span class="c"># 启用并立即启动docker.socket单元。docker.socket是一个systemd的socket单元，用于接收来自网络的Docker API请求。</span>

systemctl stop docker.service
<span class="c"># 停止运行中的docker.service单元，即停止Docker守护进程。</span>

systemctl start docker.service
<span class="c"># 启动docker.service单元，即启动Docker守护进程。</span>

systemctl restart docker.service
<span class="c"># 重启docker.service单元，即重新启动Docker守护进程。</span>

systemctl status docker.service
<span class="c"># 显示docker.service单元的当前状态，包括运行状态、是否启用等信息。</span>

docker info
<span class="c">#验证</span>
</code></pre></div></div>

<h3 id="227-解压cri-docker">2.2.7 解压cri-docker</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 由于1.24以及更高版本不支持docker所以安装cri-docker</span>
<span class="c"># 下载cri-docker </span>
<span class="c"># https://github.com/Mirantis/cri-dockerd/releases/</span>
<span class="c"># wget  https://github.com/Mirantis/cri-dockerd/releases/download/v0.3.16/cri-dockerd-0.3.16.amd64.tgz</span>

<span class="c"># 解压cri-docker</span>
<span class="nb">tar </span>xvf cri-dockerd-<span class="k">*</span>.amd64.tgz 
<span class="nb">cp</span> <span class="nt">-r</span> cri-dockerd/  /usr/bin/
<span class="nb">chmod</span> +x /usr/bin/cri-dockerd/cri-dockerd
</code></pre></div></div>

<h3 id="228-写入启动cri-docker配置文件">2.2.8 写入启动cri-docker配置文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 写入启动配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span>  /usr/lib/systemd/system/cri-docker.service <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=CRI Interface for Docker Application Container Engine
Documentation=https://docs.mirantis.com
After=network-online.target firewalld.service docker.service
Wants=network-online.target
Requires=docker.service

[Service]
Type=notify
ExecStart=/usr/bin/cri-dockerd/cri-dockerd --network-plugin=cni --pod-infra-container-image=registry.aliyuncs.com/google_containers/pause:3.7
ExecReload=/bin/kill -s HUP </span><span class="nv">$MAINPID</span><span class="sh">
TimeoutSec=0
RestartSec=5
Restart=always
StartLimitBurst=5
StartLimitInterval=60s
LimitNOFILE=infinity
LimitNPROC=infinity
LimitCORE=infinity
TasksMax=infinity
Delegate=yes
KillMode=process

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF


</span><span class="c"># [Unit]</span>
<span class="c"># - Description：该参数用于描述该单元的功能，这里描述的是CRI与Docker应用容器引擎的接口。</span>
<span class="c"># - Documentation：该参数指定了相关文档的网址，供用户参考。</span>
<span class="c"># - After：该参数指定了此单元应该在哪些其他单元之后启动，确保在网络在线、防火墙和Docker服务启动之后再启动此单元。</span>
<span class="c"># - Wants：该参数指定了此单元希望也启动的所有单元，此处是希望在网络在线之后启动。</span>
<span class="c"># - Requires：该参数指定了此单元需要依赖的单元，此处是docker.service单元。</span>
<span class="c"># </span>
<span class="c"># [Service]</span>
<span class="c"># - Type：该参数指定了服务的类型，这里是notify，表示当服务启动完成时向系统发送通知。</span>
<span class="c"># - ExecStart：该参数指定了将要运行的命令和参数，此处是执行/usr/bin/cri-dockerd/cri-dockerd命令，并指定了网络插件为cni和Pod基础设施容器的镜像为registry.aliyuncs.com/google_containers/pause:3.7。</span>
<span class="c"># - ExecReload：该参数指定在服务重载时运行的命令，此处是发送HUP信号给主进程。</span>
<span class="c"># - TimeoutSec：该参数指定了服务启动的超时时间，此处为0，表示无限制。</span>
<span class="c"># - RestartSec：该参数指定了自动重启服务的时间间隔，此处为2秒。</span>
<span class="c"># - Restart：该参数指定了在服务发生错误时自动重启，此处是始终重启。</span>
<span class="c"># - StartLimitBurst：该参数指定了在给定时间间隔内允许的启动失败次数，此处为3次。</span>
<span class="c"># - StartLimitInterval：该参数指定启动失败的时间间隔，此处为60秒。</span>
<span class="c"># - LimitNOFILE：该参数指定了允许打开文件的最大数量，此处为无限制。</span>
<span class="c"># - LimitNPROC：该参数指定了允许同时运行的最大进程数，此处为无限制。</span>
<span class="c"># - LimitCORE：该参数指定了允许生成的core文件的最大大小，此处为无限制。</span>
<span class="c"># - TasksMax：该参数指定了此服务的最大任务数，此处为无限制。</span>
<span class="c"># - Delegate：该参数指定了是否将控制权委托给指定服务，此处为是。</span>
<span class="c"># - KillMode：该参数指定了在终止服务时如何处理进程，此处是通过终止进程来终止服务。</span>
<span class="c"># </span>
<span class="c"># [Install]</span>
<span class="c"># - WantedBy：该参数指定了希望这个单元启动的多用户目标。在这里，这个单元希望在multi-user.target启动。</span>
</code></pre></div></div>

<h3 id="229-写入cri-docker的socket配置文件">2.2.9 写入cri-docker的socket配置文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 写入socket配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/cri-docker.socket <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=CRI Docker Socket for the API
PartOf=cri-docker.service

[Socket]
ListenStream=%t/cri-dockerd.sock
SocketMode=0660
SocketUser=root
SocketGroup=docker

[Install]
WantedBy=sockets.target
</span><span class="no">EOF


</span><span class="c"># 该配置文件是用于systemd的单元配置文件(unit file)，用于定义一个socket单元。</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># - Description：表示该单元的描述信息。</span>
<span class="c"># - PartOf：表示该单元是cri-docker.service的一部分。</span>
<span class="c"># </span>
<span class="c"># [Socket]</span>
<span class="c"># - ListenStream：指定了该socket要监听的地址和端口，这里使用了%t占位符，表示根据单元的类型来决定路径。%t/cri-dockerd.sock表示将监听Unix域套接字cri-dockerd.sock。Unix域套接字用于在同一台主机上的进程之间通信。</span>
<span class="c"># - SocketMode：指定了socket文件的权限模式，此处为0660，即用户和用户组有读写权限，其他用户无权限。</span>
<span class="c"># - SocketUser：指定了socket文件的所有者，此处为root用户。</span>
<span class="c"># - SocketGroup：指定了socket文件的所属用户组，此处为docker用户组。</span>
<span class="c"># </span>
<span class="c"># [Install]</span>
<span class="c"># - WantedBy：部分定义了该单元的安装配置信息。WantedBy=sockets.target表示当sockets.target单元启动时，自动启动该socket单元。sockets.target是一个系统服务，用于管理所有的socket单元。</span>
</code></pre></div></div>

<h3 id="2210-启动cri-docker">2.2.10 启动cri-docker</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> cri-docker.service
<span class="c"># 启用并立即启动cri-docker.service单元。cri-docker.service是cri-docker守护进程的systemd服务单元。</span>

systemctl restart cri-docker.service
<span class="c"># 重启cri-docker.service单元，即重新启动cri-docker守护进程。</span>

systemctl status cri-docker.service
<span class="c"># 显示docker.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h2 id="23k8s与etcd下载及安装仅在master01操作">2.3.k8s与etcd下载及安装（仅在master01操作）</h2>

<h3 id="231解压k8s安装包">2.3.1解压k8s安装包</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载安装包</span>
<span class="c"># https://github.com/etcd-io/etcd/releases/</span>
<span class="c"># https://github.com/kubernetes/kubernetes/tree/master/CHANGELOG</span>
<span class="c"># </span>
<span class="c"># wget https://github.com/etcd-io/etcd/releases/download/v3.5.21/etcd-v3.5.21-linux-amd64.tar.gz</span>
<span class="c"># wget https://cdn.dl.k8s.io/release/v1.35.0/kubernetes-server-linux-amd64.tar.gz</span>

<span class="c"># 解压k8s安装文件</span>
<span class="nb">cd </span>cby
<span class="nb">tar</span> <span class="nt">-xf</span> kubernetes-server-linux-amd64.tar.gz  <span class="nt">--strip-components</span><span class="o">=</span>3 <span class="nt">-C</span> /usr/local/bin kubernetes/server/bin/kube<span class="o">{</span><span class="nb">let</span>,ctl,-apiserver,-controller-manager,-scheduler,-proxy<span class="o">}</span>

<span class="c"># 这是一个tar命令，用于解压指定的kubernetes-server-linux-amd64.tar.gz文件，并将其中的特定文件提取到/usr/local/bin目录下。</span>
<span class="c"># </span>
<span class="c"># 命令的解释如下：</span>
<span class="c"># - tar：用于处理tar压缩文件的命令。</span>
<span class="c"># - -xf：表示解压操作。</span>
<span class="c"># - kubernetes-server-linux-amd64.tar.gz：要解压的文件名。</span>
<span class="c"># - --strip-components=3：表示解压时忽略压缩文件中的前3级目录结构，提取文件时直接放到目标目录中。</span>
<span class="c"># - -C /usr/local/bin：指定提取文件的目标目录为/usr/local/bin。</span>
<span class="c"># - kubernetes/server/bin/kube{let,ctl,-apiserver,-controller-manager,-scheduler,-proxy}：要解压和提取的文件名模式，用花括号括起来表示模式中的多个可能的文件名。</span>
<span class="c"># </span>
<span class="c"># 总的来说，这个命令的作用是将kubernetes-server-linux-amd64.tar.gz文件中的kubelet、kubectl、kube-apiserver、kube-controller-manager、kube-scheduler和kube-proxy六个文件提取到/usr/local/bin目录下，同时忽略文件路径中的前三级目录结构。</span>


<span class="c"># 解压etcd安装文件</span>
<span class="nb">tar</span> <span class="nt">-xf</span> etcd<span class="k">*</span>.tar.gz <span class="o">&amp;&amp;</span> <span class="nb">mv </span>etcd-<span class="k">*</span>/etcd /usr/local/bin/ <span class="o">&amp;&amp;</span> <span class="nb">mv </span>etcd-<span class="k">*</span>/etcdctl /usr/local/bin/

<span class="c"># 这是一个将文件解压并移动到特定目录的命令。这是一个用于 Linux 系统中的命令。</span>
<span class="c"># </span>
<span class="c"># - tar -xf etcd*.tar.gz：这个命令将解压以 etcd 开头并以.tar.gz 结尾的文件。`-xf` 是使用 `tar` 命令的选项，它表示解压文件并展开其中的内容。</span>
<span class="c"># - mv etcd-*/etcd /usr/local/bin/：这个命令将 etcd 文件移动到 /usr/local/bin 目录。`mv` 是移动命令，它将 etcd-*/etcd 路径下的 etcd 文件移动到了 /usr/local/bin 目录。</span>
<span class="c"># - mv etcd-*/etcdctl /usr/local/bin/：这个命令将 etcdctl 文件移动到 /usr/local/bin 目录，和上一条命令类似。</span>
<span class="c"># </span>
<span class="c"># 总结起来，以上命令将从名为 etcd*.tar.gz 的压缩文件中解压出 etcd 和 etcdctl 文件，并将它们移动到 /usr/local/bin 目录中。</span>

<span class="c"># 查看/usr/local/bin下内容</span>
<span class="o">[</span>root@localhost cby]# ll /usr/local/bin/
总用量 398984
<span class="nt">-rwxr-xr-x</span> 1 1000 docker 26087608 12月 18 03:41 etcd
<span class="nt">-rwxr-xr-x</span> 1 1000 docker 17207480 12月 18 03:41 etcdctl
<span class="nt">-rwxr-xr-x</span> 1 root root   85766328 12月 17 20:56 kube-apiserver
<span class="nt">-rwxr-xr-x</span> 1 root root   71815352 12月 17 20:56 kube-controller-manager
<span class="nt">-rwxr-xr-x</span> 1 root root   58597560 12月 17 20:56 kubectl
<span class="nt">-rwxr-xr-x</span> 1 root root   58110244 12月 17 20:56 kubelet
<span class="nt">-rwxr-xr-x</span> 1 root root   43258040 12月 17 20:56 kube-proxy
<span class="nt">-rwxr-xr-x</span> 1 root root   47685816 12月 17 20:56 kube-scheduler
<span class="o">[</span>root@localhost cby]# 

</code></pre></div></div>

<h3 id="232查看版本">2.3.2查看版本</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 ~]#  kubelet <span class="nt">--version</span>
Kubernetes v1.35.0
<span class="o">[</span>root@k8s-master01 ~]# etcdctl version
etcdctl version: 3.6.7
API version: 3.6
<span class="o">[</span>root@k8s-master01 ~]# 
</code></pre></div></div>

<h3 id="233将组件发送至其他k8s节点">2.3.3将组件发送至其他k8s节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">Master</span><span class="o">=</span><span class="s1">'k8s-master02 k8s-master03'</span>
<span class="nv">Work</span><span class="o">=</span><span class="s1">'k8s-node01 k8s-node02'</span>

<span class="c"># 拷贝master组件</span>
<span class="k">for </span>NODE <span class="k">in</span> <span class="nv">$Master</span><span class="p">;</span> <span class="k">do </span><span class="nb">echo</span> <span class="nv">$NODE</span><span class="p">;</span> scp /usr/local/bin/kube<span class="o">{</span><span class="nb">let</span>,ctl,-apiserver,-controller-manager,-scheduler,-proxy<span class="o">}</span> <span class="nv">$NODE</span>:/usr/local/bin/<span class="p">;</span> scp /usr/local/bin/etcd<span class="k">*</span> <span class="nv">$NODE</span>:/usr/local/bin/<span class="p">;</span> <span class="k">done</span>

<span class="c"># 该命令是一个for循环，对于在$Master变量中的每个节点，执行以下操作：</span>
<span class="c"># </span>
<span class="c"># 1. 打印出节点的名称。</span>
<span class="c"># 2. 使用scp命令将/usr/local/bin/kubelet、kubectl、kube-apiserver、kube-controller-manager、kube-scheduler和kube-proxy文件复制到节点的/usr/local/bin/目录下。</span>
<span class="c"># 3. 使用scp命令将/usr/local/bin/etcd*文件复制到节点的/usr/local/bin/目录下。</span>


<span class="c"># 拷贝work组件</span>
<span class="k">for </span>NODE <span class="k">in</span> <span class="nv">$Work</span><span class="p">;</span> <span class="k">do </span><span class="nb">echo</span> <span class="nv">$NODE</span><span class="p">;</span> scp /usr/local/bin/kube<span class="o">{</span><span class="nb">let</span>,-proxy<span class="o">}</span> <span class="nv">$NODE</span>:/usr/local/bin/ <span class="p">;</span> <span class="k">done</span>
<span class="c"># 该命令是一个for循环，对于在$Master变量中的每个节点，执行以下操作：</span>
<span class="c"># </span>
<span class="c"># 1. 打印出节点的名称。</span>
<span class="c"># 2. 使用scp命令将/usr/local/bin/kubelet和kube-proxy文件复制到节点的/usr/local/bin/目录下。</span>

<span class="c"># 所有节点执行</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> /opt/cni/bin
</code></pre></div></div>

<h2 id="23创建证书相关文件">2.3创建证书相关文件</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 请查看Github仓库 或者进行获取已经打好的包</span>
<span class="c"># 可以根据下文3.x进行手动部署操作 </span>
https://github.com/cby-chen/Kubernetes/
https://github.com/cby-chen/Kubernetes/tags
https://github.com/cby-chen/Kubernetes/releases/download/v1.35.0/kubernetes-v1.35.0.tar
</code></pre></div></div>

<h1 id="3相关证书生成">3.相关证书生成</h1>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># master01节点下载证书生成工具</span>
<span class="c"># wget "https://github.com/cloudflare/cfssl/releases/download/v1.6.5/cfssl_1.6.5_linux_amd64" -O /usr/local/bin/cfssl</span>
<span class="c"># wget "https://github.com/cloudflare/cfssl/releases/download/v1.6.5/cfssljson_1.6.5_linux_amd64" -O /usr/local/bin/cfssljson</span>

<span class="c"># 软件包内有</span>
<span class="nb">cp </span>cfssl_<span class="k">*</span>_linux_amd64 /usr/local/bin/cfssl
<span class="nb">cp </span>cfssljson_<span class="k">*</span>_linux_amd64 /usr/local/bin/cfssljson

<span class="c"># 添加执行权限</span>
<span class="nb">chmod</span> +x /usr/local/bin/cfssl /usr/local/bin/cfssljson
</code></pre></div></div>

<h2 id="31生成etcd证书">3.1.生成etcd证书</h2>

<p>特别说明除外，以下操作在所有master节点操作</p>

<h3 id="311所有master节点创建证书存放目录">3.1.1所有master节点创建证书存放目录</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> /etc/etcd/ssl <span class="nt">-p</span>
</code></pre></div></div>

<h3 id="312master01节点生成etcd证书">3.1.2master01节点生成etcd证书</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 写入生成证书所需的配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> ca-config.json <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "signing": {
    "default": {
      "expiry": "876000h"
    },
    "profiles": {
      "kubernetes": {
        "usages": [
            "signing",
            "key encipherment",
            "server auth",
            "client auth"
        ],
        "expiry": "876000h"
      }
    }
  }
}
</span><span class="no">EOF
</span><span class="c"># 这段配置文件是用于配置加密和认证签名的一些参数。</span>
<span class="c"># </span>
<span class="c"># 在这里，有两个部分：`signing`和`profiles`。</span>
<span class="c"># </span>
<span class="c"># `signing`包含了默认签名配置和配置文件。</span>
<span class="c"># 默认签名配置`default`指定了证书的过期时间为`876000h`。`876000h`表示证书有效期为100年。</span>
<span class="c"># </span>
<span class="c"># `profiles`部分定义了不同的证书配置文件。</span>
<span class="c"># 在这里，只有一个配置文件`kubernetes`。它包含了以下`usages`和过期时间`expiry`：</span>
<span class="c"># </span>
<span class="c"># 1. `signing`：用于对其他证书进行签名</span>
<span class="c"># 2. `key encipherment`：用于加密和解密传输数据</span>
<span class="c"># 3. `server auth`：用于服务器身份验证</span>
<span class="c"># 4. `client auth`：用于客户端身份验证</span>
<span class="c"># </span>
<span class="c"># 对于`kubernetes`配置文件，证书的过期时间也是`876000h`，即100年。</span>

<span class="nb">cat</span> <span class="o">&gt;</span> etcd-ca-csr.json  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "etcd",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "etcd",
      "OU": "Etcd Security"
    }
  ],
  "ca": {
    "expiry": "876000h"
  }
}
</span><span class="no">EOF
</span><span class="c"># 这是一个用于生成证书签名请求（Certificate Signing Request，CSR）的JSON配置文件。JSON配置文件指定了生成证书签名请求所需的数据。</span>
<span class="c"># </span>
<span class="c"># - "CN": "etcd" 指定了希望生成的证书的CN字段（Common Name），即证书的主题，通常是该证书标识的实体的名称。</span>
<span class="c"># - "key": {} 指定了生成证书所使用的密钥的配置信息。"algo": "rsa" 指定了密钥的算法为RSA，"size": 2048 指定了密钥的长度为2048位。</span>
<span class="c"># - "names": [] 包含了生成证书时所需的实体信息。在这个例子中，只包含了一个实体，其相关信息如下：</span>
<span class="c">#   - "C": "CN" 指定了实体的国家/地区代码，这里是中国。</span>
<span class="c">#   - "ST": "Beijing" 指定了实体所在的省/州。</span>
<span class="c">#   - "L": "Beijing" 指定了实体所在的城市。</span>
<span class="c">#   - "O": "etcd" 指定了实体的组织名称。</span>
<span class="c">#   - "OU": "Etcd Security" 指定了实体所属的组织单位。</span>
<span class="c"># - "ca": {} 指定了生成证书时所需的CA（Certificate Authority）配置信息。</span>
<span class="c">#   - "expiry": "876000h" 指定了证书的有效期，这里是876000小时。</span>
<span class="c"># </span>
<span class="c"># 生成证书签名请求时，可以使用这个JSON配置文件作为输入，根据配置文件中的信息生成相应的CSR文件。然后，可以将CSR文件发送给CA进行签名，以获得有效的证书。</span>

<span class="c"># 生成etcd证书和etcd证书的key（如果你觉得以后可能会扩容，可以在ip那多写几个预留出来）</span>
<span class="c"># 若没有IPv6 可删除可保留 </span>

cfssl gencert <span class="nt">-initca</span> etcd-ca-csr.json | cfssljson <span class="nt">-bare</span> /etc/etcd/ssl/etcd-ca
<span class="c"># 具体的解释如下：</span>
<span class="c"># </span>
<span class="c"># cfssl是一个用于生成TLS/SSL证书的工具，它支持PKI、JSON格式配置文件以及与许多其他集成工具的配合使用。</span>
<span class="c"># </span>
<span class="c"># gencert参数表示生成证书的操作。-initca参数表示初始化一个CA（证书颁发机构）。CA是用于签发其他证书的根证书。etcd-ca-csr.json是一个JSON格式的配置文件，其中包含了CA的详细信息，如私钥、公钥、有效期等。这个文件提供了生成CA证书所需的信息。</span>
<span class="c"># </span>
<span class="c"># | 符号表示将上一个命令的输出作为下一个命令的输入。</span>
<span class="c"># </span>
<span class="c"># cfssljson是cfssl工具的一个子命令，用于格式化cfssl生成的JSON数据。 -bare参数表示直接输出裸证书，即只生成证书文件，不包含其他格式的文件。/etc/etcd/ssl/etcd-ca是指定生成的证书文件的路径和名称。</span>
<span class="c"># </span>
<span class="c"># 所以，这条命令的含义是使用cfssl工具根据配置文件ca-csr.json生成一个CA证书，并将证书文件保存在/etc/etcd/ssl/etcd-ca路径下。</span>

<span class="nb">cat</span> <span class="o">&gt;</span> etcd-csr.json <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "etcd",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "etcd",
      "OU": "Etcd Security"
    }
  ]
}
</span><span class="no">EOF
</span><span class="c"># 这段代码是一个JSON格式的配置文件，用于生成一个证书签名请求（Certificate Signing Request，CSR）。</span>
<span class="c"># </span>
<span class="c"># 首先，"CN"字段指定了该证书的通用名称（Common Name），这里设为"etcd"。</span>
<span class="c"># </span>
<span class="c"># 接下来，"key"字段指定了密钥的算法（"algo"字段）和长度（"size"字段），此处使用的是RSA算法，密钥长度为2048位。</span>
<span class="c"># </span>
<span class="c"># 最后，"names"字段是一个数组，其中包含了一个名字对象，用于指定证书中的一些其他信息。这个名字对象包含了以下字段：</span>
<span class="c"># - "C"字段指定了国家代码（Country），这里设置为"CN"。</span>
<span class="c"># - "ST"字段指定了省份（State）或地区，这里设置为"Beijing"。</span>
<span class="c"># - "L"字段指定了城市（Locality），这里设置为"Beijing"。</span>
<span class="c"># - "O"字段指定了组织（Organization），这里设置为"etcd"。</span>
<span class="c"># - "OU"字段指定了组织单元（Organizational Unit），这里设置为"Etcd Security"。</span>
<span class="c"># </span>
<span class="c"># 这些字段将作为证书的一部分，用于标识和验证证书的使用范围和颁发者等信息。</span>

cfssl gencert <span class="se">\</span>
   <span class="nt">-ca</span><span class="o">=</span>/etc/etcd/ssl/etcd-ca.pem <span class="se">\</span>
   <span class="nt">-ca-key</span><span class="o">=</span>/etc/etcd/ssl/etcd-ca-key.pem <span class="se">\</span>
   <span class="nt">-config</span><span class="o">=</span>ca-config.json <span class="se">\</span>
   <span class="nt">-hostname</span><span class="o">=</span>127.0.0.1,k8s-master01,k8s-master02,k8s-master03,192.168.1.31,192.168.1.32,192.168.1.33,::1 <span class="se">\</span>
   <span class="nt">-profile</span><span class="o">=</span>kubernetes <span class="se">\</span>
   etcd-csr.json | cfssljson <span class="nt">-bare</span> /etc/etcd/ssl/etcd
<span class="c"># 这是一条使用cfssl生成etcd证书的命令，下面是各个参数的解释：</span>
<span class="c"># </span>
<span class="c"># -ca=/etc/etcd/ssl/etcd-ca.pem：指定用于签名etcd证书的CA文件的路径。</span>
<span class="c"># -ca-key=/etc/etcd/ssl/etcd-ca-key.pem：指定用于签名etcd证书的CA私钥文件的路径。</span>
<span class="c"># -config=ca-config.json：指定CA配置文件的路径，该文件定义了证书的有效期、加密算法等设置。</span>
<span class="c"># -hostname=xxxx：指定要为etcd生成证书的主机名和IP地址列表。</span>
<span class="c"># -profile=kubernetes：指定使用的证书配置文件，该文件定义了证书的用途和扩展属性。</span>
<span class="c"># etcd-csr.json：指定etcd证书请求的JSON文件的路径，该文件包含了证书请求的详细信息。</span>
<span class="c"># | cfssljson -bare /etc/etcd/ssl/etcd：通过管道将cfssl命令的输出传递给cfssljson命令，并使用-bare参数指定输出文件的前缀路径，这里将生成etcd证书的.pem和-key.pem文件。</span>
<span class="c"># </span>
<span class="c"># 这条命令的作用是使用指定的CA证书和私钥，根据证书请求的JSON文件和配置文件生成etcd的证书文件。</span>
</code></pre></div></div>

<h3 id="313将证书复制到其他节点">3.1.3将证书复制到其他节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">Master</span><span class="o">=</span><span class="s1">'k8s-master02 k8s-master03'</span>
<span class="k">for </span>NODE <span class="k">in</span> <span class="nv">$Master</span><span class="p">;</span> <span class="k">do </span>ssh <span class="nv">$NODE</span> <span class="s2">"mkdir -p /etc/etcd/ssl"</span><span class="p">;</span> <span class="k">for </span>FILE <span class="k">in </span>etcd-ca-key.pem  etcd-ca.pem  etcd-key.pem  etcd.pem<span class="p">;</span> <span class="k">do </span>scp /etc/etcd/ssl/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span> <span class="nv">$NODE</span>:/etc/etcd/ssl/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span><span class="p">;</span> <span class="k">done</span><span class="p">;</span> <span class="k">done</span>

<span class="c"># 这个命令是一个简单的for循环，在一个由`$Master`存储的主机列表中迭代执行。对于每个主机，它使用`ssh`命令登录到主机，并在远程主机上创建一个名为`/etc/etcd/ssl`的目录（如果不存在）。接下来，它使用`scp`将本地主机上`/etc/etcd/ssl`目录中的四个文件（`etcd-ca-key.pem`，`etcd-ca.pem`，`etcd-key.pem`和`etcd.pem`）复制到远程主机的`/etc/etcd/ssl`目录中。最终的结果是，远程主机上的`/etc/etcd/ssl`目录中包含与本地主机上相同的四个文件的副本。</span>
</code></pre></div></div>

<h2 id="32生成k8s相关证书">3.2.生成k8s相关证书</h2>

<p>特别说明除外，以下操作在所有master节点操作</p>

<h3 id="321-所有k8s节点创建证书存放目录">3.2.1 所有k8s节点创建证书存放目录</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /etc/kubernetes/pki
</code></pre></div></div>

<h3 id="322-master01节点生成k8s证书">3.2.2 master01节点生成k8s证书</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 写入生成证书所需的配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> ca-csr.json   <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "kubernetes",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "Kubernetes",
      "OU": "Kubernetes-manual"
    }
  ],
  "ca": {
    "expiry": "876000h"
  }
}
</span><span class="no">EOF
</span><span class="c"># 这是一个用于生成 Kubernetes 相关证书的配置文件。该配置文件中包含以下信息：</span>
<span class="c"># </span>
<span class="c"># - CN：CommonName，即用于标识证书的通用名称。在此配置中，CN 设置为 "kubernetes"，表示该证书是用于 Kubernetes。</span>
<span class="c"># - key：用于生成证书的算法和大小。在此配置中，使用的算法是 RSA，大小是 2048 位。</span>
<span class="c"># - names：用于证书中的名称字段的详细信息。在此配置中，有以下字段信息：</span>
<span class="c">#   - C：Country，即国家。在此配置中，设置为 "CN"。</span>
<span class="c">#   - ST：State，即省/州。在此配置中，设置为 "Beijing"。</span>
<span class="c">#   - L：Locality，即城市。在此配置中，设置为 "Beijing"。</span>
<span class="c">#   - O：Organization，即组织。在此配置中，设置为 "Kubernetes"。</span>
<span class="c">#   - OU：Organization Unit，即组织单位。在此配置中，设置为 "Kubernetes-manual"。</span>
<span class="c"># - ca：用于证书签名的证书颁发机构（CA）的配置信息。在此配置中，设置了证书的有效期为 876000 小时。</span>
<span class="c"># </span>
<span class="c"># 这个配置文件可以用于生成 Kubernetes 相关的证书，以确保集群中的通信安全性。</span>

cfssl gencert <span class="nt">-initca</span> ca-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/ca

<span class="c"># 具体的解释如下：</span>
<span class="c"># </span>
<span class="c"># cfssl是一个用于生成TLS/SSL证书的工具，它支持PKI、JSON格式配置文件以及与许多其他集成工具的配合使用。</span>
<span class="c"># </span>
<span class="c"># gencert参数表示生成证书的操作。-initca参数表示初始化一个CA（证书颁发机构）。CA是用于签发其他证书的根证书。ca-csr.json是一个JSON格式的配置文件，其中包含了CA的详细信息，如私钥、公钥、有效期等。这个文件提供了生成CA证书所需的信息。</span>
<span class="c"># </span>
<span class="c"># | 符号表示将上一个命令的输出作为下一个命令的输入。</span>
<span class="c"># </span>
<span class="c"># cfssljson是cfssl工具的一个子命令，用于格式化cfssl生成的JSON数据。 -bare参数表示直接输出裸证书，即只生成证书文件，不包含其他格式的文件。/etc/kubernetes/pki/ca是指定生成的证书文件的路径和名称。</span>
<span class="c"># </span>
<span class="c"># 所以，这条命令的含义是使用cfssl工具根据配置文件ca-csr.json生成一个CA证书，并将证书文件保存在/etc/kubernetes/pki/ca路径下。</span>

<span class="nb">cat</span> <span class="o">&gt;</span> apiserver-csr.json <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "kube-apiserver",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "Kubernetes",
      "OU": "Kubernetes-manual"
    }
  ]
}
</span><span class="no">EOF

</span><span class="c"># 这是一个用于生成 Kubernetes 相关证书的配置文件。该配置文件中包含以下信息：</span>
<span class="c"># </span>
<span class="c"># - `CN` 字段指定了证书的通用名称 (Common Name)，这里设置为 "kube-apiserver"，表示该证书用于 Kubernetes API Server。</span>
<span class="c"># - `key` 字段指定了生成证书时所选用的加密算法和密钥长度。这里选用了 RSA 算法，密钥长度为 2048 位。</span>
<span class="c"># - `names` 字段包含了一组有关证书持有者信息的项。这里使用了以下信息：</span>
<span class="c">#   - `C` 表示国家代码 (Country)，这里设置为 "CN" 表示中国。</span>
<span class="c">#   - `ST` 表示州或省份 (State)，这里设置为 "Beijing" 表示北京市。</span>
<span class="c">#   - `L` 表示城市或地区 (Location)，这里设置为 "Beijing" 表示北京市。</span>
<span class="c">#   - `O` 表示组织名称 (Organization)，这里设置为 "Kubernetes" 表示 Kubernetes。</span>
<span class="c">#   - `OU` 表示组织单位 (Organizational Unit)，这里设置为 "Kubernetes-manual" 表示手动管理的 Kubernetes 集群。</span>
<span class="c"># </span>
<span class="c"># 这个配置文件可以用于生成 Kubernetes 相关的证书，以确保集群中的通信安全性。</span>


<span class="c"># 生成一个根证书 ，多写了一些IP作为预留IP，为将来添加node做准备</span>
<span class="c"># 10.96.0.1是service网段的第一个地址，需要计算，192.168.1.36为高可用vip地址</span>
<span class="c"># 若没有IPv6 可删除可保留 </span>

cfssl gencert   <span class="se">\</span>
<span class="nt">-ca</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem   <span class="se">\</span>
<span class="nt">-ca-key</span><span class="o">=</span>/etc/kubernetes/pki/ca-key.pem   <span class="se">\</span>
<span class="nt">-config</span><span class="o">=</span>ca-config.json   <span class="se">\</span>
<span class="nt">-hostname</span><span class="o">=</span>10.96.0.1,192.168.1.36,127.0.0.1,kubernetes,kubernetes.default,kubernetes.default.svc,kubernetes.default.svc.cluster,kubernetes.default.svc.cluster.local,x.oiox.cn,z.oiox.cn,192.168.1.31,192.168.1.32,192.168.1.33,192.168.1.34,192.168.1.35,192.168.1.36,192.168.1.37,192.168.1.38,192.168.1.39,192.168.1.40,::1   <span class="se">\</span>
<span class="nt">-profile</span><span class="o">=</span>kubernetes   apiserver-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/apiserver

<span class="c"># 这个命令是使用cfssl工具生成Kubernetes API Server的证书。</span>
<span class="c"># </span>
<span class="c"># 命令的参数解释如下：</span>
<span class="c"># - `-ca=/etc/kubernetes/pki/ca.pem`：指定证书的颁发机构（CA）文件路径。</span>
<span class="c"># - `-ca-key=/etc/kubernetes/pki/ca-key.pem`：指定证书的颁发机构（CA）私钥文件路径。</span>
<span class="c"># - `-config=ca-config.json`：指定证书生成的配置文件路径，配置文件中包含了证书的有效期、加密算法等信息。</span>
<span class="c"># - `-hostname=10.96.0.1,192.168.1.36,127.0.0.1,fc00:43f4:1eea:1::10`：指定证书的主机名或IP地址列表。</span>
<span class="c"># - `-profile=kubernetes`：指定证书生成的配置文件中的配置文件名。</span>
<span class="c"># - `apiserver-csr.json`：API Server的证书签名请求配置文件路径。</span>
<span class="c"># - `| cfssljson -bare /etc/kubernetes/pki/apiserver`：通过管道将生成的证书输出到cfssljson工具，将其转换为PEM编码格式，并保存到 `/etc/kubernetes/pki/apiserver.pem` 和 `/etc/kubernetes/pki/apiserver-key.pem` 文件中。</span>
<span class="c"># </span>
<span class="c"># 最终，这个命令将会生成API Server的证书和私钥，并保存到指定的文件中。</span>
</code></pre></div></div>

<h3 id="323-生成apiserver聚合证书">3.2.3 生成apiserver聚合证书</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> front-proxy-ca-csr.json  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "kubernetes",
  "key": {
     "algo": "rsa",
     "size": 2048
  },
  "ca": {
    "expiry": "876000h"
  }
}
</span><span class="no">EOF

</span><span class="c"># 这个JSON文件表示了生成一个名为"kubernetes"的证书的配置信息。这个证书是用来进行Kubernetes集群的身份验证和安全通信。</span>
<span class="c"># </span>
<span class="c"># 配置信息包括以下几个部分：</span>
<span class="c"># </span>
<span class="c"># 1. "CN": "kubernetes"：这表示了证书的通用名称（Common Name），也就是证书所代表的实体的名称。在这里，证书的通用名称被设置为"kubernetes"，表示这个证书是用来代表Kubernetes集群。</span>
<span class="c"># </span>
<span class="c"># 2. "key"：这是用来生成证书的密钥相关的配置。在这里，配置使用了RSA算法，并且设置了密钥的大小为2048位。</span>
<span class="c"># </span>
<span class="c"># 3. "ca"：这个字段指定了证书的颁发机构（Certificate Authority）相关的配置。在这里，配置指定了证书的有效期为876000小时，即100年。这意味着该证书在100年内将被视为有效，过期后需要重新生成。</span>
<span class="c"># </span>
<span class="c"># 总之，这个JSON文件中的配置信息描述了如何生成一个用于Kubernetes集群的证书，包括证书的通用名称、密钥算法和大小以及证书的有效期。</span>

cfssl gencert   <span class="nt">-initca</span> front-proxy-ca-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/front-proxy-ca 
<span class="c"># 具体的解释如下：</span>
<span class="c"># </span>
<span class="c"># cfssl是一个用于生成TLS/SSL证书的工具，它支持PKI、JSON格式配置文件以及与许多其他集成工具的配合使用。</span>
<span class="c"># </span>
<span class="c"># gencert参数表示生成证书的操作。-initca参数表示初始化一个CA（证书颁发机构）。CA是用于签发其他证书的根证书。front-proxy-ca-csr.json是一个JSON格式的配置文件，其中包含了CA的详细信息，如私钥、公钥、有效期等。这个文件提供了生成CA证书所需的信息。</span>
<span class="c"># </span>
<span class="c"># | 符号表示将上一个命令的输出作为下一个命令的输入。</span>
<span class="c"># </span>
<span class="c"># cfssljson是cfssl工具的一个子命令，用于格式化cfssl生成的JSON数据。 -bare参数表示直接输出裸证书，即只生成证书文件，不包含其他格式的文件。/etc/kubernetes/pki/front-proxy-ca是指定生成的证书文件的路径和名称。</span>
<span class="c"># </span>
<span class="c"># 所以，这条命令的含义是使用cfssl工具根据配置文件ca-csr.json生成一个CA证书，并将证书文件保存在/etc/kubernetes/pki/front-proxy-ca路径下。</span>

<span class="nb">cat</span> <span class="o">&gt;</span> front-proxy-client-csr.json  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "front-proxy-client",
  "key": {
     "algo": "rsa",
     "size": 2048
  }
}
</span><span class="no">EOF

</span><span class="c"># 这是一个JSON格式的配置文件，用于描述一个名为"front-proxy-client"的配置。配置包括两个字段：CN和key。</span>
<span class="c"># </span>
<span class="c"># - CN（Common Name）字段表示证书的通用名称，这里为"front-proxy-client"。</span>
<span class="c"># - key字段描述了密钥的算法和大小。"algo"表示使用RSA算法，"size"表示密钥大小为2048位。</span>
<span class="c"># </span>
<span class="c"># 该配置文件用于生成一个SSL证书，用于在前端代理客户端进行认证和数据传输的加密。这个证书中的通用名称是"front-proxy-client"，使用RSA算法生成，密钥大小为2048位。</span>

cfssl gencert  <span class="se">\</span>
<span class="nt">-ca</span><span class="o">=</span>/etc/kubernetes/pki/front-proxy-ca.pem   <span class="se">\</span>
<span class="nt">-ca-key</span><span class="o">=</span>/etc/kubernetes/pki/front-proxy-ca-key.pem   <span class="se">\</span>
<span class="nt">-config</span><span class="o">=</span>ca-config.json   <span class="se">\</span>
<span class="nt">-profile</span><span class="o">=</span>kubernetes   front-proxy-client-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/front-proxy-client

<span class="c"># 这个命令使用cfssl工具生成一个用于Kubernetes的front-proxy-client证书。</span>
<span class="c"># </span>
<span class="c"># 主要参数解释如下：</span>
<span class="c"># - `-ca=/etc/kubernetes/pki/front-proxy-ca.pem`: 指定用于签署证书的根证书文件路径。</span>
<span class="c"># - `-ca-key=/etc/kubernetes/pki/front-proxy-ca-key.pem`: 指定用于签署证书的根证书的私钥文件路径。</span>
<span class="c"># - `-config=ca-config.json`: 指定用于配置证书签署的配置文件路径。该配置文件描述了证书生成的一些规则，如加密算法和有效期等。</span>
<span class="c"># - `-profile=kubernetes`: 指定生成证书时使用的配置文件中定义的profile，其中包含了一些默认的参数。</span>
<span class="c"># - `front-proxy-client-csr.json`: 指定用于生成证书的CSR文件路径，该文件包含了证书请求的相关信息。</span>
<span class="c"># - `| cfssljson -bare /etc/kubernetes/pki/front-proxy-client`: 通过管道将生成的证书输出到cfssljson工具进行解析，并通过`-bare`参数将证书和私钥分别保存到指定路径。</span>
<span class="c"># </span>
<span class="c"># 这个命令的作用是根据提供的CSR文件和配置信息，使用指定的根证书和私钥生成一个前端代理客户端的证书，并将证书和私钥分别保存到`/etc/kubernetes/pki/front-proxy-client.pem`和`/etc/kubernetes/pki/front-proxy-client-key.pem`文件中。</span>
</code></pre></div></div>

<h3 id="324-生成controller-manage的证书">3.2.4 生成controller-manage的证书</h3>

<p>在《5.高可用配置》选择使用那种高可用方案
若使用 haproxy、keepalived 那么为 <code class="language-plaintext highlighter-rouge">--server=https://192.168.1.36:9443</code>
若使用 nginx方案，那么为 <code class="language-plaintext highlighter-rouge">--server=https://127.0.0.1:8443</code></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> manager-csr.json <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "system:kube-controller-manager",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "system:kube-controller-manager",
      "OU": "Kubernetes-manual"
    }
  ]
}
</span><span class="no">EOF
</span><span class="c"># 这是一个用于生成密钥对（公钥和私钥）的JSON配置文件。下面是针对该文件中每个字段的详细解释：</span>
<span class="c"># </span>
<span class="c"># - "CN": 值为"system:kube-controller-manager"，代表通用名称（Common Name），是此密钥对的主题（subject）。</span>
<span class="c"># - "key": 这个字段用来定义密钥算法和大小。</span>
<span class="c">#   - "algo": 值为"rsa"，表示使用RSA算法。</span>
<span class="c">#   - "size": 值为2048，表示生成的密钥大小为2048位。</span>
<span class="c"># - "names": 这个字段用来定义密钥对的各个名称字段。</span>
<span class="c">#   - "C": 值为"CN"，表示国家（Country）名称是"CN"（中国）。</span>
<span class="c">#   - "ST": 值为"Beijing"，表示省/州（State/Province）名称是"Beijing"（北京）。</span>
<span class="c">#   - "L": 值为"Beijing"，表示城市（Locality）名称是"Beijing"（北京）。</span>
<span class="c">#   - "O": 值为"system:kube-controller-manager"，表示组织（Organization）名称是"system:kube-controller-manager"。</span>
<span class="c">#   - "OU": 值为"Kubernetes-manual"，表示组织单位（Organizational Unit）名称是"Kubernetes-manual"。</span>
<span class="c"># </span>
<span class="c"># 这个JSON配置文件基本上是告诉生成密钥对的工具，生成一个带有特定名称和属性的密钥对。</span>


cfssl gencert <span class="se">\</span>
   <span class="nt">-ca</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem <span class="se">\</span>
   <span class="nt">-ca-key</span><span class="o">=</span>/etc/kubernetes/pki/ca-key.pem <span class="se">\</span>
   <span class="nt">-config</span><span class="o">=</span>ca-config.json <span class="se">\</span>
   <span class="nt">-profile</span><span class="o">=</span>kubernetes <span class="se">\</span>
   manager-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/controller-manager

<span class="c"># 这是一个命令行操作，使用cfssl工具生成证书。</span>
<span class="c"># </span>
<span class="c"># 1. `cfssl gencert` 是cfssl工具的命令，用于生成证书。</span>
<span class="c"># 2. `-ca` 指定根证书的路径和文件名，这里是`/etc/kubernetes/pki/ca.pem`。</span>
<span class="c"># 3. `-ca-key` 指定根证书的私钥的路径和文件名，这里是`/etc/kubernetes/pki/ca-key.pem`。</span>
<span class="c"># 4. `-config` 指定配置文件的路径和文件名，这里是`ca-config.json`。</span>
<span class="c"># 5. `-profile` 指定证书使用的配置文件中的配置模板，这里是`kubernetes`。</span>
<span class="c"># 6. `manager-csr.json` 是证书签发请求的配置文件，用于生成证书签发请求。</span>
<span class="c"># 7. `|` 管道操作符，将前一条命令的输出作为后一条命令的输入。</span>
<span class="c"># 8. `cfssljson -bare` 是 cfssl 工具的命令，作用是将证书签发请求的输出转换为PKCS＃1、PKCS＃8和x509 PEM文件。</span>
<span class="c"># 9. `/etc/kubernetes/pki/controller-manager` 是转换后的 PEM 文件的存储位置和文件名。</span>
<span class="c"># </span>
<span class="c"># 这个命令的作用是根据根证书和私钥、配置文件以及证书签发请求的配置文件，生成经过签发的控制器管理器证书和私钥，并将转换后的 PEM 文件保存到指定的位置。</span>


<span class="c"># 设置一个集群项</span>
<span class="c"># 在《5.高可用配置》选择使用那种高可用方案</span>
<span class="c"># 若使用 haproxy、keepalived 那么为 `--server=https://192.168.1.36:9443`</span>
<span class="c"># 若使用 nginx方案，那么为 `--server=https://127.0.0.1:8443`</span>
kubectl config set-cluster kubernetes <span class="se">\</span>
     <span class="nt">--certificate-authority</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem <span class="se">\</span>
     <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span> <span class="se">\</span>
     <span class="nt">--server</span><span class="o">=</span>https://127.0.0.1:8443 <span class="se">\</span>
     <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/controller-manager.kubeconfig
<span class="c"># kubectl config set-cluster命令用于配置集群信息。</span>
<span class="c"># --certificate-authority选项指定了集群的证书颁发机构（CA）的路径，这个CA会验证kube-apiserver提供的证书是否合法。</span>
<span class="c"># --embed-certs选项用于将证书嵌入到生成的kubeconfig文件中，这样就不需要在kubeconfig文件中单独指定证书文件路径。</span>
<span class="c"># --server选项指定了kube-apiserver的地址，这里使用的是127.0.0.1:8443，表示使用本地主机上的kube-apiserver，默认端口为8443。</span>
<span class="c"># --kubeconfig选项指定了生成的kubeconfig文件的路径和名称，这里指定为/etc/kubernetes/controller-manager.kubeconfig。</span>
<span class="c"># 综上所述，kubectl config set-cluster命令的作用是在kubeconfig文件中设置集群信息，包括证书颁发机构、证书、kube-apiserver地址等。</span>


<span class="c"># 设置一个环境项，一个上下文</span>
kubectl config set-context system:kube-controller-manager@kubernetes <span class="se">\</span>
    <span class="nt">--cluster</span><span class="o">=</span>kubernetes <span class="se">\</span>
    <span class="nt">--user</span><span class="o">=</span>system:kube-controller-manager <span class="se">\</span>
    <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/controller-manager.kubeconfig

<span class="c"># 这个命令用于配置 Kubernetes 控制器管理器的上下文信息。下面是各个参数的详细解释：</span>
<span class="c"># 1. `kubectl config set-context system:kube-controller-manager@kubernetes`: 设置上下文的名称为 `system:kube-controller-manager@kubernetes`，这是一个标识符，用于唯一标识该上下文。</span>
<span class="c"># 2. `--cluster=kubernetes`: 指定集群的名称为 `kubernetes`，这是一个现有集群的标识符，表示要管理的 Kubernetes 集群。</span>
<span class="c"># 3. `--user=system:kube-controller-manager`: 指定使用的用户身份为 `system:kube-controller-manager`。这是一个特殊的用户身份，具有控制 Kubernetes 控制器管理器的权限。</span>
<span class="c"># 4. `--kubeconfig=/etc/kubernetes/controller-manager.kubeconfig`: 指定 kubeconfig 文件的路径为 `/etc/kubernetes/controller-manager.kubeconfig`。kubeconfig 文件是一个用于管理 Kubernetes 配置的文件，包含了集群、用户和上下文的相关信息。</span>
<span class="c"># 通过运行这个命令，可以将这些配置信息保存到 `/etc/kubernetes/controller-manager.kubeconfig` 文件中，以便在后续的操作中使用。</span>



  <span class="c"># 设置一个用户项</span>
kubectl config set-credentials system:kube-controller-manager <span class="se">\</span>
   <span class="nt">--client-certificate</span><span class="o">=</span>/etc/kubernetes/pki/controller-manager.pem <span class="se">\</span>
   <span class="nt">--client-key</span><span class="o">=</span>/etc/kubernetes/pki/controller-manager-key.pem <span class="se">\</span>
   <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span> <span class="se">\</span>
   <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/controller-manager.kubeconfig
<span class="c"># 上述命令是用于设置 Kubernetes 的 controller-manager 组件的客户端凭据。下面是每个参数的详细解释：</span>
<span class="c"># </span>
<span class="c"># - `kubectl config`: 是使用 kubectl 命令行工具的配置子命令。</span>
<span class="c"># - `set-credentials`: 是定义一个新的用户凭据配置的子命令。</span>
<span class="c"># - `system:kube-controller-manager`: 是设置用户凭据的名称，`system:` 是 Kubernetes API Server 内置的身份验证器使用的用户标识符前缀，它表示是一个系统用户，在本例中是 kube-controller-manager 组件使用的身份。</span>
<span class="c"># - `--client-certificate=/etc/kubernetes/pki/controller-manager.pem`: 指定 controller-manager.pem 客户端证书的路径。</span>
<span class="c"># - `--client-key=/etc/kubernetes/pki/controller-manager-key.pem`: 指定 controller-manager-key.pem 客户端私钥的路径。</span>
<span class="c"># - `--embed-certs=true`: 表示将证书和私钥直接嵌入到生成的 kubeconfig 文件中，而不是通过引用外部文件。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/controller-manager.kubeconfig`: 指定生成的 kubeconfig 文件的路径和文件名，即 controller-manager.kubeconfig。</span>
<span class="c"># </span>
<span class="c"># 通过运行上述命令，将根据提供的证书和私钥信息，为 kube-controller-manager 创建一个 kubeconfig 文件，以便后续使用该文件进行身份验证和访问 Kubernetes API。</span>


<span class="c"># 设置默认环境</span>
kubectl config use-context system:kube-controller-manager@kubernetes <span class="se">\</span>
     <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/controller-manager.kubeconfig

<span class="c"># 这个命令是用来指定kubectl使用指定的上下文环境来执行操作。上下文环境是kubectl用来确定要连接到哪个Kubernetes集群以及使用哪个身份验证信息的配置。</span>
<span class="c"># </span>
<span class="c"># 在这个命令中，`kubectl config use-context`是用来设置当前上下文环境的命令。 `system:kube-controller-manager@kubernetes`是指定的上下文名称，它告诉kubectl要使用的Kubernetes集群和身份验证信息。 </span>
<span class="c"># `--kubeconfig=/etc/kubernetes/controller-manager.kubeconfig`是用来指定使用的kubeconfig文件的路径。kubeconfig文件是存储集群连接和身份验证信息的配置文件。</span>
<span class="c"># 通过执行这个命令，kubectl将使用指定的上下文来执行后续的操作，包括部署和管理Kubernetes资源。</span>
</code></pre></div></div>

<h3 id="325-生成kube-scheduler的证书">3.2.5 生成kube-scheduler的证书</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> scheduler-csr.json <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "system:kube-scheduler",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "system:kube-scheduler",
      "OU": "Kubernetes-manual"
    }
  ]
}
</span><span class="no">EOF
</span><span class="c"># 这个命令是用来创建一个叫做scheduler-csr.json的文件，并将其中的内容赋值给该文件。</span>
<span class="c"># </span>
<span class="c"># 文件内容是一个JSON格式的文本，包含了一个描述证书请求的结构。</span>
<span class="c"># </span>
<span class="c"># 具体内容如下：</span>
<span class="c"># </span>
<span class="c"># - "CN": "system:kube-scheduler"：Common Name字段，表示该证书的名称为system:kube-scheduler。</span>
<span class="c"># - "key": {"algo": "rsa", "size": 2048}：key字段指定生成证书时使用的加密算法是RSA，并且密钥的长度为2048位。</span>
<span class="c"># - "names": [...]：names字段定义了证书中的另外一些标识信息。</span>
<span class="c"># - "C": "CN"：Country字段，表示国家/地区为中国。</span>
<span class="c"># - "ST": "Beijing"：State字段，表示省/市为北京。</span>
<span class="c"># - "L": "Beijing"：Locality字段，表示所在城市为北京。</span>
<span class="c"># - "O": "system:kube-scheduler"：Organization字段，表示组织为system:kube-scheduler。</span>
<span class="c"># - "OU": "Kubernetes-manual"：Organizational Unit字段，表示组织单元为Kubernetes-manual。</span>
<span class="c"># </span>
<span class="c"># 而EOF是一个占位符，用于标记开始和结束的位置。在开始的EOF之后到结束的EOF之间的内容将会被写入到scheduler-csr.json文件中。</span>
<span class="c"># </span>
<span class="c"># 总体来说，这个命令用于生成一个描述kube-scheduler证书请求的JSON文件。</span>

cfssl gencert <span class="se">\</span>
   <span class="nt">-ca</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem <span class="se">\</span>
   <span class="nt">-ca-key</span><span class="o">=</span>/etc/kubernetes/pki/ca-key.pem <span class="se">\</span>
   <span class="nt">-config</span><span class="o">=</span>ca-config.json <span class="se">\</span>
   <span class="nt">-profile</span><span class="o">=</span>kubernetes <span class="se">\</span>
   scheduler-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/scheduler

<span class="c"># 上述命令是使用cfssl工具生成Kubernetes Scheduler的证书。</span>
<span class="c"># </span>
<span class="c"># 具体解释如下：</span>
<span class="c"># </span>
<span class="c"># 1. `cfssl gencert`：使用cfssl工具生成证书。</span>
<span class="c"># 2. `-ca=/etc/kubernetes/pki/ca.pem`：指定根证书文件的路径。在这里，是指定根证书的路径为`/etc/kubernetes/pki/ca.pem`。</span>
<span class="c"># 3. `-ca-key=/etc/kubernetes/pki/ca-key.pem`：指定根证书私钥文件的路径。在这里，是指定根证书私钥的路径为`/etc/kubernetes/pki/ca-key.pem`。</span>
<span class="c"># 4. `-config=ca-config.json`：指定证书配置文件的路径。在这里，是指定证书配置文件的路径为`ca-config.json`。</span>
<span class="c"># 5. `-profile=kubernetes`：指定证书的配置文件中的一个配置文件模板。在这里，是指定配置文件中的`kubernetes`配置模板。</span>
<span class="c"># 6. `scheduler-csr.json`：指定Scheduler的证书签名请求文件（CSR）的路径。在这里，是指定请求文件的路径为`scheduler-csr.json`。</span>
<span class="c"># 7. `|`（管道符号）：将前一个命令的输出作为下一个命令的输入。</span>
<span class="c"># 8. `cfssljson`：将cfssl工具生成的证书签名请求(CSR)进行解析。</span>
<span class="c"># 9. `-bare /etc/kubernetes/pki/scheduler`：指定输出路径和前缀。在这里，是将解析的证书签名请求生成以下文件：`/etc/kubernetes/pki/scheduler.pem`（包含了证书）、`/etc/kubernetes/pki/scheduler-key.pem`（包含了私钥）。</span>
<span class="c"># </span>
<span class="c"># 总结来说，这个命令的目的是根据根证书、根证书私钥、证书配置文件、CSR文件等生成Kubernetes Scheduler的证书和私钥文件。</span>



<span class="c"># 在《5.高可用配置》选择使用那种高可用方案</span>
<span class="c"># 若使用 haproxy、keepalived 那么为 `--server=https://192.168.1.36:9443`</span>
<span class="c"># 若使用 nginx方案，那么为 `--server=https://127.0.0.1:8443`</span>

kubectl config set-cluster kubernetes <span class="se">\</span>
     <span class="nt">--certificate-authority</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem <span class="se">\</span>
     <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span> <span class="se">\</span>
     <span class="nt">--server</span><span class="o">=</span>https://127.0.0.1:8443 <span class="se">\</span>
     <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/scheduler.kubeconfig
<span class="c"># 该命令用于配置一个名为"kubernetes"的集群，并将其应用到/etc/kubernetes/scheduler.kubeconfig文件中。</span>
<span class="c"># </span>
<span class="c"># 该命令的解释如下：</span>
<span class="c"># - `kubectl config set-cluster kubernetes`: 设置一个集群并命名为"kubernetes"。</span>
<span class="c"># - `--certificate-authority=/etc/kubernetes/pki/ca.pem`: 指定集群使用的证书授权机构的路径。</span>
<span class="c"># - `--embed-certs=true`: 该标志指示将证书嵌入到生成的kubeconfig文件中。</span>
<span class="c"># - `--server=https://127.0.0.1:8443`: 指定集群的 API server 位置。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/scheduler.kubeconfig`: 指定要保存 kubeconfig 文件的路径和名称。</span>

kubectl config set-credentials system:kube-scheduler <span class="se">\</span>
     <span class="nt">--client-certificate</span><span class="o">=</span>/etc/kubernetes/pki/scheduler.pem <span class="se">\</span>
     <span class="nt">--client-key</span><span class="o">=</span>/etc/kubernetes/pki/scheduler-key.pem <span class="se">\</span>
     <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span> <span class="se">\</span>
     <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/scheduler.kubeconfig
<span class="c"># 这段命令是用于设置 kube-scheduler 组件的身份验证凭据，并生成相应的 kubeconfig 文件。</span>
<span class="c"># </span>
<span class="c"># 解释每个选项的含义如下：</span>
<span class="c"># - `kubectl config set-credentials system:kube-scheduler`：设置 `system:kube-scheduler` 用户的身份验证凭据。</span>
<span class="c"># - `--client-certificate=/etc/kubernetes/pki/scheduler.pem`：指定一个客户端证书文件，用于基于证书的身份验证。在这种情况下，指定了 kube-scheduler 组件的证书文件路径。</span>
<span class="c"># - `--client-key=/etc/kubernetes/pki/scheduler-key.pem`：指定与客户端证书相对应的客户端私钥文件。</span>
<span class="c"># - `--embed-certs=true`：将客户端证书和私钥嵌入到生成的 kubeconfig 文件中。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/scheduler.kubeconfig`：指定生成的 kubeconfig 文件的路径和名称。</span>
<span class="c"># </span>
<span class="c"># 该命令的目的是为 kube-scheduler 组件生成一个 kubeconfig 文件，以便进行身份验证和访问集群资源。kubeconfig 文件是一个包含了连接到 Kubernetes 集群所需的所有配置信息的文件，包括服务器地址、证书和秘钥等。</span>

kubectl config set-context system:kube-scheduler@kubernetes <span class="se">\</span>
     <span class="nt">--cluster</span><span class="o">=</span>kubernetes <span class="se">\</span>
     <span class="nt">--user</span><span class="o">=</span>system:kube-scheduler <span class="se">\</span>
     <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/scheduler.kubeconfig

<span class="c"># 该命令用于设置一个名为"system:kube-scheduler@kubernetes"的上下文，具体配置如下：</span>
<span class="c"># </span>
<span class="c"># 1. --cluster=kubernetes: 指定集群的名称为"kubernetes"，这个集群是在当前的kubeconfig文件中已经定义好的。</span>
<span class="c"># 2. --user=system:kube-scheduler: 指定用户的名称为"system:kube-scheduler"，这个用户也是在当前的kubeconfig文件中已经定义好的。这个用户用于认证和授权kube-scheduler组件访问Kubernetes集群的权限。</span>
<span class="c"># 3. --kubeconfig=/etc/kubernetes/scheduler.kubeconfig: 指定kubeconfig文件的路径为"/etc/kubernetes/scheduler.kubeconfig"，这个文件将被用来保存上下文的配置信息。</span>
<span class="c"># </span>
<span class="c"># 这个命令的作用是将上述的配置信息保存到指定的kubeconfig文件中，以便后续使用该文件进行认证和授权访问Kubernetes集群。</span>

kubectl config use-context system:kube-scheduler@kubernetes <span class="se">\</span>
     <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/scheduler.kubeconfig

<span class="c"># 上述命令是使用`kubectl`命令来配置Kubernetes集群中的调度器组件。</span>
<span class="c"># </span>
<span class="c"># `kubectl config use-context`命令用于切换`kubectl`当前使用的上下文。上下文是Kubernetes集群、用户和命名空间的组合，用于确定`kubectl`的连接目标。下面解释这个命令的不同部分：</span>
<span class="c"># </span>
<span class="c"># - `system:kube-scheduler@kubernetes`是一个上下文名称。它指定了使用`kube-scheduler`用户和`kubernetes`命名空间的系统级别上下文。系统级别上下文用于操作Kubernetes核心组件。</span>
<span class="c"># </span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/scheduler.kubeconfig`用于指定Kubernetes配置文件的路径。Kubernetes配置文件包含连接到Kubernetes集群所需的身份验证和连接信息。</span>
<span class="c"># </span>
<span class="c"># 通过运行以上命令，`kubectl`将使用指定的上下文和配置文件，以便在以后的命令中能正确地与Kubernetes集群中的调度器组件进行交互。</span>
</code></pre></div></div>

<h3 id="326-生成admin的证书配置">3.2.6 生成admin的证书配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> admin-csr.json <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "admin",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "system:masters",
      "OU": "Kubernetes-manual"
    }
  ]
}
</span><span class="no">EOF
</span><span class="c"># 这段代码是一个JSON格式的配置文件，用于创建和配置一个名为"admin"的Kubernetes凭证。</span>
<span class="c"># </span>
<span class="c"># 这个凭证包含以下字段：</span>
<span class="c"># </span>
<span class="c"># - "CN": "admin": 这是凭证的通用名称，表示这是一个管理员凭证。</span>
<span class="c"># - "key": 这是一个包含证书密钥相关信息的对象。</span>
<span class="c">#   - "algo": "rsa"：这是使用的加密算法类型，这里是RSA加密算法。</span>
<span class="c">#   - "size": 2048：这是密钥的大小，这里是2048位。</span>
<span class="c"># - "names": 这是一个包含证书名称信息的数组。</span>
<span class="c">#   - "C": "CN"：这是证书的国家/地区字段，这里是中国。</span>
<span class="c">#   - "ST": "Beijing"：这是证书的省/州字段，这里是北京。</span>
<span class="c">#   - "L": "Beijing"：这是证书的城市字段，这里是北京。</span>
<span class="c">#   - "O": "system:masters"：这是证书的组织字段，这里是system:masters，表示系统的管理员组。</span>
<span class="c">#   - "OU": "Kubernetes-manual"：这是证书的部门字段，这里是Kubernetes-manual。</span>
<span class="c"># </span>
<span class="c"># 通过这个配置文件创建的凭证将具有管理员权限，并且可以用于管理Kubernetes集群。</span>

cfssl gencert <span class="se">\</span>
   <span class="nt">-ca</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem <span class="se">\</span>
   <span class="nt">-ca-key</span><span class="o">=</span>/etc/kubernetes/pki/ca-key.pem <span class="se">\</span>
   <span class="nt">-config</span><span class="o">=</span>ca-config.json <span class="se">\</span>
   <span class="nt">-profile</span><span class="o">=</span>kubernetes <span class="se">\</span>
   admin-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/admin

<span class="c"># 上述命令是使用cfssl工具生成Kubernetes admin的证书。</span>
<span class="c"># </span>
<span class="c"># 具体解释如下：</span>
<span class="c"># </span>
<span class="c"># 1. `cfssl gencert`：使用cfssl工具生成证书。</span>
<span class="c"># 2. `-ca=/etc/kubernetes/pki/ca.pem`：指定根证书文件的路径。在这里，是指定根证书的路径为`/etc/kubernetes/pki/ca.pem`。</span>
<span class="c"># 3. `-ca-key=/etc/kubernetes/pki/ca-key.pem`：指定根证书私钥文件的路径。在这里，是指定根证书私钥的路径为`/etc/kubernetes/pki/ca-key.pem`。</span>
<span class="c"># 4. `-config=ca-config.json`：指定证书配置文件的路径。在这里，是指定证书配置文件的路径为`ca-config.json`。</span>
<span class="c"># 5. `-profile=kubernetes`：指定证书的配置文件中的一个配置文件模板。在这里，是指定配置文件中的`kubernetes`配置模板。</span>
<span class="c"># 6. `admin-csr.json`：指定admin的证书签名请求文件（CSR）的路径。在这里，是指定请求文件的路径为`admin-csr.json`。</span>
<span class="c"># 7. `|`（管道符号）：将前一个命令的输出作为下一个命令的输入。</span>
<span class="c"># 8. `cfssljson`：将cfssl工具生成的证书签名请求(CSR)进行解析。</span>
<span class="c"># 9. `-bare /etc/kubernetes/pki/admin`：指定输出路径和前缀。在这里，是将解析的证书签名请求生成以下文件：`/etc/kubernetes/pki/admin.pem`（包含了证书）、`/etc/kubernetes/pki/admin-key.pem`（包含了私钥）。</span>
<span class="c"># </span>
<span class="c"># 总结来说，这个命令的目的是根据根证书、根证书私钥、证书配置文件、CSR文件等生成Kubernetes Scheduler的证书和私钥文件。</span>

<span class="c"># 在《5.高可用配置》选择使用那种高可用方案</span>
<span class="c"># 若使用 haproxy、keepalived 那么为 `--server=https://192.168.1.36:9443`</span>
<span class="c"># 若使用 nginx方案，那么为 `--server=https://127.0.0.1:8443`</span>

kubectl config set-cluster kubernetes     <span class="se">\</span>
  <span class="nt">--certificate-authority</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem     <span class="se">\</span>
  <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span>     <span class="se">\</span>
  <span class="nt">--server</span><span class="o">=</span>https://127.0.0.1:8443     <span class="se">\</span>
  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/admin.kubeconfig
<span class="c"># 该命令用于配置一个名为"kubernetes"的集群，并将其应用到/etc/kubernetes/scheduler.kubeconfig文件中。</span>
<span class="c"># </span>
<span class="c"># 该命令的解释如下：</span>
<span class="c"># - `kubectl config set-cluster kubernetes`: 设置一个集群并命名为"kubernetes"。</span>
<span class="c"># - `--certificate-authority=/etc/kubernetes/pki/ca.pem`: 指定集群使用的证书授权机构的路径。</span>
<span class="c"># - `--embed-certs=true`: 该标志指示将证书嵌入到生成的kubeconfig文件中。</span>
<span class="c"># - `--server=https://127.0.0.1:8443`: 指定集群的 API server 位置。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/admin.kubeconfig`: 指定要保存 kubeconfig 文件的路径和名称。</span>

kubectl config set-credentials kubernetes-admin  <span class="se">\</span>
  <span class="nt">--client-certificate</span><span class="o">=</span>/etc/kubernetes/pki/admin.pem     <span class="se">\</span>
  <span class="nt">--client-key</span><span class="o">=</span>/etc/kubernetes/pki/admin-key.pem     <span class="se">\</span>
  <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span>     <span class="se">\</span>
  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/admin.kubeconfig
<span class="c"># 这段命令是用于设置 kubernetes-admin 组件的身份验证凭据，并生成相应的 kubeconfig 文件。</span>
<span class="c"># </span>
<span class="c"># 解释每个选项的含义如下：</span>
<span class="c"># - `kubectl config set-credentials kubernetes-admin`：设置 `kubernetes-admin` 用户的身份验证凭据。</span>
<span class="c"># - `--client-certificate=/etc/kubernetes/pki/admin.pem`：指定一个客户端证书文件，用于基于证书的身份验证。在这种情况下，指定了 admin 组件的证书文件路径。</span>
<span class="c"># - `--client-key=/etc/kubernetes/pki/admin-key.pem`：指定与客户端证书相对应的客户端私钥文件。</span>
<span class="c"># - `--embed-certs=true`：将客户端证书和私钥嵌入到生成的 kubeconfig 文件中。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/admin.kubeconfig`：指定生成的 kubeconfig 文件的路径和名称。</span>
<span class="c"># </span>
<span class="c"># 该命令的目的是为 admin 组件生成一个 kubeconfig 文件，以便进行身份验证和访问集群资源。kubeconfig 文件是一个包含了连接到 Kubernetes 集群所需的所有配置信息的文件，包括服务器地址、证书和秘钥等。</span>


kubectl config set-context kubernetes-admin@kubernetes    <span class="se">\</span>
  <span class="nt">--cluster</span><span class="o">=</span>kubernetes     <span class="se">\</span>
  <span class="nt">--user</span><span class="o">=</span>kubernetes-admin     <span class="se">\</span>
  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/admin.kubeconfig

<span class="c"># 该命令用于设置一个名为"kubernetes-admin@kubernetes"的上下文，具体配置如下：</span>
<span class="c"># </span>
<span class="c"># 1. --cluster=kubernetes: 指定集群的名称为"kubernetes"，这个集群是在当前的kubeconfig文件中已经定义好的。</span>
<span class="c"># 2. --user=kubernetes-admin: 指定用户的名称为"kubernetes-admin"，这个用户也是在当前的kubeconfig文件中已经定义好的。这个用户用于认证和授权admin组件访问Kubernetes集群的权限。</span>
<span class="c"># 3. --kubeconfig=/etc/kubernetes/admin.kubeconfig: 指定kubeconfig文件的路径为"/etc/kubernetes/admin.kubeconfig"，这个文件将被用来保存上下文的配置信息。</span>
<span class="c"># </span>
<span class="c"># 这个命令的作用是将上述的配置信息保存到指定的kubeconfig文件中，以便后续使用该文件进行认证和授权访问Kubernetes集群。</span>


kubectl config use-context kubernetes-admin@kubernetes  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/admin.kubeconfig
<span class="c"># 上述命令是使用`kubectl`命令来配置Kubernetes集群中的调度器组件。</span>
<span class="c"># </span>
<span class="c"># `kubectl config use-context`命令用于切换`kubectl`当前使用的上下文。上下文是Kubernetes集群、用户和命名空间的组合，用于确定`kubectl`的连接目标。下面解释这个命令的不同部分：</span>
<span class="c"># </span>
<span class="c"># - `kubernetes-admin@kubernetes`是一个上下文名称。它指定了使用`kubernetes-admin`用户和`kubernetes`命名空间的系统级别上下文。系统级别上下文用于操作Kubernetes核心组件。</span>
<span class="c"># </span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/admin.kubeconfig`用于指定Kubernetes配置文件的路径。Kubernetes配置文件包含连接到Kubernetes集群所需的身份验证和连接信息。</span>
<span class="c"># </span>
<span class="c"># 通过运行以上命令，`kubectl`将使用指定的上下文和配置文件，以便在以后的命令中能正确地与Kubernetes集群中的调度器组件进行交互。</span>
</code></pre></div></div>

<h3 id="327-创建kube-proxy证书">3.2.7 创建kube-proxy证书</h3>

<p>在《5.高可用配置》选择使用那种高可用方案
若使用 haproxy、keepalived 那么为 <code class="language-plaintext highlighter-rouge">--server=https://192.168.1.36:9443</code>
若使用 nginx方案，那么为 <code class="language-plaintext highlighter-rouge">--server=https://127.0.0.1:8443</code></p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> kube-proxy-csr.json  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
{
  "CN": "system:kube-proxy",
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "Beijing",
      "L": "Beijing",
      "O": "system:kube-proxy",
      "OU": "Kubernetes-manual"
    }
  ]
}
</span><span class="no">EOF
</span><span class="c"># 这段代码是一个JSON格式的配置文件，用于创建和配置一个名为"kube-proxy-csr"的Kubernetes凭证。</span>
<span class="c"># </span>
<span class="c"># 这个凭证包含以下字段：</span>
<span class="c"># </span>
<span class="c"># - "CN": "system:kube-proxy": 这是凭证的通用名称，表示这是一个管理员凭证。</span>
<span class="c"># - "key": 这是一个包含证书密钥相关信息的对象。</span>
<span class="c">#   - "algo": "rsa"：这是使用的加密算法类型，这里是RSA加密算法。</span>
<span class="c">#   - "size": 2048：这是密钥的大小，这里是2048位。</span>
<span class="c"># - "names": 这是一个包含证书名称信息的数组。</span>
<span class="c">#   - "C": "CN"：这是证书的国家/地区字段，这里是中国。</span>
<span class="c">#   - "ST": "Beijing"：这是证书的省/州字段，这里是北京。</span>
<span class="c">#   - "L": "Beijing"：这是证书的城市字段，这里是北京。</span>
<span class="c">#   - "O": "system:kube-proxy"：这是证书的组织字段，这里是system:kube-proxy。</span>
<span class="c">#   - "OU": "Kubernetes-manual"：这是证书的部门字段，这里是Kubernetes-manual。</span>
<span class="c"># </span>
<span class="c"># 通过这个配置文件创建的凭证将具有管理员权限，并且可以用于管理Kubernetes集群。</span>

cfssl gencert <span class="se">\</span>
   <span class="nt">-ca</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem <span class="se">\</span>
   <span class="nt">-ca-key</span><span class="o">=</span>/etc/kubernetes/pki/ca-key.pem <span class="se">\</span>
   <span class="nt">-config</span><span class="o">=</span>ca-config.json <span class="se">\</span>
   <span class="nt">-profile</span><span class="o">=</span>kubernetes <span class="se">\</span>
   kube-proxy-csr.json | cfssljson <span class="nt">-bare</span> /etc/kubernetes/pki/kube-proxy

<span class="c"># 上述命令是使用cfssl工具生成Kubernetes admin的证书。</span>
<span class="c"># </span>
<span class="c"># 具体解释如下：</span>
<span class="c"># </span>
<span class="c"># 1. `cfssl gencert`：使用cfssl工具生成证书。</span>
<span class="c"># 2. `-ca=/etc/kubernetes/pki/ca.pem`：指定根证书文件的路径。在这里，是指定根证书的路径为`/etc/kubernetes/pki/ca.pem`。</span>
<span class="c"># 3. `-ca-key=/etc/kubernetes/pki/ca-key.pem`：指定根证书私钥文件的路径。在这里，是指定根证书私钥的路径为`/etc/kubernetes/pki/ca-key.pem`。</span>
<span class="c"># 4. `-config=ca-config.json`：指定证书配置文件的路径。在这里，是指定证书配置文件的路径为`ca-config.json`。</span>
<span class="c"># 5. `-profile=kubernetes`：指定证书的配置文件中的一个配置文件模板。在这里，是指定配置文件中的`kubernetes`配置模板。</span>
<span class="c"># 6. `kube-proxy-csr.json`：指定admin的证书签名请求文件（CSR）的路径。在这里，是指定请求文件的路径为`kube-proxy-csr.json`。</span>
<span class="c"># 7. `|`（管道符号）：将前一个命令的输出作为下一个命令的输入。</span>
<span class="c"># 8. `cfssljson`：将cfssl工具生成的证书签名请求(CSR)进行解析。</span>
<span class="c"># 9. `-bare /etc/kubernetes/pki/kube-proxy`：指定输出路径和前缀。在这里，是将解析的证书签名请求生成以下文件：`/etc/kubernetes/pki/kube-proxy.pem`（包含了证书）、`/etc/kubernetes/pki/kube-proxy-key.pem`（包含了私钥）。</span>
<span class="c"># </span>
<span class="c"># 总结来说，这个命令的目的是根据根证书、根证书私钥、证书配置文件、CSR文件等生成Kubernetes Scheduler的证书和私钥文件。</span>


<span class="c"># 在《5.高可用配置》选择使用那种高可用方案</span>
<span class="c"># 若使用 haproxy、keepalived 那么为 `--server=https://192.168.1.36:9443`</span>
<span class="c"># 若使用 nginx方案，那么为 `--server=https://127.0.0.1:8443`</span>

kubectl config set-cluster kubernetes     <span class="se">\</span>
  <span class="nt">--certificate-authority</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem     <span class="se">\</span>
  <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span>     <span class="se">\</span>
  <span class="nt">--server</span><span class="o">=</span>https://127.0.0.1:8443     <span class="se">\</span>
  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/kube-proxy.kubeconfig
<span class="c"># 该命令用于配置一个名为"kubernetes"的集群，并将其应用到/etc/kubernetes/kube-proxy.kubeconfig文件中。</span>
<span class="c"># </span>
<span class="c"># 该命令的解释如下：</span>
<span class="c"># - `kubectl config set-cluster kubernetes`: 设置一个集群并命名为"kubernetes"。</span>
<span class="c"># - `--certificate-authority=/etc/kubernetes/pki/ca.pem`: 指定集群使用的证书授权机构的路径。</span>
<span class="c"># - `--embed-certs=true`: 该标志指示将证书嵌入到生成的kubeconfig文件中。</span>
<span class="c"># - `--server=https://127.0.0.1:8443`: 指定集群的 API server 位置。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/kube-proxy.kubeconfig`: 指定要保存 kubeconfig 文件的路径和名称。</span>

kubectl config set-credentials kube-proxy  <span class="se">\</span>
  <span class="nt">--client-certificate</span><span class="o">=</span>/etc/kubernetes/pki/kube-proxy.pem     <span class="se">\</span>
  <span class="nt">--client-key</span><span class="o">=</span>/etc/kubernetes/pki/kube-proxy-key.pem     <span class="se">\</span>
  <span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span>     <span class="se">\</span>
  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/kube-proxy.kubeconfig
<span class="c"># 这段命令是用于设置 kube-proxy 组件的身份验证凭据，并生成相应的 kubeconfig 文件。</span>
<span class="c"># </span>
<span class="c"># 解释每个选项的含义如下：</span>
<span class="c"># - `kubectl config set-credentials kube-proxy`：设置 `kube-proxy` 用户的身份验证凭据。</span>
<span class="c"># - `--client-certificate=/etc/kubernetes/pki/kube-proxy.pem`：指定一个客户端证书文件，用于基于证书的身份验证。在这种情况下，指定了 kube-proxy 组件的证书文件路径。</span>
<span class="c"># - `--client-key=/etc/kubernetes/pki/kube-proxy-key.pem`：指定与客户端证书相对应的客户端私钥文件。</span>
<span class="c"># - `--embed-certs=true`：将客户端证书和私钥嵌入到生成的 kubeconfig 文件中。</span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/kube-proxy.kubeconfig`：指定生成的 kubeconfig 文件的路径和名称。</span>
<span class="c"># </span>
<span class="c"># 该命令的目的是为 kube-proxy 组件生成一个 kubeconfig 文件，以便进行身份验证和访问集群资源。kubeconfig 文件是一个包含了连接到 Kubernetes 集群所需的所有配置信息的文件，包括服务器地址、证书和秘钥等。</span>

kubectl config set-context kube-proxy@kubernetes    <span class="se">\</span>
  <span class="nt">--cluster</span><span class="o">=</span>kubernetes     <span class="se">\</span>
  <span class="nt">--user</span><span class="o">=</span>kube-proxy     <span class="se">\</span>
  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/kube-proxy.kubeconfig

<span class="c"># 该命令用于设置一个名为"kube-proxy@kubernetes"的上下文，具体配置如下：</span>
<span class="c"># </span>
<span class="c"># 1. --cluster=kubernetes: 指定集群的名称为"kubernetes"，这个集群是在当前的kubeconfig文件中已经定义好的。</span>
<span class="c"># 2. --user=kube-proxy: 指定用户的名称为"kube-proxy"，这个用户也是在当前的kubeconfig文件中已经定义好的。这个用户用于认证和授权kube-proxy组件访问Kubernetes集群的权限。</span>
<span class="c"># 3. --kubeconfig=/etc/kubernetes/kube-proxy.kubeconfig: 指定kubeconfig文件的路径为"/etc/kubernetes/kube-proxy.kubeconfig"，这个文件将被用来保存上下文的配置信息。</span>
<span class="c"># </span>
<span class="c"># 这个命令的作用是将上述的配置信息保存到指定的kubeconfig文件中，以便后续使用该文件进行认证和授权访问Kubernetes集群。</span>

kubectl config use-context kube-proxy@kubernetes  <span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/kube-proxy.kubeconfig
<span class="c"># 上述命令是使用`kubectl`命令来配置Kubernetes集群中的调度器组件。</span>
<span class="c"># </span>
<span class="c"># `kubectl config use-context`命令用于切换`kubectl`当前使用的上下文。上下文是Kubernetes集群、用户和命名空间的组合，用于确定`kubectl`的连接目标。下面解释这个命令的不同部分：</span>
<span class="c"># </span>
<span class="c"># - `kube-proxy@kubernetes`是一个上下文名称。它指定了使用`kube-proxy`用户和`kubernetes`命名空间的系统级别上下文。系统级别上下文用于操作Kubernetes核心组件。</span>
<span class="c"># </span>
<span class="c"># - `--kubeconfig=/etc/kubernetes/kube-proxy.kubeconfig`用于指定Kubernetes配置文件的路径。Kubernetes配置文件包含连接到Kubernetes集群所需的身份验证和连接信息。</span>
<span class="c"># </span>
<span class="c"># 通过运行以上命令，`kubectl`将使用指定的上下文和配置文件，以便在以后的命令中能正确地与Kubernetes集群中的调度器组件进行交互。</span>
</code></pre></div></div>

<h3 id="328-创建serviceaccount-key-secret">3.2.8 创建ServiceAccount Key ——secret</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>openssl genrsa <span class="nt">-out</span> /etc/kubernetes/pki/sa.key 2048
openssl rsa <span class="nt">-in</span> /etc/kubernetes/pki/sa.key <span class="nt">-pubout</span> <span class="nt">-out</span> /etc/kubernetes/pki/sa.pub

<span class="c"># 这两个命令是使用OpenSSL工具生成RSA密钥对。</span>
<span class="c"># </span>
<span class="c"># 命令1：openssl genrsa -out /etc/kubernetes/pki/sa.key 2048</span>
<span class="c"># 该命令用于生成私钥文件。具体解释如下：</span>
<span class="c"># - openssl：openssl命令行工具。</span>
<span class="c"># - genrsa：生成RSA密钥对。</span>
<span class="c"># - -out /etc/kubernetes/pki/sa.key：指定输出私钥文件的路径和文件名。</span>
<span class="c"># - 2048：指定密钥长度为2048位。</span>
<span class="c"># </span>
<span class="c"># 命令2：openssl rsa -in /etc/kubernetes/pki/sa.key -pubout -out /etc/kubernetes/pki/sa.pub</span>
<span class="c"># 该命令用于从私钥中导出公钥。具体解释如下：</span>
<span class="c"># - openssl：openssl命令行工具。</span>
<span class="c"># - rsa：与私钥相关的RSA操作。</span>
<span class="c"># - -in /etc/kubernetes/pki/sa.key：指定输入私钥文件的路径和文件名。</span>
<span class="c"># - -pubout：指定输出公钥。</span>
<span class="c"># - -out /etc/kubernetes/pki/sa.pub：指定输出公钥文件的路径和文件名。</span>
<span class="c"># </span>
<span class="c"># 总结：通过以上两个命令，我们可以使用OpenSSL工具生成一个RSA密钥对，并将私钥保存在/etc/kubernetes/pki/sa.key文件中，将公钥保存在/etc/kubernetes/pki/sa.pub文件中。</span>
</code></pre></div></div>

<h3 id="329-将证书发送到其他master节点">3.2.9 将证书发送到其他master节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#其他节点创建目录</span>
<span class="c"># mkdir  /etc/kubernetes/pki/ -p</span>

<span class="k">for </span>NODE <span class="k">in </span>k8s-master02 k8s-master03<span class="p">;</span> <span class="k">do  for </span>FILE <span class="k">in</span> <span class="si">$(</span><span class="nb">ls</span> /etc/kubernetes/pki | <span class="nb">grep</span> <span class="nt">-v</span> etcd<span class="si">)</span><span class="p">;</span> <span class="k">do  </span>scp /etc/kubernetes/pki/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span> <span class="nv">$NODE</span>:/etc/kubernetes/pki/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span><span class="p">;</span> <span class="k">done</span><span class="p">;</span>  <span class="k">for </span>FILE <span class="k">in </span>admin.kubeconfig controller-manager.kubeconfig scheduler.kubeconfig<span class="p">;</span> <span class="k">do  </span>scp /etc/kubernetes/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span> <span class="nv">$NODE</span>:/etc/kubernetes/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span><span class="p">;</span> <span class="k">done</span><span class="p">;</span> <span class="k">done</span>
</code></pre></div></div>

<h3 id="3210-查看证书">3.2.10 查看证书</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@localhost cby]# ll /etc/kubernetes/pki/
总用量 104
<span class="nt">-rw-r--r--</span> 1 root root 1025 12月 19 20:22 admin.csr
<span class="nt">-rw-------</span> 1 root root 1675 12月 19 20:22 admin-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1444 12月 19 20:22 admin.pem
<span class="nt">-rw-r--r--</span> 1 root root 1415 12月 19 20:21 apiserver.csr
<span class="nt">-rw-------</span> 1 root root 1679 12月 19 20:21 apiserver-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1805 12月 19 20:21 apiserver.pem
<span class="nt">-rw-r--r--</span> 1 root root 1070 12月 19 20:21 ca.csr
<span class="nt">-rw-------</span> 1 root root 1679 12月 19 20:21 ca-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1363 12月 19 20:21 ca.pem
<span class="nt">-rw-r--r--</span> 1 root root 1082 12月 19 20:21 controller-manager.csr
<span class="nt">-rw-------</span> 1 root root 1675 12月 19 20:21 controller-manager-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1501 12月 19 20:21 controller-manager.pem
<span class="nt">-rw-r--r--</span> 1 root root  940 12月 19 20:21 front-proxy-ca.csr
<span class="nt">-rw-------</span> 1 root root 1679 12月 19 20:21 front-proxy-ca-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1094 12月 19 20:21 front-proxy-ca.pem
<span class="nt">-rw-r--r--</span> 1 root root  903 12月 19 20:21 front-proxy-client.csr
<span class="nt">-rw-------</span> 1 root root 1679 12月 19 20:21 front-proxy-client-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1188 12月 19 20:21 front-proxy-client.pem
<span class="nt">-rw-r--r--</span> 1 root root 1045 12月 19 20:22 kube-proxy.csr
<span class="nt">-rw-------</span> 1 root root 1679 12月 19 20:22 kube-proxy-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1464 12月 19 20:22 kube-proxy.pem
<span class="nt">-rw-------</span> 1 root root 1704 12月 19 20:22 sa.key
<span class="nt">-rw-r--r--</span> 1 root root  451 12月 19 20:22 sa.pub
<span class="nt">-rw-r--r--</span> 1 root root 1058 12月 19 20:22 scheduler.csr
<span class="nt">-rw-------</span> 1 root root 1679 12月 19 20:22 scheduler-key.pem
<span class="nt">-rw-r--r--</span> 1 root root 1476 12月 19 20:22 scheduler.pem
<span class="o">[</span>root@localhost cby]# 



<span class="c"># 一共26个就对了</span>
<span class="nb">ls</span> /etc/kubernetes/pki/ |wc <span class="nt">-l</span>
26
</code></pre></div></div>

<h1 id="4k8s系统组件配置">4.k8s系统组件配置</h1>

<h2 id="41etcd配置">4.1.etcd配置</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>这个配置文件是用于 etcd 集群的配置，其中包含了一些重要的参数和选项：

- <span class="sb">`</span>name<span class="sb">`</span>：指定了当前节点的名称，用于集群中区分不同的节点。
- <span class="sb">`</span>data-dir<span class="sb">`</span>：指定了 etcd 数据的存储目录。
- <span class="sb">`</span>wal-dir<span class="sb">`</span>：指定了 etcd 数据写入磁盘的目录。
- <span class="sb">`</span>snapshot-count<span class="sb">`</span>：指定了触发快照的事务数量。
- <span class="sb">`</span>heartbeat-interval<span class="sb">`</span>：指定了 etcd 集群中节点之间的心跳间隔。
- <span class="sb">`</span>election-timeout<span class="sb">`</span>：指定了选举超时时间。
- <span class="sb">`</span>quota-backend-bytes<span class="sb">`</span>：指定了存储的限额，0 表示无限制。
- <span class="sb">`</span>listen-peer-urls<span class="sb">`</span>：指定了节点之间通信的 URL，使用 HTTPS 协议。
- <span class="sb">`</span>listen-client-urls<span class="sb">`</span>：指定了客户端访问 etcd 集群的 URL，同时提供了本地访问的 URL。
- <span class="sb">`</span>max-snapshots<span class="sb">`</span>：指定了快照保留的数量。
- <span class="sb">`</span>max-wals<span class="sb">`</span>：指定了日志保留的数量。
- <span class="sb">`</span>initial-advertise-peer-urls<span class="sb">`</span>：指定了节点之间通信的初始 URL。
- <span class="sb">`</span>advertise-client-urls<span class="sb">`</span>：指定了客户端访问 etcd 集群的初始 URL。
- <span class="sb">`</span>discovery<span class="sb">`</span>：定义了 etcd 集群发现相关的选项。
- <span class="sb">`</span>initial-cluster<span class="sb">`</span>：指定了 etcd 集群的初始成员。
- <span class="sb">`</span>initial-cluster-token<span class="sb">`</span>：指定了集群的 token。
- <span class="sb">`</span>initial-cluster-state<span class="sb">`</span>：指定了集群的初始状态。
- <span class="sb">`</span>strict-reconfig-check<span class="sb">`</span>：指定了严格的重新配置检查选项。
- <span class="sb">`</span>enable-v2<span class="sb">`</span>：启用了 v2 API。
- <span class="sb">`</span>enable-pprof<span class="sb">`</span>：启用了性能分析。
- <span class="sb">`</span>proxy<span class="sb">`</span>：设置了代理模式。
- <span class="sb">`</span>client-transport-security<span class="sb">`</span>：客户端的传输安全配置。
- <span class="sb">`</span>peer-transport-security<span class="sb">`</span>：节点之间的传输安全配置。
- <span class="sb">`</span>debug<span class="sb">`</span>：是否启用调试模式。
- <span class="sb">`</span>log-package-levels<span class="sb">`</span>：日志的输出级别。
- <span class="sb">`</span>log-outputs<span class="sb">`</span>：指定了日志的输出类型。
- <span class="sb">`</span>force-new-cluster<span class="sb">`</span>：是否强制创建一个新的集群。

这些参数和选项可以根据实际需求进行调整和配置。
</code></pre></div></div>

<h3 id="411master01配置">4.1.1master01配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /etc/etcd/etcd.config.yml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
name: 'k8s-master01'
data-dir: /var/lib/etcd
wal-dir: /var/lib/etcd/wal
snapshot-count: 5000
heartbeat-interval: 100
election-timeout: 1000
quota-backend-bytes: 0
listen-peer-urls: 'https://192.168.1.31:2380'
listen-client-urls: 'https://192.168.1.31:2379,http://127.0.0.1:2379'
max-snapshots: 3
max-wals: 5
cors:
initial-advertise-peer-urls: 'https://192.168.1.31:2380'
advertise-client-urls: 'https://192.168.1.31:2379'
discovery:
discovery-fallback: 'proxy'
discovery-proxy:
discovery-srv:
initial-cluster: 'k8s-master01=https://192.168.1.31:2380,k8s-master02=https://192.168.1.32:2380,k8s-master03=https://192.168.1.33:2380'
initial-cluster-token: 'etcd-k8s-cluster'
initial-cluster-state: 'new'
strict-reconfig-check: false
enable-v2: true
enable-pprof: true
proxy: 'off'
proxy-failure-wait: 5000
proxy-refresh-interval: 30000
proxy-dial-timeout: 1000
proxy-write-timeout: 5000
proxy-read-timeout: 0
client-transport-security:
  cert-file: '/etc/kubernetes/pki/etcd/etcd.pem'
  key-file: '/etc/kubernetes/pki/etcd/etcd-key.pem'
  client-cert-auth: true
  trusted-ca-file: '/etc/kubernetes/pki/etcd/etcd-ca.pem'
  auto-tls: true
peer-transport-security:
  cert-file: '/etc/kubernetes/pki/etcd/etcd.pem'
  key-file: '/etc/kubernetes/pki/etcd/etcd-key.pem'
  peer-client-cert-auth: true
  trusted-ca-file: '/etc/kubernetes/pki/etcd/etcd-ca.pem'
  auto-tls: true
debug: false
log-package-levels:
log-outputs: [default]
force-new-cluster: false
</span><span class="no">EOF
</span></code></pre></div></div>

<h3 id="412master02配置">4.1.2master02配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /etc/etcd/etcd.config.yml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
name: 'k8s-master02'
data-dir: /var/lib/etcd
wal-dir: /var/lib/etcd/wal
snapshot-count: 5000
heartbeat-interval: 100
election-timeout: 1000
quota-backend-bytes: 0
listen-peer-urls: 'https://192.168.1.32:2380'
listen-client-urls: 'https://192.168.1.32:2379,http://127.0.0.1:2379'
max-snapshots: 3
max-wals: 5
cors:
initial-advertise-peer-urls: 'https://192.168.1.32:2380'
advertise-client-urls: 'https://192.168.1.32:2379'
discovery:
discovery-fallback: 'proxy'
discovery-proxy:
discovery-srv:
initial-cluster: 'k8s-master01=https://192.168.1.31:2380,k8s-master02=https://192.168.1.32:2380,k8s-master03=https://192.168.1.33:2380'
initial-cluster-token: 'etcd-k8s-cluster'
initial-cluster-state: 'new'
strict-reconfig-check: false
enable-v2: true
enable-pprof: true
proxy: 'off'
proxy-failure-wait: 5000
proxy-refresh-interval: 30000
proxy-dial-timeout: 1000
proxy-write-timeout: 5000
proxy-read-timeout: 0
client-transport-security:
  cert-file: '/etc/kubernetes/pki/etcd/etcd.pem'
  key-file: '/etc/kubernetes/pki/etcd/etcd-key.pem'
  client-cert-auth: true
  trusted-ca-file: '/etc/kubernetes/pki/etcd/etcd-ca.pem'
  auto-tls: true
peer-transport-security:
  cert-file: '/etc/kubernetes/pki/etcd/etcd.pem'
  key-file: '/etc/kubernetes/pki/etcd/etcd-key.pem'
  peer-client-cert-auth: true
  trusted-ca-file: '/etc/kubernetes/pki/etcd/etcd-ca.pem'
  auto-tls: true
debug: false
log-package-levels:
log-outputs: [default]
force-new-cluster: false
</span><span class="no">EOF
</span></code></pre></div></div>

<h3 id="413master03配置">4.1.3master03配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /etc/etcd/etcd.config.yml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh"> 
name: 'k8s-master03'
data-dir: /var/lib/etcd
wal-dir: /var/lib/etcd/wal
snapshot-count: 5000
heartbeat-interval: 100
election-timeout: 1000
quota-backend-bytes: 0
listen-peer-urls: 'https://192.168.1.33:2380'
listen-client-urls: 'https://192.168.1.33:2379,http://127.0.0.1:2379'
max-snapshots: 3
max-wals: 5
cors:
initial-advertise-peer-urls: 'https://192.168.1.33:2380'
advertise-client-urls: 'https://192.168.1.33:2379'
discovery:
discovery-fallback: 'proxy'
discovery-proxy:
discovery-srv:
initial-cluster: 'k8s-master01=https://192.168.1.31:2380,k8s-master02=https://192.168.1.32:2380,k8s-master03=https://192.168.1.33:2380'
initial-cluster-token: 'etcd-k8s-cluster'
initial-cluster-state: 'new'
strict-reconfig-check: false
enable-v2: true
enable-pprof: true
proxy: 'off'
proxy-failure-wait: 5000
proxy-refresh-interval: 30000
proxy-dial-timeout: 1000
proxy-write-timeout: 5000
proxy-read-timeout: 0
client-transport-security:
  cert-file: '/etc/kubernetes/pki/etcd/etcd.pem'
  key-file: '/etc/kubernetes/pki/etcd/etcd-key.pem'
  client-cert-auth: true
  trusted-ca-file: '/etc/kubernetes/pki/etcd/etcd-ca.pem'
  auto-tls: true
peer-transport-security:
  cert-file: '/etc/kubernetes/pki/etcd/etcd.pem'
  key-file: '/etc/kubernetes/pki/etcd/etcd-key.pem'
  peer-client-cert-auth: true
  trusted-ca-file: '/etc/kubernetes/pki/etcd/etcd-ca.pem'
  auto-tls: true
debug: false
log-package-levels:
log-outputs: [default]
force-new-cluster: false
</span><span class="no">EOF
</span></code></pre></div></div>

<h2 id="42创建service所有master节点操作">4.2.创建service（所有master节点操作）</h2>

<h3 id="421创建etcdservice并启动">4.2.1创建etcd.service并启动</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/etcd.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">

[Unit]
Description=Etcd Service
Documentation=https://coreos.com/etcd/docs/latest/
After=network.target

[Service]
Type=notify
ExecStart=/usr/local/bin/etcd --config-file=/etc/etcd/etcd.config.yml
TimeoutSec=0
RestartSec=60
Restart=always
StartLimitBurst=3
StartLimitInterval=60s
LimitNOFILE=infinity
LimitNPROC=infinity
LimitCORE=infinity
TasksMax=infinity

[Install]
WantedBy=multi-user.target
Alias=etcd3.service
</span><span class="no">
EOF
</span><span class="c"># 这是一个系统服务配置文件，用于启动和管理Etcd服务。</span>
<span class="c"># </span>
<span class="c"># [Unit] 部分包含了服务的一些基本信息，它定义了服务的描述和文档链接，并指定了服务应在网络连接之后启动。</span>
<span class="c"># </span>
<span class="c"># [Service] 部分定义了服务的具体配置。在这里，服务的类型被设置为notify，意味着当服务成功启动时，它将通知系统。ExecStart 指定了启动服务时要执行的命令，这里是运行 /usr/local/bin/etcd 命令并传递一个配置文件 /etc/etcd/etcd.config.yml。Restart 设置为 on-failure，意味着当服务失败时将自动重启，并且在10秒后进行重启。LimitNOFILE 指定了服务的最大文件打开数。</span>
<span class="c"># </span>
<span class="c"># [Install] 部分定义了服务的安装配置。WantedBy 指定了服务应该被启动的目标，这里是 multi-user.target，表示在系统进入多用户模式时启动。Alias 定义了一个别名，可以通过etcd3.service来引用这个服务。</span>
<span class="c"># </span>
<span class="c"># 这个配置文件描述了如何启动和管理Etcd服务，并将其安装到系统中。通过这个配置文件，可以确保Etcd服务在系统启动后自动启动，并在出现问题时进行重启。</span>
</code></pre></div></div>

<h3 id="422创建etcd证书目录">4.2.2创建etcd证书目录</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> /etc/kubernetes/pki/etcd
<span class="nb">ln</span> <span class="nt">-s</span> /etc/etcd/ssl/<span class="k">*</span> /etc/kubernetes/pki/etcd/

systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> etcd.service
<span class="c"># 启用并立即启动etcd.service单元。etcd.service是etcd守护进程的systemd服务单元。</span>

systemctl restart etcd.service
<span class="c"># 重启etcd.service单元，即重新启动etcd守护进程。</span>

systemctl status etcd.service
<span class="c"># etcd.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h3 id="423查看etcd状态">4.2.3查看etcd状态</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 如果要用IPv6那么把IPv4地址修改为IPv6即可</span>
<span class="nb">export </span><span class="nv">ETCDCTL_API</span><span class="o">=</span>3
etcdctl <span class="nt">--endpoints</span><span class="o">=</span><span class="s2">"192.168.1.33:2379,192.168.1.32:2379,192.168.1.31:2379"</span> <span class="nt">--cacert</span><span class="o">=</span>/etc/kubernetes/pki/etcd/etcd-ca.pem <span class="nt">--cert</span><span class="o">=</span>/etc/kubernetes/pki/etcd/etcd.pem <span class="nt">--key</span><span class="o">=</span>/etc/kubernetes/pki/etcd/etcd-key.pem  endpoint status <span class="nt">--write-out</span><span class="o">=</span>table
+-------------------+------------------+---------+-----------------+---------+--------+-----------------------+--------+-----------+------------+-----------+------------+--------------------+--------+--------------------------+-------------------+
|     ENDPOINT      |        ID        | VERSION | STORAGE VERSION | DB SIZE | IN USE | PERCENTAGE NOT IN USE | QUOTA  | IS LEADER | IS LEARNER | RAFT TERM | RAFT INDEX | RAFT APPLIED INDEX | ERRORS | DOWNGRADE TARGET VERSION | DOWNGRADE ENABLED |
+-------------------+------------------+---------+-----------------+---------+--------+-----------------------+--------+-----------+------------+-----------+------------+--------------------+--------+--------------------------+-------------------+
| 192.168.1.33:2379 |  8065f2e59c8d68c |   3.6.7 |           3.6.0 |   20 kB |  16 kB |                   20% | 2.1 GB |     <span class="nb">false</span> |      <span class="nb">false</span> |         4 |         14 |                 14 |        |                          |             <span class="nb">false</span> |
| 192.168.1.32:2379 | b7b7ad6bf4db3f28 |   3.6.7 |           3.6.0 |   20 kB |  16 kB |                   20% | 2.1 GB |      <span class="nb">true</span> |      <span class="nb">false</span> |         4 |         14 |                 14 |        |                          |             <span class="nb">false</span> |
| 192.168.1.31:2379 | bf047bcfe3b9bf27 |   3.6.7 |           3.6.0 |   20 kB |  16 kB |                   20% | 2.1 GB |     <span class="nb">false</span> |      <span class="nb">false</span> |         4 |         14 |                 14 |        |                          |             <span class="nb">false</span> |
+-------------------+------------------+---------+-----------------+---------+--------+-----------------------+--------+-----------+------------+-----------+------------+--------------------+--------+--------------------------+-------------------+



<span class="c"># 这个命令是使用etcdctl工具，用于查看指定etcd集群的健康状态。下面是每个参数的详细解释：</span>
<span class="c"># </span>
<span class="c"># - `--endpoints`：指定要连接的etcd集群节点的地址和端口。在这个例子中，指定了3个节点的地址和端口，分别是`192.168.1.33:2379,192.168.1.32:2379,192.168.1.31:2379`。</span>
<span class="c"># - `--cacert`：指定用于验证etcd服务器证书的CA证书的路径。在这个例子中，指定了CA证书的路径为`/etc/kubernetes/pki/etcd/etcd-ca.pem`。CA证书用于验证etcd服务器证书的有效性。</span>
<span class="c"># - `--cert`：指定用于与etcd服务器进行通信的客户端证书的路径。在这个例子中，指定了客户端证书的路径为`/etc/kubernetes/pki/etcd/etcd.pem`。客户端证书用于在与etcd服务器建立安全通信时进行身份验证。</span>
<span class="c"># - `--key`：指定与客户端证书配对的私钥的路径。在这个例子中，指定了私钥的路径为`/etc/kubernetes/pki/etcd/etcd-key.pem`。私钥用于对通信进行加密解密和签名验证。</span>
<span class="c"># - `endpoint status`：子命令，用于检查etcd集群节点的健康状态。</span>
<span class="c"># - `--write-out`：指定输出的格式。在这个例子中，指定以表格形式输出。</span>
<span class="c"># </span>
<span class="c"># 通过执行这个命令，可以获取到etcd集群节点的健康状态，并以表格形式展示。</span>
</code></pre></div></div>

<h1 id="5高可用配置在master服务器上操作">5.高可用配置（在Master服务器上操作）</h1>

<p><strong>注意* 5.1.1 和5.1.2 二选一即可</strong></p>

<p>选择使用那种高可用方案，同时可以俩种都选用，实现内外兼顾的效果，比如：
5.1 的 NGINX方案实现集群内的高可用
5.2 的 haproxy、keepalived 方案实现集群外访问</p>

<p>在《3.2.生成k8s相关证书》</p>

<p>若使用 nginx方案，那么为 <code class="language-plaintext highlighter-rouge">--server=https://127.0.0.1:8443</code>
若使用 haproxy、keepalived 那么为 <code class="language-plaintext highlighter-rouge">--server=https://192.168.1.36:9443</code></p>

<h2 id="51-nginx高可用方案">5.1 NGINX高可用方案</h2>

<h3 id="511-进行编译">5.1.1 进行编译</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 安装编译环境</span>
yum <span class="nb">install </span>gcc <span class="nt">-y</span>

<span class="c"># 下载解压nginx二进制文件</span>
<span class="c"># wget http://nginx.org/download/nginx-1.25.3.tar.gz</span>
<span class="nb">tar </span>xvf nginx-<span class="k">*</span>.tar.gz
<span class="nb">cd </span>nginx-<span class="k">*</span>

<span class="c"># 进行编译</span>
./configure <span class="nt">--with-stream</span> <span class="nt">--without-http</span> <span class="nt">--without-http_uwsgi_module</span> <span class="nt">--without-http_scgi_module</span> <span class="nt">--without-http_fastcgi_module</span>
make <span class="o">&amp;&amp;</span> make <span class="nb">install</span> 

<span class="c"># 拷贝编译好的nginx</span>
<span class="nv">node</span><span class="o">=</span><span class="s1">'k8s-master02 k8s-master03 k8s-node01 k8s-node02'</span>
<span class="k">for </span>NODE <span class="k">in</span> <span class="nv">$node</span><span class="p">;</span> <span class="k">do </span>scp <span class="nt">-r</span> /usr/local/nginx/ <span class="nv">$NODE</span>:/usr/local/nginx/<span class="p">;</span> <span class="k">done</span>

<span class="c"># 这是一系列命令行指令，用于编译和安装软件。</span>
<span class="c"># </span>
<span class="c"># 1. `./configure` 是用于配置软件的命令。在这个例子中，配置的软件是一个Web服务器，指定了一些选项来启用流模块，并禁用了HTTP、uwsgi、scgi和fastcgi模块。</span>
<span class="c"># 2. `--with-stream` 指定启用流模块。流模块通常用于代理TCP和UDP流量。</span>
<span class="c"># 3. `--without-http` 指定禁用HTTP模块。这意味着编译的软件将没有HTTP服务器功能。</span>
<span class="c"># 4. `--without-http_uwsgi_module` 指定禁用uwsgi模块。uwsgi是一种Web服务器和应用服务器之间的通信协议。</span>
<span class="c"># 5. `--without-http_scgi_module` 指定禁用scgi模块。scgi是一种用于将Web服务器请求传递到应用服务器的协议。</span>
<span class="c"># 6. `--without-http_fastcgi_module` 指定禁用fastcgi模块。fastcgi是一种用于在Web服务器和应用服务器之间交换数据的协议。</span>
<span class="c"># 7. `make` 是用于编译软件的命令。该命令将根据之前的配置生成可执行文件。</span>
<span class="c"># 8. `make install` 用于安装软件。该命令将生成的可执行文件和其他必要文件复制到系统的适当位置，以便可以使用该软件。</span>
<span class="c"># </span>
<span class="c"># 总之，这个命令序列用于编译一个配置了特定选项的Web服务器，并将其安装到系统中。</span>
</code></pre></div></div>

<h3 id="512-写入启动配置">5.1.2 写入启动配置</h3>

<p>在所有主机上执行</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 写入nginx配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/local/nginx/conf/kube-nginx.conf <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
worker_processes 1;
events {
    worker_connections  1024;
}
stream {
    upstream backend {
        least_conn;
        hash </span><span class="nv">$remote_addr</span><span class="sh"> consistent;
        server 192.168.1.31:6443        max_fails=3 fail_timeout=30s;
        server 192.168.1.32:6443        max_fails=3 fail_timeout=30s;
        server 192.168.1.33:6443        max_fails=3 fail_timeout=30s;
    }
    server {
        listen 127.0.0.1:8443;
        proxy_connect_timeout 1s;
        proxy_pass backend;
    }
}
</span><span class="no">EOF
</span><span class="c"># 这段配置是一个nginx的stream模块的配置，用于代理TCP和UDP流量。</span>
<span class="c"># </span>
<span class="c"># 首先，`worker_processes 1;`表示启动一个worker进程用于处理流量。</span>
<span class="c"># 接下来，`events { worker_connections 1024; }`表示每个worker进程可以同时处理最多1024个连接。</span>
<span class="c"># 在stream块里面，定义了一个名为`backend`的upstream，用于负载均衡和故障转移。</span>
<span class="c"># `least_conn`表示使用最少连接算法进行负载均衡。</span>
<span class="c"># `hash $remote_addr consistent`表示用客户端的IP地址进行哈希分配请求，保持相同IP的请求始终访问同一台服务器。</span>
<span class="c"># `server`指令用于定义后端的服务器，每个服务器都有一个IP地址和端口号，以及一些可选的参数。</span>
<span class="c"># `max_fails=3`表示当一个服务器连续失败3次时将其标记为不可用。</span>
<span class="c"># `fail_timeout=30s`表示如果一个服务器被标记为不可用，nginx将在30秒后重新尝试。</span>
<span class="c"># 在server块内部，定义了一个监听地址为127.0.0.1:8443的服务器。</span>
<span class="c"># `proxy_connect_timeout 1s`表示与后端服务器建立连接的超时时间为1秒。</span>
<span class="c"># `proxy_pass backend`表示将流量代理到名为backend的上游服务器组。</span>
<span class="c"># </span>
<span class="c"># 总结起来，这段配置将流量代理到一个包含3个后端服务器的上游服务器组中，使用最少连接算法进行负载均衡，并根据客户端的IP地址进行哈希分配请求。如果一个服务器连续失败3次，则将其标记为不可用，并在30秒后重新尝试。</span>


<span class="c"># 写入启动配置文件</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /etc/systemd/system/kube-nginx.service <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
[Unit]
Description=kube-apiserver nginx proxy
After=network.target
After=network-online.target
Wants=network-online.target

[Service]
Type=forking
ExecStartPre=/usr/local/nginx/sbin/nginx -c /usr/local/nginx/conf/kube-nginx.conf -p /usr/local/nginx -t
ExecStart=/usr/local/nginx/sbin/nginx -c /usr/local/nginx/conf/kube-nginx.conf -p /usr/local/nginx
ExecReload=/usr/local/nginx/sbin/nginx -c /usr/local/nginx/conf/kube-nginx.conf -p /usr/local/nginx -s reload
PrivateTmp=true
Restart=always
RestartSec=5
StartLimitInterval=0
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF
</span><span class="c"># 这是一个用于kube-apiserver的NGINX代理的systemd单位文件。</span>
<span class="c"># </span>
<span class="c"># [Unit]部分包含了单位的描述和依赖关系。它指定了在network.target和network-online.target之后启动，并且需要network-online.target。</span>
<span class="c"># </span>
<span class="c"># [Service]部分定义了如何运行该服务。Type指定了服务进程的类型（forking表示主进程会派生一个子进程）。ExecStartPre指定了在服务启动之前需要运行的命令，用于检查NGINX配置文件的语法是否正确。ExecStart指定了启动服务所需的命令。ExecReload指定了在重新加载配置文件时运行的命令。PrivateTmp设置为true表示将为服务创建一个私有的临时文件系统。Restart和RestartSec用于设置服务的自动重启机制。StartLimitInterval设置为0表示无需等待，可以立即重启服务。LimitNOFILE指定了服务的文件描述符的限制。</span>
<span class="c"># </span>
<span class="c"># [Install]部分指定了在哪些target下该单位应该被启用。</span>
<span class="c"># </span>
<span class="c"># 综上所述，此单位文件用于启动和管理kube-apiserver的NGINX代理服务。它通过NGINX来反向代理和负载均衡kube-apiserver的请求。该服务会在系统启动时自动启动，并具有自动重启的机制。</span>


<span class="c"># 设置开机自启</span>

systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>
systemctl <span class="nb">enable</span> <span class="nt">--now</span> kube-nginx.service
<span class="c"># 启用并立即启动kube-nginx.service单元。kube-nginx.service是kube-nginx守护进程的systemd服务单元。</span>
systemctl restart kube-nginx.service
<span class="c"># 重启kube-nginx.service单元，即重新启动kube-nginx守护进程。</span>
systemctl status kube-nginx.service
<span class="c"># kube-nginx.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h2 id="52-keepalived和haproxy-高可用方案">5.2 keepalived和haproxy 高可用方案</h2>

<h3 id="521安装keepalived和haproxy服务">5.2.1安装keepalived和haproxy服务</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl disable <span class="nt">--now</span> firewalld
setenforce 0
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s1">'s#SELINUX=enforcing#SELINUX=disabled#g'</span> /etc/selinux/config
yum <span class="nt">-y</span> <span class="nb">install </span>keepalived haproxy
</code></pre></div></div>

<h3 id="522修改haproxy配置文件配置文件一样">5.2.2修改haproxy配置文件（配置文件一样）</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp</span> /etc/haproxy/haproxy.cfg /etc/haproxy/haproxy.cfg.bak
<span class="nb">cat</span> <span class="o">&gt;</span>/etc/haproxy/haproxy.cfg<span class="o">&lt;&lt;</span><span class="sh">"</span><span class="no">EOF</span><span class="sh">"
global
 maxconn 2000
 ulimit-n 16384
 log 127.0.0.1 local0 err
 stats timeout 30s

defaults
 log global
 mode http
 option httplog
 timeout connect 5000
 timeout client 50000
 timeout server 50000
 timeout http-request 15s
 timeout http-keep-alive 15s


frontend monitor-in
 bind *:33305
 mode http
 option httplog
 monitor-uri /monitor

frontend k8s-master
 bind 0.0.0.0:9443
 bind 127.0.0.1:9443
 mode tcp
 option tcplog
 tcp-request inspect-delay 5s
 default_backend k8s-master


backend k8s-master
 mode tcp
 option tcplog
 option tcp-check
 balance roundrobin
 default-server inter 10s downinter 5s rise 2 fall 2 slowstart 60s maxconn 250 maxqueue 256 weight 100
 server  k8s-master01  192.168.1.31:6443 check
 server  k8s-master02  192.168.1.32:6443 check
 server  k8s-master03  192.168.1.33:6443 check
</span><span class="no">EOF
</span></code></pre></div></div>

<p>参数</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>这段配置代码是指定了一个HAProxy负载均衡器的配置。下面对各部分进行详细解释：
1. global:
   - maxconn 2000: 设置每个进程的最大连接数为2000。
   - ulimit-n 16384: 设置每个进程的最大文件描述符数为16384。
   - log 127.0.0.1 local0 err: 指定日志的输出地址为本地主机的127.0.0.1，并且只记录错误级别的日志。
   - stats <span class="nb">timeout </span>30s: 设置查看负载均衡器统计信息的超时时间为30秒。

2. defaults:
   - log global: 使默认日志与global部分相同。
   - mode http: 设定负载均衡器的工作模式为HTTP模式。
   - option httplog: 使负载均衡器记录HTTP协议的日志。
   - <span class="nb">timeout </span>connect 5000: 设置与后端服务器建立连接的超时时间为5秒。
   - <span class="nb">timeout </span>client 50000: 设置与客户端的连接超时时间为50秒。
   - <span class="nb">timeout </span>server 50000: 设置与后端服务器连接的超时时间为50秒。
   - <span class="nb">timeout </span>http-request 15s: 设置处理HTTP请求的超时时间为15秒。
   - <span class="nb">timeout </span>http-keep-alive 15s: 设置保持HTTP连接的超时时间为15秒。

3. frontend monitor-in:
   - <span class="nb">bind</span> <span class="k">*</span>:33305: 监听所有IP地址的33305端口。
   - mode http: 设定frontend的工作模式为HTTP模式。
   - option httplog: 记录HTTP协议的日志。
   - monitor-uri /monitor: 设置监控URI为/monitor。

4. frontend k8s-master:
   - <span class="nb">bind </span>0.0.0.0:9443: 监听所有IP地址的9443端口。
   - <span class="nb">bind </span>127.0.0.1:9443: 监听本地主机的9443端口。
   - mode tcp: 设定frontend的工作模式为TCP模式。
   - option tcplog: 记录TCP协议的日志。
   - tcp-request inspect-delay 5s: 设置在接收到请求后延迟5秒进行检查。
   - default_backend k8s-master: 设置默认的后端服务器组为k8s-master。

5. backend k8s-master:
   - mode tcp: 设定backend的工作模式为TCP模式。
   - option tcplog: 记录TCP协议的日志。
   - option tcp-check: 启用TCP检查功能。
   - balance roundrobin: 使用轮询算法进行负载均衡。
   - default-server inter 10s downinter 5s rise 2 fall 2 slowstart 60s maxconn 250 maxqueue 256 weight 100: 设置默认的服务器参数。
   - server k8s-master01 192.168.1.31:6443 check: 增加一个名为k8s-master01的服务器，IP地址为192.168.1.31，端口号为6443，并对其进行健康检查。
   - server k8s-master02 192.168.1.32:6443 check: 增加一个名为k8s-master02的服务器，IP地址为192.168.1.32，端口号为6443，并对其进行健康检查。
   - server k8s-master03 192.168.1.33:6443 check: 增加一个名为k8s-master03的服务器，IP地址为192.168.1.33，端口号为6443，并对其进行健康检查。

以上就是这段配置代码的详细解释。它主要定义了全局配置、默认配置、前端监听和后端服务器组的相关参数和设置。通过这些配置，可以实现负载均衡和监控功能。
</code></pre></div></div>

<h3 id="523master01配置keepalived-master节点">5.2.3Master01配置keepalived master节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp</span> /etc/keepalived/keepalived.conf /etc/keepalived/keepalived.conf.bak

<span class="nb">cat</span> <span class="o">&gt;</span> /etc/keepalived/keepalived.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
! Configuration File for keepalived

global_defs {
    router_id LVS_DEVEL
}
vrrp_script chk_apiserver {
    script "/etc/keepalived/check_apiserver.sh"
    interval 5 
    weight -5
    fall 2
    rise 1
}
vrrp_instance VI_1 {
    state MASTER
    # 注意网卡名
    interface ens160 
    mcast_src_ip 192.168.1.31
    virtual_router_id 51
    priority 100
    nopreempt
    advert_int 2
    authentication {
        auth_type PASS
        auth_pass K8SHA_KA_AUTH
    }
    virtual_ipaddress {
        192.168.1.36
    }
    track_script {
      chk_apiserver 
} }
</span><span class="no">
EOF
</span></code></pre></div></div>

<h3 id="524master02配置keepalived-backup节点">5.2.4Master02配置keepalived backup节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp</span> /etc/keepalived/keepalived.conf /etc/keepalived/keepalived.conf.bak

<span class="nb">cat</span> <span class="o">&gt;</span> /etc/keepalived/keepalived.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
! Configuration File for keepalived

global_defs {
    router_id LVS_DEVEL
}
vrrp_script chk_apiserver {
    script "/etc/keepalived/check_apiserver.sh"
    interval 5 
    weight -5
    fall 2
    rise 1

}
vrrp_instance VI_1 {
    state BACKUP
    # 注意网卡名
    interface ens160
    mcast_src_ip 192.168.1.32
    virtual_router_id 51
    priority 80
    nopreempt
    advert_int 2
    authentication {
        auth_type PASS
        auth_pass K8SHA_KA_AUTH
    }
    virtual_ipaddress {
        192.168.1.36
    }
    track_script {
      chk_apiserver 
} }
</span><span class="no">
EOF
</span></code></pre></div></div>

<h3 id="525master03配置keepalived-backup节点">5.2.5Master03配置keepalived backup节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cp</span> /etc/keepalived/keepalived.conf /etc/keepalived/keepalived.conf.bak

<span class="nb">cat</span> <span class="o">&gt;</span> /etc/keepalived/keepalived.conf <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
! Configuration File for keepalived

global_defs {
    router_id LVS_DEVEL
}
vrrp_script chk_apiserver {
    script "/etc/keepalived/check_apiserver.sh"
    interval 5 
    weight -5
    fall 2
    rise 1

}
vrrp_instance VI_1 {
    state BACKUP
    # 注意网卡名
    interface ens160
    mcast_src_ip 192.168.1.33
    virtual_router_id 51
    priority 50
    nopreempt
    advert_int 2
    authentication {
        auth_type PASS
        auth_pass K8SHA_KA_AUTH
    }
    virtual_ipaddress {
        192.168.1.36
    }
    track_script {
      chk_apiserver 
} }
</span><span class="no">
EOF
</span></code></pre></div></div>

<p>参数</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>这是一个用于配置keepalived的配置文件。下面是对每个部分的详细解释：

- <span class="sb">`</span>global_defs<span class="sb">`</span>部分定义了全局参数。
- <span class="sb">`</span>router_id<span class="sb">`</span>参数指定了当前路由器的标识，这里设置为<span class="s2">"LVS_DEVEL"</span>。

- <span class="sb">`</span>vrrp_script<span class="sb">`</span>部分定义了一个VRRP脚本。<span class="sb">`</span>chk_apiserver<span class="sb">`</span>是脚本的名称，
    - <span class="sb">`</span>script<span class="sb">`</span>参数指定了脚本的路径。该脚本每5秒执行一次，返回值为0表示服务正常，返回值为1表示服务异常。
    - <span class="sb">`</span>weight<span class="sb">`</span>参数指定了根据脚本返回的值来调整优先级，这里设置为-5。
    - <span class="sb">`</span>fall<span class="sb">`</span>参数指定了失败阈值，当连续2次脚本返回值为1时认为服务异常。
    - <span class="sb">`</span>rise<span class="sb">`</span>参数指定了恢复阈值，当连续1次脚本返回值为0时认为服务恢复正常。

- <span class="sb">`</span>vrrp_instance<span class="sb">`</span>部分定义了一个VRRP实例。<span class="sb">`</span>VI_1<span class="sb">`</span>是实例的名称。
    - <span class="sb">`</span>state<span class="sb">`</span>参数指定了当前实例的状态，这里设置为MASTER表示当前实例是主节点。
    - <span class="sb">`</span>interface<span class="sb">`</span>参数指定了要监听的网卡，这里设置为ens160。
    - <span class="sb">`</span>mcast_src_ip<span class="sb">`</span>参数指定了VRRP报文的源IP地址，这里设置为192.168.1.31。
    - <span class="sb">`</span>virtual_router_id<span class="sb">`</span>参数指定了虚拟路由器的ID，这里设置为51。
    - <span class="sb">`</span>priority<span class="sb">`</span>参数指定了实例的优先级，优先级越高（数值越大）越有可能被选为主节点。
    - <span class="sb">`</span>nopreempt<span class="sb">`</span>参数指定了当主节点失效后不要抢占身份，即不要自动切换为主节点。
    - <span class="sb">`</span>advert_int<span class="sb">`</span>参数指定了发送广播的间隔时间，这里设置为2秒。
    - <span class="sb">`</span>authentication<span class="sb">`</span>部分指定了认证参数
        - <span class="sb">`</span>auth_type<span class="sb">`</span>参数指定了认证类型，这里设置为PASS表示使用密码认证，
        - <span class="sb">`</span>auth_pass<span class="sb">`</span>参数指定了认证密码，这里设置为K8SHA_KA_AUTH。
    - <span class="sb">`</span>virtual_ipaddress<span class="sb">`</span>部分指定了虚拟IP地址，这里设置为192.168.1.36。
    - <span class="sb">`</span>track_script<span class="sb">`</span>部分指定了要跟踪的脚本，这里跟踪了chk_apiserver脚本。
</code></pre></div></div>

<h3 id="526健康检查脚本配置lb主机">5.2.6健康检查脚本配置（lb主机）</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span>  /etc/keepalived/check_apiserver.sh <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
#!/bin/bash

err=0
for k in </span><span class="se">\$</span><span class="sh">(seq 1 3)
do
    check_code=</span><span class="se">\$</span><span class="sh">(pgrep haproxy)
    if [[ </span><span class="se">\$</span><span class="sh">check_code == "" ]]; then
        err=</span><span class="se">\$</span><span class="sh">(expr </span><span class="se">\$</span><span class="sh">err + 1)
        sleep 1
        continue
    else
        err=0
        break
    fi
done

if [[ </span><span class="se">\$</span><span class="sh">err != "0" ]]; then
    echo "systemctl stop keepalived"
    /usr/bin/systemctl stop keepalived
    exit 1
else
    exit 0
fi
</span><span class="no">EOF

</span><span class="c"># 给脚本授权</span>

<span class="nb">chmod</span> +x /etc/keepalived/check_apiserver.sh

<span class="c"># 这段脚本是一个简单的bash脚本，主要用来检查是否有名为haproxy的进程正在运行。</span>
<span class="c"># </span>
<span class="c"># 脚本的主要逻辑如下：</span>
<span class="c"># 1. 首先设置一个变量err为0，用来记录错误次数。</span>
<span class="c"># 2. 使用一个循环，在循环内部执行以下操作：</span>
<span class="c">#    a. 使用pgrep命令检查是否有名为haproxy的进程在运行。如果不存在该进程，将err加1，并暂停1秒钟，然后继续下一次循环。</span>
<span class="c">#    b. 如果存在haproxy进程，将err重置为0，并跳出循环。</span>
<span class="c"># 3. 检查err的值，如果不为0，表示检查失败，输出一条错误信息并执行“systemctl stop keepalived”命令停止keepalived进程，并退出脚本返回1。</span>
<span class="c"># 4. 如果err的值为0，表示检查成功，退出脚本返回0。</span>
<span class="c"># </span>
<span class="c"># 该脚本的主要作用是检查是否存在运行中的haproxy进程，如果无法检测到haproxy进程，将停止keepalived进程并返回错误状态。如果haproxy进程存在，则返回成功状态。这个脚本可能是作为一个健康检查脚本的一部分，在确保haproxy服务可用的情况下，才继续运行其他操作。</span>
</code></pre></div></div>

<h3 id="527启动服务">5.2.7启动服务</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>
systemctl <span class="nb">enable</span> <span class="nt">--now</span> haproxy.service
<span class="c"># 启用并立即启动haproxy.service单元。haproxy.service是haproxy守护进程的systemd服务单元。</span>
systemctl <span class="nb">enable</span> <span class="nt">--now</span> keepalived.service
<span class="c"># 启用并立即启动keepalived.service单元。keepalived.service是keepalived守护进程的systemd服务单元。</span>
systemctl status haproxy.service
<span class="c"># haproxy.service单元的当前状态，包括运行状态、是否启用等信息。</span>
systemctl status keepalived.service
<span class="c"># keepalived.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h3 id="528测试高可用">5.2.8测试高可用</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 能ping同</span>
<span class="o">[</span>root@k8s-node02 ~]# ping 192.168.1.36

<span class="c"># 能telnet访问</span>
<span class="o">[</span>root@k8s-node02 ~]# telnet 192.168.1.36 9443

<span class="c"># 关闭主节点，看vip是否漂移到备节点</span>
</code></pre></div></div>

<h1 id="6k8s组件配置">6.k8s组件配置</h1>

<p>所有k8s节点创建以下目录</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /etc/kubernetes/manifests/ /etc/systemd/system/kubelet.service.d /var/lib/kubelet /var/log/kubernetes
</code></pre></div></div>

<h2 id="61创建apiserver所有master节点">6.1.创建apiserver（所有master节点）</h2>

<h3 id="611master01节点配置">6.1.1master01节点配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 若关闭IPv6 删除 --service-cluster-ip-range 的 IPv6 即可</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kube-apiserver.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes API Server
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-apiserver </span><span class="se">\\</span><span class="sh">
      --v=2  </span><span class="se">\\</span><span class="sh">
      --allow-privileged=true  </span><span class="se">\\</span><span class="sh">
      --bind-address=0.0.0.0  </span><span class="se">\\</span><span class="sh">
      --secure-port=6443  </span><span class="se">\\</span><span class="sh">
      --advertise-address=192.168.1.31 </span><span class="se">\\</span><span class="sh">
      --service-cluster-ip-range=10.96.0.0/12,fd00:1111::/112  </span><span class="se">\\</span><span class="sh">
      --service-node-port-range=30000-32767  </span><span class="se">\\</span><span class="sh">
      --etcd-servers=https://192.168.1.31:2379,https://192.168.1.32:2379,https://192.168.1.33:2379 </span><span class="se">\\</span><span class="sh">
      --etcd-cafile=/etc/etcd/ssl/etcd-ca.pem  </span><span class="se">\\</span><span class="sh">
      --etcd-certfile=/etc/etcd/ssl/etcd.pem  </span><span class="se">\\</span><span class="sh">
      --etcd-keyfile=/etc/etcd/ssl/etcd-key.pem  </span><span class="se">\\</span><span class="sh">
      --client-ca-file=/etc/kubernetes/pki/ca.pem  </span><span class="se">\\</span><span class="sh">
      --tls-cert-file=/etc/kubernetes/pki/apiserver.pem  </span><span class="se">\\</span><span class="sh">
      --tls-private-key-file=/etc/kubernetes/pki/apiserver-key.pem  </span><span class="se">\\</span><span class="sh">
      --kubelet-client-certificate=/etc/kubernetes/pki/apiserver.pem  </span><span class="se">\\</span><span class="sh">
      --kubelet-client-key=/etc/kubernetes/pki/apiserver-key.pem  </span><span class="se">\\</span><span class="sh">
      --service-account-key-file=/etc/kubernetes/pki/sa.pub  </span><span class="se">\\</span><span class="sh">
      --service-account-signing-key-file=/etc/kubernetes/pki/sa.key  </span><span class="se">\\</span><span class="sh">
      --service-account-issuer=https://kubernetes.default.svc.cluster.local </span><span class="se">\\</span><span class="sh">
      --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname  </span><span class="se">\\</span><span class="sh">
      --enable-admission-plugins=NamespaceLifecycle,LimitRanger,ServiceAccount,DefaultStorageClass,DefaultTolerationSeconds,NodeRestriction,ResourceQuota  </span><span class="se">\</span><span class="sh">
      --authorization-mode=Node,RBAC  </span><span class="se">\\</span><span class="sh">
      --enable-bootstrap-token-auth=true  </span><span class="se">\\</span><span class="sh">
      --requestheader-client-ca-file=/etc/kubernetes/pki/front-proxy-ca.pem  </span><span class="se">\\</span><span class="sh">
      --proxy-client-cert-file=/etc/kubernetes/pki/front-proxy-client.pem  </span><span class="se">\\</span><span class="sh">
      --proxy-client-key-file=/etc/kubernetes/pki/front-proxy-client-key.pem  </span><span class="se">\\</span><span class="sh">
      --requestheader-allowed-names=aggregator  </span><span class="se">\\</span><span class="sh">
      --requestheader-group-headers=X-Remote-Group  </span><span class="se">\\</span><span class="sh">
      --requestheader-extra-headers-prefix=X-Remote-Extra-  </span><span class="se">\\</span><span class="sh">
      --requestheader-username-headers=X-Remote-User </span><span class="se">\\</span><span class="sh">
      --enable-aggregator-routing=true
Restart=on-failure
RestartSec=10s
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
</span><span class="no">
EOF
</span></code></pre></div></div>

<h3 id="612master02节点配置">6.1.2master02节点配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 若关闭IPv6 删除 --service-cluster-ip-range 的 IPv6 即可</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kube-apiserver.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes API Server
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-apiserver </span><span class="se">\\</span><span class="sh">
      --v=2  </span><span class="se">\\</span><span class="sh">
      --allow-privileged=true  </span><span class="se">\\</span><span class="sh">
      --bind-address=0.0.0.0  </span><span class="se">\\</span><span class="sh">
      --secure-port=6443  </span><span class="se">\\</span><span class="sh">
      --advertise-address=192.168.1.32 </span><span class="se">\\</span><span class="sh">
      --service-cluster-ip-range=10.96.0.0/12,fd00:1111::/112  </span><span class="se">\\</span><span class="sh">
      --service-node-port-range=30000-32767  </span><span class="se">\\</span><span class="sh">
      --etcd-servers=https://192.168.1.31:2379,https://192.168.1.32:2379,https://192.168.1.33:2379 </span><span class="se">\\</span><span class="sh">
      --etcd-cafile=/etc/etcd/ssl/etcd-ca.pem  </span><span class="se">\\</span><span class="sh">
      --etcd-certfile=/etc/etcd/ssl/etcd.pem  </span><span class="se">\\</span><span class="sh">
      --etcd-keyfile=/etc/etcd/ssl/etcd-key.pem  </span><span class="se">\\</span><span class="sh">
      --client-ca-file=/etc/kubernetes/pki/ca.pem  </span><span class="se">\\</span><span class="sh">
      --tls-cert-file=/etc/kubernetes/pki/apiserver.pem  </span><span class="se">\\</span><span class="sh">
      --tls-private-key-file=/etc/kubernetes/pki/apiserver-key.pem  </span><span class="se">\\</span><span class="sh">
      --kubelet-client-certificate=/etc/kubernetes/pki/apiserver.pem  </span><span class="se">\\</span><span class="sh">
      --kubelet-client-key=/etc/kubernetes/pki/apiserver-key.pem  </span><span class="se">\\</span><span class="sh">
      --service-account-key-file=/etc/kubernetes/pki/sa.pub  </span><span class="se">\\</span><span class="sh">
      --service-account-signing-key-file=/etc/kubernetes/pki/sa.key  </span><span class="se">\\</span><span class="sh">
      --service-account-issuer=https://kubernetes.default.svc.cluster.local </span><span class="se">\\</span><span class="sh">
      --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname  </span><span class="se">\\</span><span class="sh">
      --enable-admission-plugins=NamespaceLifecycle,LimitRanger,ServiceAccount,DefaultStorageClass,DefaultTolerationSeconds,NodeRestriction,ResourceQuota  </span><span class="se">\\</span><span class="sh">
      --authorization-mode=Node,RBAC  </span><span class="se">\\</span><span class="sh">
      --enable-bootstrap-token-auth=true  </span><span class="se">\\</span><span class="sh">
      --requestheader-client-ca-file=/etc/kubernetes/pki/front-proxy-ca.pem  </span><span class="se">\\</span><span class="sh">
      --proxy-client-cert-file=/etc/kubernetes/pki/front-proxy-client.pem  </span><span class="se">\\</span><span class="sh">
      --proxy-client-key-file=/etc/kubernetes/pki/front-proxy-client-key.pem  </span><span class="se">\\</span><span class="sh">
      --requestheader-allowed-names=aggregator  </span><span class="se">\\</span><span class="sh">
      --requestheader-group-headers=X-Remote-Group  </span><span class="se">\\</span><span class="sh">
      --requestheader-extra-headers-prefix=X-Remote-Extra-  </span><span class="se">\\</span><span class="sh">
      --requestheader-username-headers=X-Remote-User </span><span class="se">\\</span><span class="sh">
      --enable-aggregator-routing=true

Restart=on-failure
RestartSec=10s
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
</span><span class="no">
EOF
</span></code></pre></div></div>

<h3 id="613master03节点配置">6.1.3master03节点配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 若关闭IPv6 删除 --service-cluster-ip-range 的 IPv6 即可</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kube-apiserver.service  <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes API Server
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-apiserver </span><span class="se">\\</span><span class="sh">
      --v=2  </span><span class="se">\\</span><span class="sh">
      --allow-privileged=true  </span><span class="se">\\</span><span class="sh">
      --bind-address=0.0.0.0  </span><span class="se">\\</span><span class="sh">
      --secure-port=6443  </span><span class="se">\\</span><span class="sh">
      --advertise-address=192.168.1.33 </span><span class="se">\\</span><span class="sh">
      --service-cluster-ip-range=10.96.0.0/12,fd00:1111::/112  </span><span class="se">\\</span><span class="sh">
      --service-node-port-range=30000-32767  </span><span class="se">\\</span><span class="sh">
      --etcd-servers=https://192.168.1.31:2379,https://192.168.1.32:2379,https://192.168.1.33:2379 </span><span class="se">\\</span><span class="sh">
      --etcd-cafile=/etc/etcd/ssl/etcd-ca.pem  </span><span class="se">\\</span><span class="sh">
      --etcd-certfile=/etc/etcd/ssl/etcd.pem  </span><span class="se">\\</span><span class="sh">
      --etcd-keyfile=/etc/etcd/ssl/etcd-key.pem  </span><span class="se">\\</span><span class="sh">
      --client-ca-file=/etc/kubernetes/pki/ca.pem  </span><span class="se">\\</span><span class="sh">
      --tls-cert-file=/etc/kubernetes/pki/apiserver.pem  </span><span class="se">\\</span><span class="sh">
      --tls-private-key-file=/etc/kubernetes/pki/apiserver-key.pem  </span><span class="se">\\</span><span class="sh">
      --kubelet-client-certificate=/etc/kubernetes/pki/apiserver.pem  </span><span class="se">\\</span><span class="sh">
      --kubelet-client-key=/etc/kubernetes/pki/apiserver-key.pem  </span><span class="se">\\</span><span class="sh">
      --service-account-key-file=/etc/kubernetes/pki/sa.pub  </span><span class="se">\\</span><span class="sh">
      --service-account-signing-key-file=/etc/kubernetes/pki/sa.key  </span><span class="se">\\</span><span class="sh">
      --service-account-issuer=https://kubernetes.default.svc.cluster.local </span><span class="se">\\</span><span class="sh">
      --kubelet-preferred-address-types=InternalIP,ExternalIP,Hostname  </span><span class="se">\\</span><span class="sh">
      --enable-admission-plugins=NamespaceLifecycle,LimitRanger,ServiceAccount,DefaultStorageClass,DefaultTolerationSeconds,NodeRestriction,ResourceQuota  </span><span class="se">\\</span><span class="sh">
      --authorization-mode=Node,RBAC  </span><span class="se">\\</span><span class="sh">
      --enable-bootstrap-token-auth=true  </span><span class="se">\\</span><span class="sh">
      --requestheader-client-ca-file=/etc/kubernetes/pki/front-proxy-ca.pem  </span><span class="se">\\</span><span class="sh">
      --proxy-client-cert-file=/etc/kubernetes/pki/front-proxy-client.pem  </span><span class="se">\\</span><span class="sh">
      --proxy-client-key-file=/etc/kubernetes/pki/front-proxy-client-key.pem  </span><span class="se">\\</span><span class="sh">
      --requestheader-allowed-names=aggregator  </span><span class="se">\\</span><span class="sh">
      --requestheader-group-headers=X-Remote-Group  </span><span class="se">\\</span><span class="sh">
      --requestheader-extra-headers-prefix=X-Remote-Extra-  </span><span class="se">\\</span><span class="sh">
      --requestheader-username-headers=X-Remote-User </span><span class="se">\\</span><span class="sh">
      --enable-aggregator-routing=true

Restart=on-failure
RestartSec=10s
LimitNOFILE=65535

[Install]
WantedBy=multi-user.target
</span><span class="no">
EOF
</span></code></pre></div></div>

<p>参数</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>该配置文件是用于定义Kubernetes API Server的systemd服务的配置。systemd是一个用于启动和管理Linux系统服务的守护进程。

<span class="o">[</span>Unit]
- Description: 服务的描述信息，用于显示在日志和系统管理工具中。
- Documentation: 提供关于服务的文档链接。
- After: 规定服务依赖于哪些其他服务或单元。在这个例子中，API Server服务在网络目标启动之后启动。

<span class="o">[</span>Service]
- ExecStart: 定义服务的命令行参数和命令。这里指定了API Server的启动命令，包括各种参数选项。
- Restart: 指定当服务退出时应该如何重新启动。在这个例子中，服务在失败时将被重新启动。
- RestartSec: 指定两次重新启动之间的等待时间。
- LimitNOFILE: 指定进程可以打开的文件描述符的最大数量。

<span class="o">[</span>Install]
- WantedBy: 指定服务应该安装到哪个系统目标。在这个例子中，服务将被安装到multi-user.target目标，以便在多用户模式下启动。

上述配置文件中定义的kube-apiserver服务将以指定的参数运行，这些参数包括：

- <span class="sb">`</span><span class="nt">--v</span><span class="o">=</span>2<span class="sb">`</span> 指定日志级别为2，打印详细的API Server日志。
- <span class="sb">`</span><span class="nt">--allow-privileged</span><span class="o">=</span><span class="nb">true</span><span class="sb">`</span> 允许特权容器运行。
- <span class="sb">`</span><span class="nt">--bind-address</span><span class="o">=</span>0.0.0.0<span class="sb">`</span> 绑定API Server监听的IP地址。
- <span class="sb">`</span><span class="nt">--secure-port</span><span class="o">=</span>6443<span class="sb">`</span> 指定API Server监听的安全端口。
- <span class="sb">`</span><span class="nt">--advertise-address</span><span class="o">=</span>192.168.1.31<span class="sb">`</span> 广告API Server的地址。
- <span class="sb">`</span><span class="nt">--service-cluster-ip-range</span><span class="o">=</span>10.96.0.0/12,fd00:1111::/112<span class="sb">`</span> 指定服务CIDR范围。
- <span class="sb">`</span><span class="nt">--service-node-port-range</span><span class="o">=</span>30000-32767<span class="sb">`</span> 指定NodePort的范围。
- <span class="sb">`</span><span class="nt">--etcd-servers</span><span class="o">=</span>https://192.168.1.31:2379,https://192.168.1.32:2379,https://192.168.1.33:2379<span class="sb">`</span> 指定etcd服务器的地址。
- <span class="sb">`</span><span class="nt">--etcd-cafile</span><span class="sb">`</span> 指定etcd服务器的CA证书。
- <span class="sb">`</span><span class="nt">--etcd-certfile</span><span class="sb">`</span> 指定etcd服务器的证书。
- <span class="sb">`</span><span class="nt">--etcd-keyfile</span><span class="sb">`</span> 指定etcd服务器的私钥。
- <span class="sb">`</span><span class="nt">--client-ca-file</span><span class="sb">`</span> 指定客户端CA证书。
- <span class="sb">`</span><span class="nt">--tls-cert-file</span><span class="sb">`</span> 指定服务的证书。
- <span class="sb">`</span><span class="nt">--tls-private-key-file</span><span class="sb">`</span> 指定服务的私钥。
- <span class="sb">`</span><span class="nt">--kubelet-client-certificate</span><span class="sb">`</span> 和 <span class="sb">`</span><span class="nt">--kubelet-client-key</span><span class="sb">`</span> 指定与kubelet通信的客户端证书和私钥。
- <span class="sb">`</span><span class="nt">--service-account-key-file</span><span class="sb">`</span> 指定服务账户公钥文件。
- <span class="sb">`</span><span class="nt">--service-account-signing-key-file</span><span class="sb">`</span> 指定服务账户签名密钥文件。
- <span class="sb">`</span><span class="nt">--service-account-issuer</span><span class="sb">`</span> 指定服务账户的发布者。
- <span class="sb">`</span><span class="nt">--kubelet-preferred-address-types</span><span class="sb">`</span> 指定kubelet通信时的首选地址类型。
- <span class="sb">`</span><span class="nt">--enable-admission-plugins</span><span class="sb">`</span> 启用一系列准入插件。
- <span class="sb">`</span><span class="nt">--authorization-mode</span><span class="sb">`</span> 指定授权模式。
- <span class="sb">`</span><span class="nt">--enable-bootstrap-token-auth</span><span class="sb">`</span> 启用引导令牌认证。
- <span class="sb">`</span><span class="nt">--requestheader-client-ca-file</span><span class="sb">`</span> 指定请求头中的客户端CA证书。
- <span class="sb">`</span><span class="nt">--proxy-client-cert-file</span><span class="sb">`</span> 和 <span class="sb">`</span><span class="nt">--proxy-client-key-file</span><span class="sb">`</span> 指定代理客户端的证书和私钥。
- <span class="sb">`</span><span class="nt">--requestheader-allowed-names</span><span class="sb">`</span> 指定请求头中允许的名字。
- <span class="sb">`</span><span class="nt">--requestheader-group-headers</span><span class="sb">`</span> 指定请求头中的组头。
- <span class="sb">`</span><span class="nt">--requestheader-extra-headers-prefix</span><span class="sb">`</span> 指定请求头中的额外头前缀。
- <span class="sb">`</span><span class="nt">--requestheader-username-headers</span><span class="sb">`</span> 指定请求头中的用户名头。
- <span class="sb">`</span><span class="nt">--enable-aggregator-routing</span><span class="sb">`</span> 启用聚合路由。

整个配置文件为Kubernetes API Server提供了必要的参数，以便正确地启动和运行。
</code></pre></div></div>

<h3 id="614启动apiserver所有master节点">6.1.4启动apiserver（所有master节点）</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> kube-apiserver.service
<span class="c"># 启用并立即启动kube-apiserver.service单元。kube-apiserver.service是kube-apiserver守护进程的systemd服务单元。</span>

systemctl restart kube-apiserver.service
<span class="c"># 重启kube-apiserver.service单元，即重新启动etcd守护进程。</span>

systemctl status kube-apiserver.service
<span class="c"># kube-apiserver.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h2 id="62配置kube-controller-manager-service">6.2.配置kube-controller-manager service</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 所有master节点配置，且配置相同</span>
<span class="c"># 172.16.0.0/12为pod网段，按需求设置你自己的网段</span>

<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kube-controller-manager.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes Controller Manager
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-controller-manager </span><span class="se">\\</span><span class="sh">
      --v=2 </span><span class="se">\\</span><span class="sh">
      --bind-address=0.0.0.0 </span><span class="se">\\</span><span class="sh">
      --root-ca-file=/etc/kubernetes/pki/ca.pem </span><span class="se">\\</span><span class="sh">
      --cluster-signing-cert-file=/etc/kubernetes/pki/ca.pem </span><span class="se">\\</span><span class="sh">
      --cluster-signing-key-file=/etc/kubernetes/pki/ca-key.pem </span><span class="se">\\</span><span class="sh">
      --service-account-private-key-file=/etc/kubernetes/pki/sa.key </span><span class="se">\\</span><span class="sh">
      --kubeconfig=/etc/kubernetes/controller-manager.kubeconfig </span><span class="se">\\</span><span class="sh">
      --leader-elect=true </span><span class="se">\\</span><span class="sh">
      --use-service-account-credentials=true </span><span class="se">\\</span><span class="sh">
      --node-monitor-grace-period=40s </span><span class="se">\\</span><span class="sh">
      --node-monitor-period=5s </span><span class="se">\\</span><span class="sh">
      --controllers=*,bootstrapsigner,tokencleaner </span><span class="se">\\</span><span class="sh">
      --allocate-node-cidrs=true </span><span class="se">\\</span><span class="sh">
      --service-cluster-ip-range=10.96.0.0/12,fd00:1111::/112 </span><span class="se">\\</span><span class="sh">
      --cluster-cidr=172.16.0.0/12,fc00:2222::/112 </span><span class="se">\\</span><span class="sh">
      --node-cidr-mask-size-ipv4=24 </span><span class="se">\\</span><span class="sh">
      --node-cidr-mask-size-ipv6=120 </span><span class="se">\\</span><span class="sh">
      --requestheader-client-ca-file=/etc/kubernetes/pki/front-proxy-ca.pem

Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF



</span><span class="c"># 关闭IPv6 </span>
<span class="c"># 删除 --service-cluster-ip-range 中的IPv6地址</span>
<span class="c"># 删除 --cluster-cidr 中的IPv6地址</span>
<span class="c"># 删除 --node-cidr-mask-size-ipv6=120</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kube-controller-manager.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes Controller Manager
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-controller-manager </span><span class="se">\\</span><span class="sh">
      --v=2 </span><span class="se">\\</span><span class="sh">
      --bind-address=0.0.0.0 </span><span class="se">\\</span><span class="sh">
      --root-ca-file=/etc/kubernetes/pki/ca.pem </span><span class="se">\\</span><span class="sh">
      --cluster-signing-cert-file=/etc/kubernetes/pki/ca.pem </span><span class="se">\\</span><span class="sh">
      --cluster-signing-key-file=/etc/kubernetes/pki/ca-key.pem </span><span class="se">\\</span><span class="sh">
      --service-account-private-key-file=/etc/kubernetes/pki/sa.key </span><span class="se">\\</span><span class="sh">
      --kubeconfig=/etc/kubernetes/controller-manager.kubeconfig </span><span class="se">\\</span><span class="sh">
      --leader-elect=true </span><span class="se">\\</span><span class="sh">
      --use-service-account-credentials=true </span><span class="se">\\</span><span class="sh">
      --node-monitor-grace-period=40s </span><span class="se">\\</span><span class="sh">
      --node-monitor-period=5s </span><span class="se">\\</span><span class="sh">
      --controllers=*,bootstrapsigner,tokencleaner </span><span class="se">\\</span><span class="sh">
      --allocate-node-cidrs=true </span><span class="se">\\</span><span class="sh">
      --service-cluster-ip-range=10.96.0.0/12 </span><span class="se">\\</span><span class="sh">
      --cluster-cidr=172.16.0.0/12 </span><span class="se">\\</span><span class="sh">
      --node-cidr-mask-size-ipv4=24 </span><span class="se">\\</span><span class="sh">
      --requestheader-client-ca-file=/etc/kubernetes/pki/front-proxy-ca.pem

Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF
</span></code></pre></div></div>

<p>参数</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>这是一个用于启动 Kubernetes 控制器管理器的 systemd 服务单元文件。下面是对每个部分的详细解释：

<span class="o">[</span>Unit]：单元的基本信息部分，用于描述和标识这个服务单元。
Description：服务单元的描述信息，说明了该服务单元的作用，这里是 Kubernetes 控制器管理器。
Documentation：可选项，提供了关于该服务单元的文档链接。
After：定义了该服务单元在哪些其他单元之后启动，这里是 network.target，即在网络服务启动之后启动。

<span class="o">[</span>Service]：定义了服务的运行参数和行为。
ExecStart：指定服务启动时执行的命令，这里是 /usr/local/bin/kube-controller-manager，并通过后续的行继续传递了一系列的参数设置。
Restart：定义了服务在退出后的重新启动策略，这里设置为 always，表示总是重新启动服务。
RestartSec：定义了重新启动服务的时间间隔，这里设置为 10 秒。

<span class="o">[</span>Install]：定义了如何安装和启用服务单元。
WantedBy：指定了服务单元所属的 target，这里是 multi-user.target，表示启动多用户模式下的服务。
在 ExecStart 中传递的参数说明如下：

<span class="nt">--v</span><span class="o">=</span>2：设置日志的详细级别为 2。
<span class="nt">--bind-address</span><span class="o">=</span>0.0.0.0：绑定的 IP 地址，用于监听 Kubernetes 控制平面的请求，这里设置为 0.0.0.0，表示监听所有网络接口上的请求。
<span class="nt">--root-ca-file</span>：根证书文件的路径，用于验证其他组件的证书。
<span class="nt">--cluster-signing-cert-file</span>：用于签名集群证书的证书文件路径。
<span class="nt">--cluster-signing-key-file</span>：用于签名集群证书的私钥文件路径。
<span class="nt">--service-account-private-key-file</span>：用于签名服务账户令牌的私钥文件路径。
<span class="nt">--kubeconfig</span>：kubeconfig 文件的路径，包含了与 Kubernetes API 服务器通信所需的配置信息。
<span class="nt">--leader-elect</span><span class="o">=</span><span class="nb">true</span>：启用 Leader 选举机制，确保只有一个控制器管理器作为 leader 在运行。
<span class="nt">--use-service-account-credentials</span><span class="o">=</span><span class="nb">true</span>：使用服务账户的凭据进行认证和授权。
<span class="nt">--node-monitor-grace-period</span><span class="o">=</span>40s：节点监控的优雅退出时间，节点长时间不响应时会触发节点驱逐。
<span class="nt">--node-monitor-period</span><span class="o">=</span>5s：节点监控的检测周期，用于检测节点是否正常运行。
<span class="nt">--controllers</span>：指定要运行的控制器类型，在这里使用了通配符 <span class="k">*</span>，表示运行所有的控制器，同时还包括了 bootstrapsigner 和 tokencleaner 控制器。
<span class="nt">--allocate-node-cidrs</span><span class="o">=</span><span class="nb">true</span>：为节点分配 CIDR 子网，用于分配 Pod 网络地址。
<span class="nt">--service-cluster-ip-range</span>：定义 Service 的 IP 范围，这里设置为 10.96.0.0/12 和 fd00::/108。
<span class="nt">--cluster-cidr</span>：定义集群的 CIDR 范围，这里设置为 172.16.0.0/12 和 fc00::/48。
<span class="nt">--node-cidr-mask-size-ipv4</span>：分配给每个节点的 IPv4 子网掩码大小，默认是 24。
<span class="nt">--node-cidr-mask-size-ipv6</span>：分配给每个节点的 IPv6 子网掩码大小，默认是 120。
<span class="nt">--requestheader-client-ca-file</span>：设置请求头中客户端 CA 的证书文件路径，用于认证请求头中的 CA 证书。

这个服务单元文件描述了 Kubernetes 控制器管理器的启动参数和行为，并且定义了服务的依赖关系和重新启动策略。通过 systemd 启动该服务单元，即可启动 Kubernetes 控制器管理器组件。
</code></pre></div></div>

<h3 id="621启动kube-controller-manager并查看状态">6.2.1启动kube-controller-manager，并查看状态</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> kube-controller-manager.service
<span class="c"># 启用并立即启动kube-controller-manager.service单元。kube-controller-manager.service是kube-controller-manager守护进程的systemd服务单元。</span>

systemctl restart kube-controller-manager.service
<span class="c"># 重启kube-controller-manager.service单元，即重新启动etcd守护进程。</span>

systemctl status kube-controller-manager.service
<span class="c"># kube-controller-manager.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h2 id="63配置kube-scheduler-service">6.3.配置kube-scheduler service</h2>

<h3 id="631所有master节点配置且配置相同">6.3.1所有master节点配置，且配置相同</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kube-scheduler.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">

[Unit]
Description=Kubernetes Scheduler
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-scheduler </span><span class="se">\\</span><span class="sh">
      --v=2 </span><span class="se">\\</span><span class="sh">
      --bind-address=0.0.0.0 </span><span class="se">\\</span><span class="sh">
      --leader-elect=true </span><span class="se">\\</span><span class="sh">
      --kubeconfig=/etc/kubernetes/scheduler.kubeconfig

Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
</span><span class="no">
EOF
</span></code></pre></div></div>

<p>参数</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>这是一个用于启动 Kubernetes 调度器的 systemd 服务单元文件。下面是对每个部分的详细解释：

<span class="o">[</span>Unit]：单元的基本信息部分，用于描述和标识这个服务单元。
Description：服务单元的描述信息，说明了该服务单元的作用，这里是 Kubernetes 调度器。
Documentation：可选项，提供了关于该服务单元的文档链接。
After：定义了该服务单元在哪些其他单元之后启动，这里是 network.target，即在网络服务启动之后启动。

<span class="o">[</span>Service]：定义了服务的运行参数和行为。
ExecStart：指定服务启动时执行的命令，这里是 /usr/local/bin/kube-scheduler，并通过后续的行继续传递了一系列的参数设置。
Restart：定义了服务在退出后的重新启动策略，这里设置为 always，表示总是重新启动服务。
RestartSec：定义了重新启动服务的时间间隔，这里设置为 10 秒。

<span class="o">[</span>Install]：定义了如何安装和启用服务单元。
WantedBy：指定了服务单元所属的 target，这里是 multi-user.target，表示启动多用户模式下的服务。

在 ExecStart 中传递的参数说明如下：

<span class="nt">--v</span><span class="o">=</span>2：设置日志的详细级别为 2。
<span class="nt">--bind-address</span><span class="o">=</span>0.0.0.0：绑定的 IP 地址，用于监听 Kubernetes 控制平面的请求，这里设置为 0.0.0.0，表示监听所有网络接口上的请求。
<span class="nt">--leader-elect</span><span class="o">=</span><span class="nb">true</span>：启用 Leader 选举机制，确保只有一个调度器作为 leader 在运行。
<span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/scheduler.kubeconfig：kubeconfig 文件的路径，包含了与 Kubernetes API 服务器通信所需的配置信息。

这个服务单元文件描述了 Kubernetes 调度器的启动参数和行为，并且定义了服务的依赖关系和重新启动策略。通过 systemd 启动该服务单元，即可启动 Kubernetes 调度器组件。
</code></pre></div></div>

<h3 id="632启动并查看服务状态">6.3.2启动并查看服务状态</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> kube-scheduler.service
<span class="c"># 启用并立即启动kube-scheduler.service单元。kube-scheduler.service是kube-scheduler守护进程的systemd服务单元。</span>

systemctl restart kube-scheduler.service
<span class="c"># 重启kube-scheduler.service单元，即重新启动etcd守护进程。</span>

systemctl status kube-scheduler.service
<span class="c"># kube-scheduler.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h1 id="7tls-bootstrapping配置">7.TLS Bootstrapping配置</h1>

<h2 id="71在master01上配置">7.1在master01上配置</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 在《5.高可用配置》选择使用那种高可用方案</span>
<span class="c"># 若使用 haproxy、keepalived 那么为 `--server=https://192.168.1.36:9443`</span>
<span class="c"># 若使用 nginx方案，那么为 `--server=https://127.0.0.1:8443`</span>

kubectl config set-cluster kubernetes     <span class="se">\</span>
<span class="nt">--certificate-authority</span><span class="o">=</span>/etc/kubernetes/pki/ca.pem     <span class="se">\</span>
<span class="nt">--embed-certs</span><span class="o">=</span><span class="nb">true</span>     <span class="nt">--server</span><span class="o">=</span>https://127.0.0.1:8443     <span class="se">\</span>
<span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/bootstrap-kubelet.kubeconfig
<span class="c"># 这是一个使用 kubectl 命令设置 Kubernetes 集群配置的命令示例。下面是对每个选项的详细解释：</span>
<span class="c"># </span>
<span class="c"># config set-cluster kubernetes：指定要设置的集群名称为 "kubernetes"，表示要修改名为 "kubernetes" 的集群配置。</span>
<span class="c"># --certificate-authority=/etc/kubernetes/pki/ca.pem：指定证书颁发机构（CA）的证书文件路径，用于验证服务器证书的有效性。</span>
<span class="c"># --embed-certs=true：将证书文件嵌入到生成的 kubeconfig 文件中。这样可以避免在 kubeconfig 文件中引用外部证书文件。</span>
<span class="c"># --server=https://127.0.0.1:8443：指定 Kubernetes API 服务器的地址和端口，这里使用的是 https 协议和本地地址（127.0.0.1），端口号为 8443。你可以根据实际环境修改该参数。</span>
<span class="c"># --kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig：指定 kubeconfig 文件的路径和名称，这里是 /etc/kubernetes/bootstrap-kubelet.kubeconfig。</span>
<span class="c"># 通过执行此命令，你可以设置名为 "kubernetes" 的集群配置，并提供 CA 证书、API 服务器地址和端口，并将这些配置信息嵌入到 bootstrap-kubelet.kubeconfig 文件中。这个 kubeconfig 文件可以用于认证和授权 kubelet 组件与 Kubernetes API 服务器之间的通信。请确保路径和文件名与实际环境中的配置相匹配。</span>


<span class="c"># 可以使用这个命令进行创建token也可以使用我的</span>
<span class="nb">echo</span> <span class="s2">"</span><span class="si">$(</span><span class="nb">head</span> <span class="nt">-c</span> 6 /dev/urandom | <span class="nb">md5sum</span> | <span class="nb">head</span> <span class="nt">-c</span> 6<span class="si">)</span><span class="s2">"</span>.<span class="s2">"</span><span class="si">$(</span><span class="nb">head</span> <span class="nt">-c</span> 16 /dev/urandom | <span class="nb">md5sum</span> | <span class="nb">head</span> <span class="nt">-c</span> 16<span class="si">)</span><span class="s2">"</span>

kubectl config set-credentials tls-bootstrap-token-user     <span class="se">\</span>
<span class="nt">--token</span><span class="o">=</span>c8ad9c.2e4d610cf3e7426e <span class="se">\</span>
<span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/bootstrap-kubelet.kubeconfig
<span class="c"># 这是一个使用 kubectl 命令设置凭证信息的命令示例。下面是对每个选项的详细解释：</span>
<span class="c"># </span>
<span class="c"># config set-credentials tls-bootstrap-token-user：指定要设置的凭证名称为 "tls-bootstrap-token-user"，表示要修改名为 "tls-bootstrap-token-user" 的用户凭证配置。</span>
<span class="c"># --token=c8ad9c.2e4d610cf3e7426e：指定用户的身份验证令牌（token）。在这个示例中，令牌是 c8ad9c.2e4d610cf3e7426e。你可以根据实际情况修改该令牌。</span>
<span class="c"># --kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig：指定 kubeconfig 文件的路径和名称，这里是 /etc/kubernetes/bootstrap-kubelet.kubeconfig。</span>
<span class="c"># 通过执行此命令，你可以设置名为 "tls-bootstrap-token-user" 的用户凭证，并将令牌信息加入到 bootstrap-kubelet.kubeconfig 文件中。这个 kubeconfig 文件可以用于认证和授权 kubelet 组件与 Kubernetes API 服务器之间的通信。请确保路径和文件名与实际环境中的配置相匹配。</span>

kubectl config set-context tls-bootstrap-token-user@kubernetes     <span class="se">\</span>
<span class="nt">--cluster</span><span class="o">=</span>kubernetes     <span class="se">\</span>
<span class="nt">--user</span><span class="o">=</span>tls-bootstrap-token-user     <span class="se">\</span>
<span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/bootstrap-kubelet.kubeconfig
<span class="c"># 这是一个使用 kubectl 命令设置上下文信息的命令示例。下面是对每个选项的详细解释：</span>
<span class="c"># </span>
<span class="c"># config set-context tls-bootstrap-token-user@kubernetes：指定要设置的上下文名称为 "tls-bootstrap-token-user@kubernetes"，表示要修改名为 "tls-bootstrap-token-user@kubernetes" 的上下文配置。</span>
<span class="c"># --cluster=kubernetes：指定上下文关联的集群名称为 "kubernetes"，表示使用名为 "kubernetes" 的集群配置。</span>
<span class="c"># --user=tls-bootstrap-token-user：指定上下文关联的用户凭证名称为 "tls-bootstrap-token-user"，表示使用名为 "tls-bootstrap-token-user" 的用户凭证配置。</span>
<span class="c"># --kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig：指定 kubeconfig 文件的路径和名称，这里是 /etc/kubernetes/bootstrap-kubelet.kubeconfig。</span>
<span class="c"># 通过执行此命令，你可以设置名为 "tls-bootstrap-token-user@kubernetes" 的上下文，并将其关联到名为 "kubernetes" 的集群配置和名为 "tls-bootstrap-token-user" 的用户凭证配置。这样，bootstrap-kubelet.kubeconfig 文件就包含了完整的上下文信息，可以用于指定与 Kubernetes 集群建立连接时要使用的集群和凭证。请确保路径和文件名与实际环境中的配置相匹配。</span>

kubectl config use-context tls-bootstrap-token-user@kubernetes     <span class="se">\</span>
<span class="nt">--kubeconfig</span><span class="o">=</span>/etc/kubernetes/bootstrap-kubelet.kubeconfig
<span class="c"># 这是一个使用 kubectl 命令设置当前上下文的命令示例。下面是对每个选项的详细解释：</span>
<span class="c"># </span>
<span class="c"># config use-context tls-bootstrap-token-user@kubernetes：指定要使用的上下文名称为 "tls-bootstrap-token-user@kubernetes"，表示要将当前上下文切换为名为 "tls-bootstrap-token-user@kubernetes" 的上下文。</span>
<span class="c"># --kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig：指定 kubeconfig 文件的路径和名称，这里是 /etc/kubernetes/bootstrap-kubelet.kubeconfig。</span>
<span class="c"># 通过执行此命令，你可以将当前上下文设置为名为 "tls-bootstrap-token-user@kubernetes" 的上下文。这样，当你执行其他 kubectl 命令时，它们将使用该上下文与 Kubernetes 集群进行交互。请确保路径和文件名与实际环境中的配置相匹配。</span>


<span class="c"># token的位置在bootstrap.secret.yaml，如果修改的话到这个文件修改</span>
<span class="nb">mkdir</span> <span class="nt">-p</span> /root/.kube <span class="p">;</span> <span class="nb">cp</span> /etc/kubernetes/admin.kubeconfig /root/.kube/config
</code></pre></div></div>

<h2 id="72查看集群状态没问题的话继续后续操作">7.2查看集群状态，没问题的话继续后续操作</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1.28 版本只能查看到一个etcd 属于正常现象</span>
<span class="c"># export ETCDCTL_API=3</span>
<span class="c"># etcdctl --endpoints="192.168.1.33:2379,192.168.1.32:2379,192.168.1.31:2379" --cacert=/etc/kubernetes/pki/etcd/etcd-ca.pem --cert=/etc/kubernetes/pki/etcd/etcd.pem --key=/etc/kubernetes/pki/etcd/etcd-key.pem  endpoint status --write-out=table</span>

kubectl get cs
Warning: v1 ComponentStatus is deprecated <span class="k">in </span>v1.19+
NAME                 STATUS    MESSAGE   ERROR
scheduler            Healthy   ok        
controller-manager   Healthy   ok        
etcd-0               Healthy   ok 

<span class="c"># 写入bootstrap-token</span>
<span class="nb">cat</span> <span class="o">&gt;</span> bootstrap.secret.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: v1
kind: Secret
metadata:
  name: bootstrap-token-c8ad9c
  namespace: kube-system
type: bootstrap.kubernetes.io/token
stringData:
  description: "The default bootstrap token generated by 'kubelet '."
  token-id: c8ad9c
  token-secret: 2e4d610cf3e7426e
  usage-bootstrap-authentication: "true"
  usage-bootstrap-signing: "true"
  auth-extra-groups:  system:bootstrappers:default-node-token,system:bootstrappers:worker,system:bootstrappers:ingress

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kubelet-bootstrap
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:node-bootstrapper
subjects:
- apiGroup: rbac.authorization.k8s.io
  kind: Group
  name: system:bootstrappers:default-node-token
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: node-autoapprove-bootstrap
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:certificates.k8s.io:certificatesigningrequests:nodeclient
subjects:
- apiGroup: rbac.authorization.k8s.io
  kind: Group
  name: system:bootstrappers:default-node-token
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: node-autoapprove-certificate-rotation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:certificates.k8s.io:certificatesigningrequests:selfnodeclient
subjects:
- apiGroup: rbac.authorization.k8s.io
  kind: Group
  name: system:nodes
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    rbac.authorization.kubernetes.io/autoupdate: "true"
  labels:
    kubernetes.io/bootstrapping: rbac-defaults
  name: system:kube-apiserver-to-kubelet
rules:
  - apiGroups:
      - ""
    resources:
      - nodes/proxy
      - nodes/stats
      - nodes/log
      - nodes/spec
      - nodes/metrics
    verbs:
      - "*"
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: system:kube-apiserver
  namespace: ""
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:kube-apiserver-to-kubelet
subjects:
  - apiGroup: rbac.authorization.k8s.io
    kind: User
    name: kube-apiserver
</span><span class="no">EOF
</span><span class="c"># 切记执行，别忘记！！！</span>
kubectl create <span class="nt">-f</span> bootstrap.secret.yaml
</code></pre></div></div>

<h1 id="8node节点配置">8.node节点配置</h1>

<h2 id="81在master01上将证书复制到node节点">8.1.在master01上将证书复制到node节点</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> /etc/kubernetes/

<span class="k">for </span>NODE <span class="k">in </span>k8s-master02 k8s-master03 k8s-node01 k8s-node02<span class="p">;</span> <span class="k">do </span>ssh <span class="nv">$NODE</span> <span class="nb">mkdir</span> <span class="nt">-p</span> /etc/kubernetes/pki<span class="p">;</span> <span class="k">for </span>FILE <span class="k">in </span>pki/ca.pem pki/ca-key.pem pki/front-proxy-ca.pem bootstrap-kubelet.kubeconfig kube-proxy.kubeconfig<span class="p">;</span> <span class="k">do </span>scp /etc/kubernetes/<span class="nv">$FILE</span> <span class="nv">$NODE</span>:/etc/kubernetes/<span class="k">${</span><span class="nv">FILE</span><span class="k">}</span><span class="p">;</span> <span class="k">done</span><span class="p">;</span> <span class="k">done</span>
</code></pre></div></div>

<h2 id="82kubelet配置">8.2.kubelet配置</h2>

<p><strong>注意 ： 8.2.1 和 8.2.2 需要和 上方 2.1 和 2.2 对应起来</strong></p>

<h3 id="821当使用docker作为runtime">8.2.1当使用docker作为Runtime</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kubelet.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">

[Unit]
Description=Kubernetes Kubelet
Documentation=https://github.com/kubernetes/kubernetes
After=network-online.target firewalld.service cri-docker.service docker.socket containerd.service
Wants=network-online.target
Requires=docker.socket containerd.service

[Service]
ExecStart=/usr/local/bin/kubelet </span><span class="se">\\</span><span class="sh">
    --bootstrap-kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig  </span><span class="se">\\</span><span class="sh">
    --kubeconfig=/etc/kubernetes/kubelet.kubeconfig </span><span class="se">\\</span><span class="sh">
    --config=/etc/kubernetes/kubelet-conf.yml </span><span class="se">\\</span><span class="sh">
    --container-runtime-endpoint=unix:///run/cri-dockerd.sock  </span><span class="se">\\</span><span class="sh">
    --node-labels=node.kubernetes.io/node= 


[Install]
WantedBy=multi-user.target
</span><span class="no">EOF

</span><span class="c"># 这是一个表示 Kubernetes Kubelet 服务的 systemd 单位文件示例。下面是对每个节（[Unit]、[Service]、[Install]）的详细解释：</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># </span>
<span class="c"># Description=Kubernetes Kubelet：指定了此单位文件对应的服务描述信息为 "Kubernetes Kubelet"。</span>
<span class="c"># Documentation=...：指定了对该服务的文档链接。</span>
<span class="c"># - After: 说明该服务在哪些其他服务之后启动，这里是在网络在线、firewalld服务和containerd服务后启动。</span>
<span class="c"># - Wants: 说明该服务想要的其他服务，这里是网络在线服务。</span>
<span class="c"># - Requires: 说明该服务需要的其他服务，这里是docker.socket和containerd.service。</span>
<span class="c"># [Service]</span>
<span class="c"># </span>
<span class="c"># ExecStart=/usr/local/bin/kubelet ...：指定了启动 Kubelet 服务的命令和参数。这里使用的是 /usr/local/bin/kubelet 命令，并传递了一系列参数来配置 Kubelet 的运行。这些参数包括：</span>
<span class="c"># --bootstrap-kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig：指定了用于引导 kubelet 的 kubeconfig 文件的路径和名称。</span>
<span class="c"># --kubeconfig=/etc/kubernetes/kubelet.kubeconfig：指定了 kubelet 的 kubeconfig 文件的路径和名称。</span>
<span class="c"># --config=/etc/kubernetes/kubelet-conf.yml：指定了 kubelet 的配置文件的路径和名称。</span>
<span class="c"># --container-runtime-endpoint=unix:///run/cri-dockerd.sock：指定了容器运行时接口的端点地址，这里使用的是 Docker 运行时（cri-dockerd）的 UNIX 套接字。</span>
<span class="c"># --node-labels=node.kubernetes.io/node=：指定了节点的标签。这里的示例只给节点添加了一个简单的标签 node.kubernetes.io/node=。</span>
<span class="c"># [Install]</span>
<span class="c"># </span>
<span class="c"># WantedBy=multi-user.target：指定了在 multi-user.target 被启动时，该服务应该被启用。</span>
<span class="c"># 通过这个单位文件，你可以配置 Kubelet 服务的启动参数，指定相关的配置文件和凭证文件，以及定义节点的标签。请确认路径和文件名与你的实际环境中的配置相匹配。</span>


<span class="c"># IPv6示例</span>
<span class="c"># 若不使用IPv6那么忽略此项即可</span>
<span class="c"># 下方 --node-ip 更换为每个节点的IP即可</span>
<span class="k">**</span><span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kubelet.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes Kubelet
Documentation=https://github.com/kubernetes/kubernetes
After=network-online.target firewalld.service cri-docker.service docker.socket containerd.service
Wants=network-online.target
Requires=docker.socket containerd.service

[Service]
ExecStart=/usr/local/bin/kubelet </span><span class="se">\\</span><span class="sh">
    --bootstrap-kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig  </span><span class="se">\\</span><span class="sh">
    --kubeconfig=/etc/kubernetes/kubelet.kubeconfig </span><span class="se">\\</span><span class="sh">
    --config=/etc/kubernetes/kubelet-conf.yml </span><span class="se">\\</span><span class="sh">
    --container-runtime-endpoint=unix:///run/cri-dockerd.sock  </span><span class="se">\\</span><span class="sh">
    --node-labels=node.kubernetes.io/node=   </span><span class="se">\\</span><span class="sh">
    --node-ip=192.168.1.31,fc00::31
[Install]
WantedBy=multi-user.target
EOF**
</span></code></pre></div></div>

<h3 id="822当使用containerd作为runtime">8.2.2当使用Containerd作为Runtime</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> /var/lib/kubelet /var/log/kubernetes /etc/systemd/system/kubelet.service.d /etc/kubernetes/manifests/

<span class="c"># 所有k8s节点配置kubelet service</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kubelet.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">

[Unit]
Description=Kubernetes Kubelet
Documentation=https://github.com/kubernetes/kubernetes
After=network-online.target firewalld.service containerd.service
Wants=network-online.target
Requires=containerd.service

[Service]
ExecStart=/usr/local/bin/kubelet </span><span class="se">\\</span><span class="sh">
    --bootstrap-kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig  </span><span class="se">\\</span><span class="sh">
    --kubeconfig=/etc/kubernetes/kubelet.kubeconfig </span><span class="se">\\</span><span class="sh">
    --config=/etc/kubernetes/kubelet-conf.yml </span><span class="se">\\</span><span class="sh">
    --container-runtime-endpoint=unix:///run/containerd/containerd.sock  </span><span class="se">\\</span><span class="sh">
    --node-labels=node.kubernetes.io/node=

[Install]
WantedBy=multi-user.target
</span><span class="no">EOF

</span><span class="c"># 这是一个表示 Kubernetes Kubelet 服务的 systemd 单位文件示例。与之前相比，添加了 After 和 Requires 字段来指定依赖关系。</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># </span>
<span class="c"># Description=Kubernetes Kubelet：指定了此单位文件对应的服务描述信息为 "Kubernetes Kubelet"。</span>
<span class="c"># Documentation=...：指定了对该服务的文档链接。</span>
<span class="c"># - After: 说明该服务在哪些其他服务之后启动，这里是在网络在线、firewalld服务和containerd服务后启动。</span>
<span class="c"># - Wants: 说明该服务想要的其他服务，这里是网络在线服务。</span>
<span class="c"># - Requires: 说明该服务需要的其他服务，这里是docker.socket和containerd.service。</span>
<span class="c"># [Service]</span>
<span class="c"># </span>
<span class="c"># ExecStart=/usr/local/bin/kubelet ...：指定了启动 Kubelet 服务的命令和参数，与之前的示例相同。</span>
<span class="c"># --container-runtime-endpoint=unix:///run/containerd/containerd.sock：修改了容器运行时接口的端点地址，将其更改为使用 containerd 运行时（通过 UNIX 套接字）。</span>
<span class="c"># [Install]</span>
<span class="c"># </span>
<span class="c"># WantedBy=multi-user.target：指定了在 multi-user.target 被启动时，该服务应该被启用。</span>
<span class="c"># 通过这个单位文件，你可以配置 Kubelet 服务的启动参数，并指定了它依赖的 containerd 服务。确保路径和文件名与你实际环境中的配置相匹配。</span>



<span class="c"># IPv6示例</span>
<span class="c"># 若不使用IPv6那么忽略此项即可</span>
<span class="c"># 下方 --node-ip 更换为每个节点的IP即可</span>
<span class="nb">cat</span> <span class="o">&gt;</span> /usr/lib/systemd/system/kubelet.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">

[Unit]
Description=Kubernetes Kubelet
Documentation=https://github.com/kubernetes/kubernetes
After=network-online.target firewalld.service containerd.service
Wants=network-online.target
Requires=containerd.service

[Service]
ExecStart=/usr/local/bin/kubelet </span><span class="se">\\</span><span class="sh">
    --bootstrap-kubeconfig=/etc/kubernetes/bootstrap-kubelet.kubeconfig  </span><span class="se">\\</span><span class="sh">
    --kubeconfig=/etc/kubernetes/kubelet.kubeconfig </span><span class="se">\\</span><span class="sh">
    --config=/etc/kubernetes/kubelet-conf.yml </span><span class="se">\\</span><span class="sh">
    --container-runtime-endpoint=unix:///run/containerd/containerd.sock  </span><span class="se">\\</span><span class="sh">
    --node-labels=node.kubernetes.io/node=  </span><span class="se">\\</span><span class="sh">
    --node-ip=192.168.1.31,fc00::31
[Install]
WantedBy=multi-user.target
</span><span class="no">EOF
</span></code></pre></div></div>

<h3 id="823所有k8s节点创建kubelet的配置文件">8.2.3所有k8s节点创建kubelet的配置文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /etc/kubernetes/kubelet-conf.yml <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
address: 0.0.0.0
port: 10250
readOnlyPort: 10255
authentication:
  anonymous:
    enabled: false
  webhook:
    cacheTTL: 2m0s
    enabled: true
  x509:
    clientCAFile: /etc/kubernetes/pki/ca.pem
authorization:
  mode: Webhook
  webhook:
    cacheAuthorizedTTL: 5m0s
    cacheUnauthorizedTTL: 30s
cgroupDriver: systemd
cgroupsPerQOS: true
clusterDNS:
- 10.96.0.10
clusterDomain: cluster.local
containerLogMaxFiles: 5
containerLogMaxSize: 10Mi
contentType: application/vnd.kubernetes.protobuf
cpuCFSQuota: true
cpuManagerPolicy: none
cpuManagerReconcilePeriod: 10s
enableControllerAttachDetach: true
enableDebuggingHandlers: true
enforceNodeAllocatable:
- pods
eventBurst: 10
eventRecordQPS: 5
evictionHard:
  imagefs.available: 15%
  memory.available: 100Mi
  nodefs.available: 10%
  nodefs.inodesFree: 5%
evictionPressureTransitionPeriod: 5m0s
failSwapOn: true
fileCheckFrequency: 20s
hairpinMode: promiscuous-bridge
healthzBindAddress: 127.0.0.1
healthzPort: 10248
httpCheckFrequency: 20s
imageGCHighThresholdPercent: 85
imageGCLowThresholdPercent: 80
imageMinimumGCAge: 2m0s
iptablesDropBit: 15
iptablesMasqueradeBit: 14
kubeAPIBurst: 10
kubeAPIQPS: 5
makeIPTablesUtilChains: true
maxOpenFiles: 1000000
maxPods: 110
nodeStatusUpdateFrequency: 10s
oomScoreAdj: -999
podPidsLimit: -1
registryBurst: 10
registryPullQPS: 5
resolvConf: /etc/resolv.conf
rotateCertificates: true
runtimeRequestTimeout: 2m0s
serializeImagePulls: true
staticPodPath: /etc/kubernetes/manifests
streamingConnectionIdleTimeout: 4h0m0s
syncFrequency: 1m0s
volumeStatsAggPeriod: 1m0s
</span><span class="no">EOF

</span><span class="c"># 这是一个Kubelet的配置文件，用于配置Kubelet的各项参数。</span>
<span class="c"># </span>
<span class="c"># - apiVersion: kubelet.config.k8s.io/v1beta1：指定了配置文件的API版本为kubelet.config.k8s.io/v1beta1。</span>
<span class="c"># - kind: KubeletConfiguration：指定了配置类别为KubeletConfiguration。</span>
<span class="c"># - address: 0.0.0.0：指定了Kubelet监听的地址为0.0.0.0。</span>
<span class="c"># - port: 10250：指定了Kubelet监听的端口为10250。</span>
<span class="c"># - readOnlyPort: 10255：指定了只读端口为10255，用于提供只读的状态信息。</span>
<span class="c"># - authentication：指定了认证相关的配置信息。</span>
<span class="c">#   - anonymous.enabled: false：禁用了匿名认证。</span>
<span class="c">#   - webhook.enabled: true：启用了Webhook认证。</span>
<span class="c">#   - x509.clientCAFile: /etc/kubernetes/pki/ca.pem：指定了X509证书的客户端CA文件路径。</span>
<span class="c"># - authorization：指定了授权相关的配置信息。</span>
<span class="c">#   - mode: Webhook：指定了授权模式为Webhook。</span>
<span class="c">#   - webhook.cacheAuthorizedTTL: 5m0s：指定了授权缓存时间段为5分钟。</span>
<span class="c">#   - webhook.cacheUnauthorizedTTL: 30s：指定了未授权缓存时间段为30秒。</span>
<span class="c"># - cgroupDriver: systemd：指定了Cgroup驱动为systemd。</span>
<span class="c"># - cgroupsPerQOS: true：启用了每个QoS类别一个Cgroup的设置。</span>
<span class="c"># - clusterDNS: 指定了集群的DNS服务器地址列表。</span>
<span class="c">#   - 10.96.0.10：指定了DNS服务器地址为10.96.0.10。</span>
<span class="c"># - clusterDomain: cluster.local：指定了集群的域名后缀为cluster.local。</span>
<span class="c"># - containerLogMaxFiles: 5：指定了容器日志文件保留的最大数量为5个。</span>
<span class="c"># - containerLogMaxSize: 10Mi：指定了容器日志文件的最大大小为10Mi。</span>
<span class="c"># - contentType: application/vnd.kubernetes.protobuf：指定了内容类型为protobuf。</span>
<span class="c"># - cpuCFSQuota: true：启用了CPU CFS Quota。</span>
<span class="c"># - cpuManagerPolicy: none：禁用了CPU Manager。</span>
<span class="c"># - cpuManagerReconcilePeriod: 10s：指定了CPU管理器的调整周期为10秒。</span>
<span class="c"># - enableControllerAttachDetach: true：启用了控制器的挂载和拆卸。</span>
<span class="c"># - enableDebuggingHandlers: true：启用了调试处理程序。</span>
<span class="c"># - enforceNodeAllocatable: 指定了强制节点可分配资源的列表。</span>
<span class="c">#   - pods：强制节点可分配pods资源。</span>
<span class="c"># - eventBurst: 10：指定了事件突发的最大数量为10。</span>
<span class="c"># - eventRecordQPS: 5：指定了事件记录的最大请求量为5。</span>
<span class="c"># - evictionHard: 指定了驱逐硬性限制参数的配置信息。</span>
<span class="c">#   - imagefs.available: 15%：指定了镜像文件系统可用空间的限制为15%。</span>
<span class="c">#   - memory.available: 100Mi：指定了可用内存的限制为100Mi。</span>
<span class="c">#   - nodefs.available: 10%：指定了节点文件系统可用空间的限制为10%。</span>
<span class="c">#   - nodefs.inodesFree: 5%：指定了节点文件系统可用inode的限制为5%。</span>
<span class="c"># - evictionPressureTransitionPeriod: 5m0s：指定了驱逐压力转换的时间段为5分钟。</span>
<span class="c"># - failSwapOn: true：指定了在发生OOM时禁用交换分区。</span>
<span class="c"># - fileCheckFrequency: 20s：指定了文件检查频率为20秒。</span>
<span class="c"># - hairpinMode: promiscuous-bridge：设置了Hairpin Mode为"promiscuous-bridge"。</span>
<span class="c"># - healthzBindAddress: 127.0.0.1：指定了健康检查的绑定地址为127.0.0.1。</span>
<span class="c"># - healthzPort: 10248：指定了健康检查的端口为10248。</span>
<span class="c"># - httpCheckFrequency: 20s：指定了HTTP检查的频率为20秒。</span>
<span class="c"># - imageGCHighThresholdPercent: 85：指定了镜像垃圾回收的上阈值为85%。</span>
<span class="c"># - imageGCLowThresholdPercent: 80：指定了镜像垃圾回收的下阈值为80%。</span>
<span class="c"># - imageMinimumGCAge: 2m0s：指定了镜像垃圾回收的最小时间为2分钟。</span>
<span class="c"># - iptablesDropBit: 15：指定了iptables的Drop Bit为15。</span>
<span class="c"># - iptablesMasqueradeBit: 14：指定了iptables的Masquerade Bit为14。</span>
<span class="c"># - kubeAPIBurst: 10：指定了KubeAPI的突发请求数量为10个。</span>
<span class="c"># - kubeAPIQPS: 5：指定了KubeAPI的每秒请求频率为5个。</span>
<span class="c"># - makeIPTablesUtilChains: true：指定了是否使用iptables工具链。</span>
<span class="c"># - maxOpenFiles: 1000000：指定了最大打开文件数为1000000。</span>
<span class="c"># - maxPods: 110：指定了最大的Pod数量为110。</span>
<span class="c"># - nodeStatusUpdateFrequency: 10s：指定了节点状态更新的频率为10秒。</span>
<span class="c"># - oomScoreAdj: -999：指定了OOM Score Adjustment为-999。</span>
<span class="c"># - podPidsLimit: -1：指定了Pod的PID限制为-1，表示无限制。</span>
<span class="c"># - registryBurst: 10：指定了Registry的突发请求数量为10个。</span>
<span class="c"># - registryPullQPS: 5：指定了Registry的每秒拉取请求数量为5个。</span>
<span class="c"># - resolvConf: /etc/resolv.conf：指定了resolv.conf的文件路径。</span>
<span class="c"># - rotateCertificates: true：指定了是否轮转证书。</span>
<span class="c"># - runtimeRequestTimeout: 2m0s：指定了运行时请求的超时时间为2分钟。</span>
<span class="c"># - serializeImagePulls: true：指定了是否序列化镜像拉取。</span>
<span class="c"># - staticPodPath: /etc/kubernetes/manifests：指定了静态Pod的路径。</span>
<span class="c"># - streamingConnectionIdleTimeout: 4h0m0s：指定了流式连接的空闲超时时间为4小时。</span>
<span class="c"># - syncFrequency: 1m0s：指定了同步频率为1分钟。</span>
<span class="c"># - volumeStatsAggPeriod: 1m0s：指定了卷统计聚合周期为1分钟。</span>
</code></pre></div></div>

<h3 id="824启动kubelet">8.2.4启动kubelet</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> kubelet.service
<span class="c"># 启用并立即启动kubelet.service单元。kubelet.service是kubelet守护进程的systemd服务单元。</span>

systemctl restart kubelet.service
<span class="c"># 重启kubelet.service单元，即重新启动kubelet守护进程。</span>

systemctl status kubelet.service
<span class="c"># kubelet.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h3 id="825查看集群">8.2.5查看集群</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 ~]# kubectl  get node
NAME           STATUS     ROLES    AGE   VERSION
k8s-master01   NotReady   &lt;none&gt;   15s   v1.35.0
k8s-master02   NotReady   &lt;none&gt;   14s   v1.35.0
k8s-master03   NotReady   &lt;none&gt;   12s   v1.35.0
k8s-node01     NotReady   &lt;none&gt;   11s   v1.35.0
k8s-node02     NotReady   &lt;none&gt;   9s    v1.35.0
<span class="o">[</span>root@k8s-master01 ~]# 

</code></pre></div></div>

<h3 id="826查看容器运行时">8.2.6查看容器运行时</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 ~]# kubectl describe node | <span class="nb">grep </span>Runtime
  Container Runtime Version:  containerd://2.2.1
  Container Runtime Version:  containerd://2.2.1
  Container Runtime Version:  containerd://2.2.1
  Container Runtime Version:  containerd://2.2.1
  Container Runtime Version:  containerd://2.2.1
<span class="o">[</span>root@k8s-master01 ~]# kubectl describe node | <span class="nb">grep </span>Runtime
  Container Runtime Version:  docker://29.1.3
  Container Runtime Version:  docker://29.1.3
  Container Runtime Version:  docker://29.1.3
  Container Runtime Version:  docker://29.1.3
  Container Runtime Version:  docker://29.1.3
<span class="o">[</span>root@k8s-master01 ~]# 
</code></pre></div></div>

<h2 id="83kube-proxy配置">8.3.kube-proxy配置</h2>

<h3 id="831将kubeconfig发送至其他节点">8.3.1将kubeconfig发送至其他节点</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># master-1执行</span>
<span class="k">for </span>NODE <span class="k">in </span>k8s-master02 k8s-master03 k8s-node01 k8s-node02<span class="p">;</span> <span class="k">do </span>scp /etc/kubernetes/kube-proxy.kubeconfig <span class="nv">$NODE</span>:/etc/kubernetes/kube-proxy.kubeconfig<span class="p">;</span> <span class="k">done</span>
</code></pre></div></div>

<h3 id="832所有k8s节点添加kube-proxy的service文件">8.3.2所有k8s节点添加kube-proxy的service文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span>  /usr/lib/systemd/system/kube-proxy.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes Kube Proxy
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-proxy </span><span class="se">\\</span><span class="sh">
  --config=/etc/kubernetes/kube-proxy.yaml </span><span class="se">\\</span><span class="sh">
  --cluster-cidr=172.16.0.0/12,fc00:2222::/112 </span><span class="se">\\</span><span class="sh">
  --v=2
Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
</span><span class="no">
EOF


</span><span class="c"># 关闭IPv6</span>
<span class="c"># 删除 --cluster-cidr 的IPv6地址</span>
<span class="nb">cat</span> <span class="o">&gt;</span>  /usr/lib/systemd/system/kube-proxy.service <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
[Unit]
Description=Kubernetes Kube Proxy
Documentation=https://github.com/kubernetes/kubernetes
After=network.target

[Service]
ExecStart=/usr/local/bin/kube-proxy </span><span class="se">\\</span><span class="sh">
  --config=/etc/kubernetes/kube-proxy.yaml </span><span class="se">\\</span><span class="sh">
  --cluster-cidr=172.16.0.0/12 </span><span class="se">\\</span><span class="sh">
  --v=2
Restart=always
RestartSec=10s

[Install]
WantedBy=multi-user.target
</span><span class="no">
EOF

</span><span class="c"># 这是一个 systemd 服务单元文件的示例，用于配置 Kubernetes Kube Proxy 服务。下面是对其中一些字段的详细解释：</span>
<span class="c"># </span>
<span class="c"># [Unit]</span>
<span class="c"># </span>
<span class="c"># Description: 描述了该服务单元的用途，这里是 Kubernetes Kube Proxy。</span>
<span class="c"># Documentation: 指定了该服务单元的文档地址，即 https://github.com/kubernetes/kubernetes。</span>
<span class="c"># After: 指定该服务单元应在 network.target（网络目标）之后启动。</span>
<span class="c"># [Service]</span>
<span class="c"># </span>
<span class="c"># ExecStart: 指定了启动 Kube Proxy 服务的命令。通过 /usr/local/bin/kube-proxy 命令启动，并指定了配置文件的路径为 /etc/kubernetes/kube-proxy.yaml，同时指定了日志级别为 2。</span>
<span class="c"># Restart: 配置了服务在失败或退出后自动重启。</span>
<span class="c"># RestartSec: 配置了重启间隔，这里是每次重启之间的等待时间为 10 秒。</span>
<span class="c"># [Install]</span>
<span class="c"># </span>
<span class="c"># WantedBy: 指定了该服务单元的安装目标为 multi-user.target（多用户目标），表示该服务将在多用户模式下启动。</span>
<span class="c"># 通过配置这些字段，你可以启动和管理 Kubernetes Kube Proxy 服务。请注意，你需要根据实际情况修改 ExecStart 中的路径和文件名，确保与你的环境一致。另外，可以根据需求修改其他字段的值，以满足你的特定要求。</span>
</code></pre></div></div>

<h3 id="833所有k8s节点添加kube-proxy的配置">8.3.3所有k8s节点添加kube-proxy的配置</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> /etc/kubernetes/kube-proxy.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: kubeproxy.config.k8s.io/v1alpha1
bindAddress: 0.0.0.0
clientConnection:
  acceptContentTypes: ""
  burst: 10
  contentType: application/vnd.kubernetes.protobuf
  kubeconfig: /etc/kubernetes/kube-proxy.kubeconfig
  qps: 5
clusterCIDR: 172.16.0.0/12,fc00:2222::/112
configSyncPeriod: 15m0s
conntrack:
  max: null
  maxPerCore: 32768
  min: 131072
  tcpCloseWaitTimeout: 1h0m0s
  tcpEstablishedTimeout: 24h0m0s
enableProfiling: false
healthzBindAddress: 0.0.0.0:10256
hostnameOverride: ""
iptables:
  masqueradeAll: false
  masqueradeBit: 14
  minSyncPeriod: 0s
  syncPeriod: 30s
ipvs:
  masqueradeAll: true
  minSyncPeriod: 5s
  scheduler: "rr"
  syncPeriod: 30s
kind: KubeProxyConfiguration
metricsBindAddress: 127.0.0.1:10249
mode: "ipvs"
nodePortAddresses: null
oomScoreAdj: -999
portRange: ""
udpIdleTimeout: 250ms
</span><span class="no">EOF

</span><span class="c"># 这是一个Kubernetes的kube-proxy组件配置文件示例。以下是每个配置项的详细解释：</span>
<span class="c"># 1. apiVersion: kubeproxy.config.k8s.io/v1alpha1</span>
<span class="c">#    - 指定该配置文件的API版本。</span>
<span class="c"># 2. bindAddress: 0.0.0.0</span>
<span class="c">#    - 指定kube-proxy使用的监听地址。0.0.0.0表示监听所有网络接口。</span>
<span class="c"># 3. clientConnection:</span>
<span class="c">#    - 客户端连接配置项。</span>
<span class="c">#    a. acceptContentTypes: ""</span>
<span class="c">#       - 指定接受的内容类型。</span>
<span class="c">#    b. burst: 10</span>
<span class="c">#       - 客户端请求超出qps设置时的最大突发请求数。</span>
<span class="c">#    c. contentType: application/vnd.kubernetes.protobuf</span>
<span class="c">#       - 指定客户端请求的内容类型。</span>
<span class="c">#    d. kubeconfig: /etc/kubernetes/kube-proxy.kubeconfig</span>
<span class="c">#       - kube-proxy使用的kubeconfig文件路径。</span>
<span class="c">#    e. qps: 5</span>
<span class="c">#       - 每秒向API服务器发送的请求数量。</span>
<span class="c"># 4. clusterCIDR: 172.16.0.0/12,fc00:2222::/112</span>
<span class="c">#    - 指定集群使用的CIDR范围，用于自动分配Pod IP。</span>
<span class="c"># 5. configSyncPeriod: 15m0s</span>
<span class="c">#    - 指定kube-proxy配置同步到节点的频率。</span>
<span class="c"># 6. conntrack:</span>
<span class="c">#    - 连接跟踪设置。</span>
<span class="c">#    a. max: null</span>
<span class="c">#       - 指定连接跟踪的最大值。</span>
<span class="c">#    b. maxPerCore: 32768</span>
<span class="c">#       - 指定每个核心的最大连接跟踪数.</span>
<span class="c">#    c. min: 131072</span>
<span class="c">#       - 指定最小的连接跟踪数。</span>
<span class="c">#    d. tcpCloseWaitTimeout: 1h0m0s</span>
<span class="c">#       - 指定处于CLOSE_WAIT状态的TCP连接的超时时间。</span>
<span class="c">#    e. tcpEstablishedTimeout: 24h0m0s</span>
<span class="c">#       - 指定已建立的TCP连接的超时时间。</span>
<span class="c"># 7. enableProfiling: false</span>
<span class="c">#    - 是否启用性能分析。</span>
<span class="c"># 8. healthzBindAddress: 0.0.0.0:10256</span>
<span class="c">#    - 指定健康检查监听地址和端口。</span>
<span class="c"># 9. hostnameOverride: ""</span>
<span class="c">#    - 指定覆盖默认主机名的值。</span>
<span class="c"># 10. iptables:</span>
<span class="c">#     - iptables设置。</span>
<span class="c">#     a. masqueradeAll: false</span>
<span class="c">#        - 是否对所有流量使用IP伪装。</span>
<span class="c">#     b. masqueradeBit: 14</span>
<span class="c">#        - 指定伪装的Bit标记。</span>
<span class="c">#     c. minSyncPeriod: 0s</span>
<span class="c">#        - 指定同步iptables规则的最小间隔。</span>
<span class="c">#     d. syncPeriod: 30s</span>
<span class="c">#        - 指定同步iptables规则的时间间隔。</span>
<span class="c"># 11. ipvs:</span>
<span class="c">#     - ipvs设置。</span>
<span class="c">#     a. masqueradeAll: true</span>
<span class="c">#        - 是否对所有流量使用IP伪装。</span>
<span class="c">#     b. minSyncPeriod: 5s</span>
<span class="c">#        - 指定同步ipvs规则的最小间隔。</span>
<span class="c">#     c. scheduler: "rr"</span>
<span class="c">#        - 指定ipvs默认使用的调度算法。</span>
<span class="c">#     d. syncPeriod: 30s</span>
<span class="c">#        - 指定同步ipvs规则的时间间隔。</span>
<span class="c"># 12. kind: KubeProxyConfiguration</span>
<span class="c">#     - 指定该配置文件的类型。</span>
<span class="c"># 13. metricsBindAddress: 127.0.0.1:10249</span>
<span class="c">#     - 指定指标绑定的地址和端口。</span>
<span class="c"># 14. mode: "ipvs"</span>
<span class="c">#     - 指定kube-proxy的模式。这里指定为ipvs，使用IPVS代理模式。</span>
<span class="c"># 15. nodePortAddresses: null</span>
<span class="c">#     - 指定可用于NodePort的网络地址。</span>
<span class="c"># 16. oomScoreAdj: -999</span>
<span class="c">#     - 指定kube-proxy的OOM优先级。</span>
<span class="c"># 17. portRange: ""</span>
<span class="c">#     - 指定可用于服务端口范围。</span>
<span class="c"># 18. udpIdleTimeout: 250ms</span>
<span class="c">#     - 指定UDP连接的空闲超时时间。</span>
</code></pre></div></div>

<h3 id="834启动kube-proxy">8.3.4启动kube-proxy</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code> systemctl daemon-reload
<span class="c"># 用于重新加载systemd管理的单位文件。当你新增或修改了某个单位文件（如.service文件、.socket文件等），需要运行该命令来刷新systemd对该文件的配置。</span>

systemctl <span class="nb">enable</span> <span class="nt">--now</span> kube-proxy.service
<span class="c"># 启用并立即启动kube-proxy.service单元。kube-proxy.service是kube-proxy守护进程的systemd服务单元。</span>

systemctl restart kube-proxy.service
<span class="c"># 重启kube-proxy.service单元，即重新启动kube-proxy守护进程。</span>

systemctl status kube-proxy.service
<span class="c"># kube-proxy.service单元的当前状态，包括运行状态、是否启用等信息。</span>
</code></pre></div></div>

<h1 id="9安装网络插件">9.安装网络插件</h1>

<p><strong>注意 9.1 和 9.2 二选其一即可，建议在此处创建好快照后在进行操作，后续出问题可以回滚</strong></p>

<p>** centos7 要升级libseccomp 不然 无法安装网络插件**</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># https://github.com/opencontainers/runc/releases</span>
<span class="c"># 升级runc</span>
<span class="c"># wget https://github.com/opencontainers/runc/releases/download/v1.1.12/runc.amd64</span>

<span class="nb">install</span> <span class="nt">-m</span> 755 runc.amd64 /usr/local/sbin/runc
<span class="nb">cp</span> <span class="nt">-p</span> /usr/local/sbin/runc  /usr/local/bin/runc
<span class="nb">cp</span> <span class="nt">-p</span> /usr/local/sbin/runc  /usr/bin/runc

<span class="c">#查看当前版本</span>
<span class="o">[</span>root@k8s-master-1 ~]# rpm <span class="nt">-qa</span> | <span class="nb">grep </span>libseccomp
libseccomp-2.5.2-2.el9.x86_64

<span class="c">#下载高于2.4以上的包</span>
<span class="c"># yum -y install http://rpmfind.net/linux/centos/8-stream/BaseOS/x86_64/os/Packages/libseccomp-2.5.1-1.el8.x86_64.rpm</span>
<span class="c"># 清华源</span>
<span class="c"># yum -y install https://mirrors.tuna.tsinghua.edu.cn/centos/8-stream/BaseOS/x86_64/os/Packages/libseccomp-2.5.1-1.el8.x86_64.rpm</span>
</code></pre></div></div>

<h2 id="91安装calico">9.1安装Calico</h2>

<h3 id="911更改calico网段">9.1.1更改calico网段</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="c"># 安装operator</span>
kubectl create <span class="nt">-f</span> https://raw.githubusercontent.com/projectcalico/calico/v3.31.2/manifests/tigera-operator.yaml


<span class="c"># 下载配置文件</span>
curl https://raw.githubusercontent.com/projectcalico/calico/v3.31.2/manifests/custom-resources.yaml <span class="nt">-O</span>




<span class="c"># 修改地址池</span>
vim custom-resources.yaml
apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    ipPools:
    - name: default-ipv4-ippool
      blockSize: 26
      cidr: 172.16.0.0/12
      encapsulation: VXLANCrossSubnet
      natOutgoing: Enabled
      nodeSelector: all<span class="o">()</span>




<span class="c"># 修改地址池</span>
vim custom-resources.yaml
apiVersion: operator.tigera.io/v1
kind: Installation
metadata:
  name: default
spec:
  calicoNetwork:
    ipPools:
    - name: ipv4-ippool
      cidr: 172.16.0.0/12
      blockSize: 26
      encapsulation: IPIP
      natOutgoing: Enabled
      nodeSelector: all<span class="o">()</span>
    - name: ipv6-ippool
      cidr: <span class="s2">"fd00:100::/64"</span>
      blockSize: 122
      encapsulation: None
      natOutgoing: Enabled
      nodeSelector: all<span class="o">()</span>
    nodeAddressAutodetectionV4:
      interface: <span class="s2">"eth.*|en.*"</span>
    nodeAddressAutodetectionV6:
      interface: <span class="s2">"eth.*|en.*"</span>

<span class="c"># 执行安装</span>
kubectl create <span class="nt">-f</span> custom-resources.yaml



<span class="c"># 安装客户端</span>
curl <span class="nt">-L</span> https://github.com/projectcalico/calico/releases/download/v3.30.3/calicoctl-linux-amd64 <span class="nt">-o</span> calicoctl


<span class="c"># 给客户端添加执行权限</span>
<span class="nb">chmod</span> +x ./calicoctl


<span class="c"># 查看集群节点</span>
./calicoctl get nodes <span class="nt">--allow-version-mismatch</span>
<span class="c"># 查看集群节点状态</span>
./calicoctl node status <span class="nt">--allow-version-mismatch</span>
<span class="c">#查看地址池</span>
./calicoctl get ipPool <span class="nt">--allow-version-mismatch</span>
./calicoctl get ipPool <span class="nt">--allow-version-mismatch</span> <span class="nt">-o</span> yaml


</code></pre></div></div>

<h3 id="912查看容器状态">9.1.2查看容器状态</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># calico 初始化会很慢 需要耐心等待一下，大约十分钟左右</span>
<span class="o">[</span>root@k8s-master01 kubernetes-v1.35.0]# kubectl get pod <span class="nt">-A</span>
NAMESPACE         NAME                                       READY   STATUS    RESTARTS   AGE
calico-system     calico-apiserver-8944cd746-8cf9n           1/1     Running   0          3m17s
calico-system     calico-apiserver-8944cd746-kdvx4           1/1     Running   0          3m17s
calico-system     calico-kube-controllers-757dc6b956-bpx2m   1/1     Running   0          3m16s
calico-system     calico-node-89xvc                          1/1     Running   0          3m16s
calico-system     calico-node-95dgk                          1/1     Running   0          3m16s
calico-system     calico-node-jbm6g                          1/1     Running   0          3m16s
calico-system     calico-node-p5mb2                          1/1     Running   0          3m16s
calico-system     calico-node-t68xn                          1/1     Running   0          3m16s
calico-system     calico-typha-68687d94bd-7pcmq              1/1     Running   0          3m14s
calico-system     calico-typha-68687d94bd-cfclr              1/1     Running   0          3m16s
calico-system     calico-typha-68687d94bd-t62kd              1/1     Running   0          3m14s
calico-system     csi-node-driver-5mwsv                      2/2     Running   0          3m16s
calico-system     csi-node-driver-jtp56                      2/2     Running   0          3m16s
calico-system     csi-node-driver-ncd7q                      2/2     Running   0          3m16s
calico-system     csi-node-driver-nvb6d                      2/2     Running   0          3m16s
calico-system     csi-node-driver-s6dk9                      2/2     Running   0          3m16s
calico-system     goldmane-576ddf78d8-4zkch                  1/1     Running   0          3m17s
calico-system     whisker-58576c6584-nv246                   2/2     Running   0          2m43s
tigera-operator   tigera-operator-5cd7bd49c7-rwb54           1/1     Running   0          3m34s
<span class="o">[</span>root@k8s-master01 kubernetes-v1.35.0]# 


</code></pre></div></div>

<h2 id="92-安装cilium">9.2 安装cilium</h2>

<h3 id="921-安装helm">9.2.1 安装helm</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># [root@k8s-master01 ~]# curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3</span>
<span class="c"># [root@k8s-master01 ~]# chmod 700 get_helm.sh</span>
<span class="c"># [root@k8s-master01 ~]# ./get_helm.sh</span>

<span class="c"># wget https://get.helm.sh/helm-v4.0.4-linux-amd64.tar.gz</span>
<span class="nb">tar </span>xvf helm-<span class="k">*</span><span class="nt">-linux-amd64</span>.tar.gz
<span class="nb">cp </span>linux-amd64/helm /usr/local/bin/
</code></pre></div></div>

<h3 id="922-安装cilium">9.2.2 安装cilium</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 添加源</span>
helm repo add cilium https://helm.cilium.io

<span class="c"># 修改为国内源</span>
helm pull cilium/cilium
<span class="nb">tar </span>xvf cilium-<span class="k">*</span>.tgz
<span class="nb">cd </span>cilium/

<span class="c"># sed -i "s#quay.io/#quay.m.daocloud.io/#g" values.yaml</span>

<span class="c"># 默认参数安装</span>
helm <span class="nb">install  </span>cilium ./cilium/ <span class="nt">-n</span> kube-system

helm <span class="nb">install </span>cilium cilium/cilium <span class="nt">--version</span> 1.17.4 <span class="nt">-n</span> kube-system

<span class="c"># 启用ipv6</span>
<span class="c"># helm install cilium cilium/cilium -n cilium-system --set ipv6.enabled=true </span>


<span class="c"># 启用路由信息和监控插件</span>
<span class="c"># helm install cilium ./cilium/ --namespace kube-system --set ipv6.enabled=true --set hubble.relay.enabled=true --set hubble.ui.enabled=true --set prometheus.enabled=true --set operator.prometheus.enabled=true --set hubble.enabled=true --set hubble.metrics.enabled="{dns,drop,tcp,flow,port-distribution,icmp,http}" </span>
</code></pre></div></div>

<h3 id="923-查看">9.2.3 查看</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 ~]# kubectl  get pod <span class="nt">-A</span> | <span class="nb">grep </span>cil
NAMESPACE     NAME                               READY   STATUS    RESTARTS   AGE
kube-system   cilium-2tnfb                       1/1     Running   0          60s
kube-system   cilium-5tgcb                       1/1     Running   0          60s
kube-system   cilium-6shf5                       1/1     Running   0          60s
kube-system   cilium-ccbcx                       1/1     Running   0          60s
kube-system   cilium-cppft                       1/1     Running   0          60s
kube-system   cilium-operator-675f685d59-7q27q   1/1     Running   0          60s
kube-system   cilium-operator-675f685d59-kwmqz   1/1     Running   0          60s
<span class="o">[</span>root@k8s-master01 ~]#
</code></pre></div></div>

<h3 id="924-下载专属监控面板">9.2.4 下载专属监控面板</h3>

<p>安装时候没有创建 监控可以忽略</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 yaml]# wget https://raw.githubusercontent.com/cilium/cilium/1.12.1/examples/kubernetes/addons/prometheus/monitoring-example.yaml

<span class="o">[</span>root@k8s-master01 yaml]# <span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#docker.io/#jockerhub.com/#g"</span> monitoring-example.yaml

<span class="o">[</span>root@k8s-master01 yaml]# kubectl  apply <span class="nt">-f</span> monitoring-example.yaml
namespace/cilium-monitoring created
serviceaccount/prometheus-k8s created
configmap/grafana-config created
configmap/grafana-cilium-dashboard created
configmap/grafana-cilium-operator-dashboard created
configmap/grafana-hubble-dashboard created
configmap/prometheus created
clusterrole.rbac.authorization.k8s.io/prometheus created
clusterrolebinding.rbac.authorization.k8s.io/prometheus created
service/grafana created
service/prometheus created
deployment.apps/grafana created
deployment.apps/prometheus created
<span class="o">[</span>root@k8s-master01 yaml]#
</code></pre></div></div>

<h3 id="925-修改为nodeport">9.2.5 修改为NodePort</h3>

<p>安装时候没有创建 监控可以忽略</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 yaml]# kubectl  edit svc  <span class="nt">-n</span> kube-system hubble-ui
service/hubble-ui edited
<span class="o">[</span>root@k8s-master01 yaml]#
<span class="o">[</span>root@k8s-master01 yaml]# kubectl  edit svc  <span class="nt">-n</span> cilium-monitoring grafana
service/grafana edited
<span class="o">[</span>root@k8s-master01 yaml]#
<span class="o">[</span>root@k8s-master01 yaml]# kubectl  edit svc  <span class="nt">-n</span> cilium-monitoring prometheus
service/prometheus edited
<span class="o">[</span>root@k8s-master01 yaml]#

<span class="nb">type</span>: NodePort
</code></pre></div></div>

<h3 id="926-查看端口">9.2.6 查看端口</h3>

<p>安装时候没有创建 监控可以忽略</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 yaml]# kubectl get svc <span class="nt">-A</span> | <span class="nb">grep </span>NodePort
cilium-monitoring   grafana          NodePort    10.111.74.3      &lt;none&gt;        3000:32648/TCP   74s
cilium-monitoring   prometheus       NodePort    10.107.240.124   &lt;none&gt;        9090:30495/TCP   74s
kube-system         hubble-ui        NodePort    10.96.185.26     &lt;none&gt;        80:31568/TCP     99s
</code></pre></div></div>

<h3 id="927-访问">9.2.7 访问</h3>

<p>安装时候没有创建 监控可以忽略</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://192.168.1.31:32648
http://192.168.1.31:30495
http://192.168.1.31:31568
</code></pre></div></div>

<h1 id="10安装coredns">10.安装CoreDNS</h1>

<h2 id="101以下步骤只在master01操作">10.1以下步骤只在master01操作</h2>

<h3 id="1011修改文件">10.1.1修改文件</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载tgz包</span>
helm repo add coredns https://coredns.github.io/helm
helm pull coredns/coredns
<span class="nb">tar </span>xvf coredns-<span class="k">*</span>.tgz
<span class="nb">cd </span>coredns/

<span class="c"># 修改IP地址</span>
vim values.yaml
<span class="nb">cat </span>values.yaml | <span class="nb">grep </span>clusterIP:
clusterIP: <span class="s2">"10.96.0.10"</span>

<span class="c"># 示例</span>
<span class="nt">---</span>
service:
<span class="c"># clusterIP: ""</span>
<span class="c"># clusterIPs: []</span>
<span class="c"># loadBalancerIP: ""</span>
<span class="c"># externalIPs: []</span>
<span class="c"># externalTrafficPolicy: ""</span>
<span class="c"># ipFamilyPolicy: ""</span>
  <span class="c"># The name of the Service</span>
  <span class="c"># If not set, a name is generated using the fullname template</span>
  clusterIP: <span class="s2">"10.96.0.10"</span>
  name: <span class="s2">""</span>
  annotations: <span class="o">{}</span>
<span class="nt">---</span>

<span class="c"># 修改为国内源</span>
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#registry.k8s.io/#k8s.m.daocloud.io/#g"</span> values.yaml

<span class="c"># 默认参数安装</span>
helm <span class="nb">install  </span>coredns ./coredns/ <span class="nt">-n</span> kube-system
</code></pre></div></div>

<h1 id="11安装metrics-server">11.安装Metrics Server</h1>

<h2 id="111以下步骤只在master01操作">11.1以下步骤只在master01操作</h2>

<h3 id="1111安装metrics-server">11.1.1安装Metrics-server</h3>

<p>在新版的Kubernetes中系统资源的采集均使用Metrics-server，可以通过Metrics采集节点和Pod的内存、磁盘、CPU和网络的使用率</p>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载 </span>
wget https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml <span class="nt">-O</span> metrics-server.yaml 

<span class="c"># 修改配置</span>
vim metrics-server.yaml 

<span class="nt">---</span>
<span class="c"># 1</span>
    - args:
        - <span class="nt">--cert-dir</span><span class="o">=</span>/tmp
        - <span class="nt">--kubelet-preferred-address-types</span><span class="o">=</span>InternalIP,ExternalIP,Hostname
        - <span class="nt">--kubelet-use-node-status-port</span>
        - <span class="nt">--metric-resolution</span><span class="o">=</span>15s
        - <span class="nt">--kubelet-insecure-tls</span>
        - <span class="nt">--requestheader-client-ca-file</span><span class="o">=</span>/etc/kubernetes/pki/front-proxy-ca.pem
        - <span class="nt">--requestheader-username-headers</span><span class="o">=</span>X-Remote-User
        - <span class="nt">--requestheader-group-headers</span><span class="o">=</span>X-Remote-Group
        - <span class="nt">--requestheader-extra-headers-prefix</span><span class="o">=</span>X-Remote-Extra-

<span class="c"># 2</span>
        volumeMounts:
        - mountPath: /tmp
          name: tmp-dir
        - name: ca-ssl
          mountPath: /etc/kubernetes/pki

<span class="c"># 3</span>
      volumes:
      - emptyDir: <span class="o">{}</span>
        name: tmp-dir
      - name: ca-ssl
        hostPath:
          path: /etc/kubernetes/pki
<span class="nt">---</span>


<span class="c"># 修改为国内源 docker源可选</span>
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#registry.k8s.io/#k8s.m.daocloud.io/#g"</span> metrics-server.yaml

<span class="c"># 执行部署</span>
kubectl apply <span class="nt">-f</span> metrics-server.yaml 
</code></pre></div></div>

<h3 id="1112稍等片刻查看状态">11.1.2稍等片刻查看状态</h3>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl  top node
NAME           CPU<span class="o">(</span>cores<span class="o">)</span>   CPU<span class="o">(</span>%<span class="o">)</span>   MEMORY<span class="o">(</span>bytes<span class="o">)</span>   MEMORY<span class="o">(</span>%<span class="o">)</span>   
k8s-master01   182m         2%       2059Mi          58%         
k8s-master02   104m         1%       1577Mi          44%         
k8s-master03   113m         1%       1516Mi          42%         
k8s-node01     53m          0%       915Mi           25%         
k8s-node02     66m          0%       930Mi           26%         
</code></pre></div></div>

<h1 id="12集群验证">12.集群验证</h1>

<h2 id="121部署pod资源">12.1部署pod资源</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span><span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | kubectl apply -f -
apiVersion: v1
kind: Pod
metadata:
  name: busybox
  namespace: default
spec:
  containers:
  - name: busybox
    image: docker.m.daocloud.io/library/busybox:1.28
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
  restartPolicy: Always
</span><span class="no">EOF

</span><span class="c"># 查看</span>
kubectl  get pod
NAME      READY   STATUS    RESTARTS   AGE
busybox   1/1     Running   0          17s
</code></pre></div></div>

<h2 id="122用pod解析默认命名空间中的kubernetes">12.2用pod解析默认命名空间中的kubernetes</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 查看name</span>
kubectl get svc
NAME         TYPE        CLUSTER-IP   EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>   AGE
kubernetes   ClusterIP   10.96.0.1    &lt;none&gt;        443/TCP   17h

<span class="c"># 进行解析</span>
kubectl <span class="nb">exec  </span>busybox <span class="nt">-n</span> default <span class="nt">--</span> nslookup kubernetes
3Server:    10.96.0.10
Address 1: 10.96.0.10 kube-dns.kube-system.svc.cluster.local

Name:      kubernetes
Address 1: 10.96.0.1 kubernetes.default.svc.cluster.local
</code></pre></div></div>

<h2 id="123测试跨命名空间是否可以解析">12.3测试跨命名空间是否可以解析</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 查看有那些name</span>
kubectl  get svc <span class="nt">-A</span>
NAMESPACE     NAME              TYPE        CLUSTER-IP      EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>         AGE
default       kubernetes        ClusterIP   10.96.0.1       &lt;none&gt;        443/TCP         76m
kube-system   calico-typha      ClusterIP   10.105.100.82   &lt;none&gt;        5473/TCP        35m
kube-system   coredns-coredns   ClusterIP   10.96.0.10      &lt;none&gt;        53/UDP,53/TCP   8m14s
kube-system   metrics-server    ClusterIP   10.105.60.31    &lt;none&gt;        443/TCP         109s

<span class="c"># 进行解析</span>
kubectl <span class="nb">exec  </span>busybox <span class="nt">-n</span> default <span class="nt">--</span> nslookup coredns.kube-system
Server:    10.96.0.10
Address 1: 10.96.0.10 coredns-coredns.kube-system.svc.cluster.local

Name:      coredns-coredns.kube-system
Address 1: 10.96.0.10 coredns-coredns.kube-system.svc.cluster.local
<span class="o">[</span>root@k8s-master01 metrics-server]# 
</code></pre></div></div>

<h2 id="124每个节点都必须要能访问kubernetes的kubernetes-svc-443和kube-dns的service-53">12.4每个节点都必须要能访问Kubernetes的kubernetes svc 443和kube-dns的service 53</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>telnet 10.96.0.1 443
Trying 10.96.0.1...
Connected to 10.96.0.1.
Escape character is <span class="s1">'^]'</span><span class="nb">.</span>

telnet 10.96.0.10 53
Trying 10.96.0.10...
Connected to 10.96.0.10.
Escape character is <span class="s1">'^]'</span><span class="nb">.</span>

curl 10.96.0.10:53
curl: <span class="o">(</span>52<span class="o">)</span> Empty reply from server
</code></pre></div></div>

<h2 id="125pod和pod之前要能通">12.5Pod和Pod之前要能通</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl get po <span class="nt">-owide</span>
NAME      READY   STATUS    RESTARTS   AGE   IP              NODE         NOMINATED NODE   READINESS GATES
busybox   1/1     Running   0          17m   172.27.14.193   k8s-node02   &lt;none&gt;           &lt;none&gt;

kubectl get po <span class="nt">-n</span> kube-system <span class="nt">-owide</span>
NAME                                       READY   STATUS    RESTARTS   AGE     IP               NODE           NOMINATED NODE   READINESS GATES
calico-kube-controllers-76754ff848-pw4xg   1/1     Running   0          38m     172.25.244.193   k8s-master01   &lt;none&gt;           &lt;none&gt;
calico-node-97m55                          1/1     Running   0          38m     192.168.1.34     k8s-node01     &lt;none&gt;           &lt;none&gt;
calico-node-hlz7j                          1/1     Running   0          38m     192.168.1.32     k8s-master02   &lt;none&gt;           &lt;none&gt;
calico-node-jtlck                          1/1     Running   0          38m     192.168.1.33     k8s-master03   &lt;none&gt;           &lt;none&gt;
calico-node-lxfkf                          1/1     Running   0          38m     192.168.1.35     k8s-node02     &lt;none&gt;           &lt;none&gt;
calico-node-t667x                          1/1     Running   0          38m     192.168.1.31     k8s-master01   &lt;none&gt;           &lt;none&gt;
calico-typha-59d75c5dd4-gbhfp              1/1     Running   0          38m     192.168.1.35     k8s-node02     &lt;none&gt;           &lt;none&gt;
coredns-coredns-c5c6d4d9b-bd829            1/1     Running   0          10m     172.25.92.65     k8s-master02   &lt;none&gt;           &lt;none&gt;
metrics-server-7c8b55c754-w7q8v            1/1     Running   0          3m56s   172.17.125.3     k8s-node01     &lt;none&gt;           &lt;none&gt;

<span class="c"># 进入busybox ping其他节点上的pod</span>

kubectl <span class="nb">exec</span> <span class="nt">-ti</span> busybox <span class="nt">--</span> sh
/ <span class="c"># ping 192.168.1.34</span>
PING 192.168.1.34 <span class="o">(</span>192.168.1.34<span class="o">)</span>: 56 data bytes
64 bytes from 192.168.1.34: <span class="nb">seq</span><span class="o">=</span>0 <span class="nv">ttl</span><span class="o">=</span>63 <span class="nb">time</span><span class="o">=</span>0.358 ms
64 bytes from 192.168.1.34: <span class="nb">seq</span><span class="o">=</span>1 <span class="nv">ttl</span><span class="o">=</span>63 <span class="nb">time</span><span class="o">=</span>0.668 ms
64 bytes from 192.168.1.34: <span class="nb">seq</span><span class="o">=</span>2 <span class="nv">ttl</span><span class="o">=</span>63 <span class="nb">time</span><span class="o">=</span>0.637 ms
64 bytes from 192.168.1.34: <span class="nb">seq</span><span class="o">=</span>3 <span class="nv">ttl</span><span class="o">=</span>63 <span class="nb">time</span><span class="o">=</span>0.624 ms
64 bytes from 192.168.1.34: <span class="nb">seq</span><span class="o">=</span>4 <span class="nv">ttl</span><span class="o">=</span>63 <span class="nb">time</span><span class="o">=</span>0.907 ms

<span class="c"># 可以连通证明这个pod是可以跨命名空间和跨主机通信的</span>
</code></pre></div></div>

<h2 id="126创建三个副本可以看到3个副本分布在不同的节点上用完可以删了">12.6创建三个副本，可以看到3个副本分布在不同的节点上（用完可以删了）</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span><span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | kubectl apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
</span><span class="no">EOF

</span>kubectl  get pod 
NAME                               READY   STATUS    RESTARTS   AGE
busybox                            1/1     Running   0          6m25s
nginx-deployment-9456bbbf9-4bmvk   1/1     Running   0          8s
nginx-deployment-9456bbbf9-9rcdk   1/1     Running   0          8s
nginx-deployment-9456bbbf9-dqv8s   1/1     Running   0          8s

<span class="c"># 删除nginx</span>
<span class="o">[</span>root@k8s-master01 ~]# kubectl delete deployments nginx-deployment 
</code></pre></div></div>

<h1 id="13安装dashboard">13.安装dashboard</h1>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 添加源信息</span>
helm repo add kubernetes-dashboard https://kubernetes.github.io/dashboard/


<span class="c"># 修改为国内源</span>
helm pull kubernetes-dashboard/kubernetes-dashboard
<span class="nb">tar </span>xvf kubernetes-dashboard-<span class="k">*</span>.tgz
<span class="nb">cd </span>kubernetes-dashboard
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#docker.io/#jockerhub.com/#g"</span> values.yaml

<span class="c"># 默认参数安装</span>
helm upgrade <span class="nt">--install</span> kubernetes-dashboard ./kubernetes-dashboard/  <span class="nt">--create-namespace</span> <span class="nt">--namespace</span> kube-system


<span class="c"># 我的集群使用默认参数安装 kubernetes-dashboard-kong 出现异常 8444 端口占用</span>
<span class="c"># 使用下面的命令进行安装，在安装时关闭kong.tls功能</span>
helm upgrade <span class="nt">--install</span> kubernetes-dashboard ./kubernetes-dashboard/ <span class="nt">--namespace</span> kube-system <span class="nt">--set</span> kong.admin.tls.enabled<span class="o">=</span><span class="nb">false</span>
</code></pre></div></div>

<h2 id="131更改dashboard的svc为nodeport如果已是请忽略">13.1更改dashboard的svc为NodePort，如果已是请忽略</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>kubectl edit svc  <span class="nt">-n</span> kube-system kubernetes-dashboard-kong-proxy
  <span class="nb">type</span>: NodePort
</code></pre></div></div>

<h2 id="132查看端口号">13.2查看端口号</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 ~]# kubectl get svc kubernetes-dashboard-kong-proxy <span class="nt">-n</span> kube-system
NAME                              TYPE       CLUSTER-IP     EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>         AGE
kubernetes-dashboard-kong-proxy   NodePort   10.100.9.217   &lt;none&gt;        443:31330/TCP   42s
<span class="o">[</span>root@k8s-master01 ~]# 
</code></pre></div></div>

<h2 id="133创建token">13.3创建token</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> dashboard-user.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-user
  namespace: kube-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: admin-user
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: admin-user
  namespace: kube-system
</span><span class="no">EOF

</span>kubectl  apply <span class="nt">-f</span> dashboard-user.yaml

<span class="c"># 创建token</span>
kubectl <span class="nt">-n</span> kube-system create token admin-user
eyJhbGciOiJSUzI1NiIsImtpZCI6ImtxelI3UzBZOFl5MGtWU1k0c2QyUVRaOXhQeEFNYTNHWlNyRUFucHBIVGMifQ..oDH1uZAWnxeq7gun0ej0ihbSJa6YYZwXcDt9O_tHQsWRi9Lr4kcusG0sm6UntEAy7fHLf6UWLTNHtv-V2lvDjUxRM46xQUzDOQ2v5hBI9NnpcRXQRvMvwn0s6wFeGNr2aFZ9i9aeSJZTdqHOlBjrqxsJtYD6fqDGtJYGdbvUA50nvXPN25a9ofgKvEtWjMA50ojGCr3yqF3vrWruEV77suFeYyIye5YXMnJw_I7Cp3zSEwrAuqfyVAlRv8mYQKxZJsNyban2-wcV10V5qzkofSL4DkgBaqqTDh1aOeD2NJrjmLKPQUt8meLohYy5tMGoyZgx0NGzr-9R71KF8tFHoA
</code></pre></div></div>

<h2 id="134创建长期token">13.4创建长期token</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> dashboard-user-token.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: v1
kind: Secret
metadata:
  name: admin-user
  namespace: kube-system
  annotations:
    kubernetes.io/service-account.name: "admin-user"   
type: kubernetes.io/service-account-token  
</span><span class="no">EOF

</span>kubectl  apply <span class="nt">-f</span> dashboard-user-token.yaml

<span class="c"># 查看密码</span>
kubectl get secret admin-user <span class="nt">-n</span> kube-system <span class="nt">-o</span> <span class="nv">jsonpath</span><span class="o">={</span><span class="s2">".data.token"</span><span class="o">}</span> | <span class="nb">base64</span> <span class="nt">-d</span>

eyJhbGciOiJSUzI1NiIsImtpZCI6ImtxelI3UzBZOFl5MGtWU1k0c2QyUVRaOXhQeEFNYTNHWlNyRUFucHBIVGMifQ..etS6ehzcPjtdipt4zm3L2FJpcbYFlJGLGXmrd8dLMGDw2xVU5XQgZM2gMrHa_rrJPnRKcksaG7qNpasM574D7__CpcktduTM_EsUiOc5i7u9lHDMxsPczyiFxlOp7d8jOdNbqwnlEPA5iYWJ6P3Cj3LiDfFKKHQChKgv8mS588Eh-nnYiMsblWltqvPd_DOJ9JxJ4G4T25yWsbEgpH3uZmXUAyVUDNgDFLNIKRPQXLg5d07rlmgHjJInvEsnrrCs8dxp173iNW8LYxnDNy7pwjrXbvniYiRXjltAwc6F68G590hQaogy9AEVUskeTgBvONPJwkZRHbf0beOL30pT0w
</code></pre></div></div>

<h2 id="135登录dashboard">13.5登录dashboard</h2>

<p>https://192.168.1.31:31268/</p>

<h1 id="14部署metallb">14.部署metallb</h1>

<h2 id="141-下载部署">14.1 下载部署</h2>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 下载应用包</span>
wget https://raw.githubusercontent.com/metallb/metallb/v0.15.2/config/manifests/metallb-native.yaml

<span class="c"># 修改镜像地址</span>
<span class="c"># 自行找代理</span>
<span class="nb">sed</span> <span class="nt">-i</span> <span class="s2">"s#quay.io#quay.chenby.cn#g"</span> metallb-native.yaml 
<span class="nb">cat </span>metallb-native.yaml | <span class="nb">grep </span>image
        image: quay.chenby.cn/metallb/controller:v0.14.5
        image: quay.chenby.cn/metallb/speaker:v0.14.5
        
<span class="c"># 执行部署</span>
kubectl apply <span class="nt">-f</span> metallb-native.yaml

<span class="c"># 可以连接国际网络</span>
<span class="c"># kubectl apply -f https://raw.githubusercontent.com/metallb/metallb/v0.15.2/config/manifests/metallb-native.yaml</span>
</code></pre></div></div>
<h2 id="142-查看运行情况">14.2 查看运行情况</h2>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>root@k8s-master01 kubernetes-v1.35.0]# kubectl <span class="nt">-n</span> metallb-system get all 
NAME                              READY   STATUS              RESTARTS   AGE
pod/controller-59f49888d7-chwst   1/1     Running             0          54s
pod/speaker-4nkv6                 0/1     Running             0          54s
pod/speaker-dzfzs                 0/1     ErrImagePull        0          53s
pod/speaker-fmwgn                 0/1     ContainerCreating   0          53s
pod/speaker-jch8z                 0/1     Running             0          53s
pod/speaker-pplvk                 0/1     Running             0          53s

NAME                              TYPE        CLUSTER-IP     EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>   AGE
service/metallb-webhook-service   ClusterIP   10.100.70.55   &lt;none&gt;        443/TCP   54s

NAME                     DESIRED   CURRENT   READY   UP-TO-DATE   AVAILABLE   NODE SELECTOR            AGE
daemonset.apps/speaker   5         5         0       5            0           kubernetes.io/os<span class="o">=</span>linux   54s

NAME                         READY   UP-TO-DATE   AVAILABLE   AGE
deployment.apps/controller   1/1     1            1           54s

NAME                                    DESIRED   CURRENT   READY   AGE
replicaset.apps/controller-59f49888d7   1         1         1       54s
<span class="o">[</span>root@k8s-master01 kubernetes-v1.35.0]# 


</code></pre></div></div>

<h2 id="143-配置vip的资源池">14.3 配置VIP的资源池</h2>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 新版本metallb使用了CR（Custom Resources），这里我们通过IPAddressPool的CR，进行地址池的定义。</span>
<span class="c"># 如果实例中不设置IPAddressPool选择器L2Advertisement；那么L2Advertisement默认为该实例所有的IPAddressPool相关联。</span>

<span class="nb">cat</span> <span class="o">&gt;</span> metallb-config-ipaddresspool.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: first-pool
  namespace: metallb-system
spec:
  addresses:
  - 192.168.1.71-192.168.1.75
</span><span class="no">EOF

</span><span class="c"># 进行L2关联地址池的绑定。</span>

<span class="nb">cat</span> <span class="o">&gt;</span> metallb-config-L2Advertisement.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: example
  namespace: metallb-system
spec:
  ipAddressPools:
  - first-pool
</span><span class="no">EOF

</span><span class="c"># 执行部署</span>
kubectl apply <span class="nt">-f</span> metallb-config-ipaddresspool.yaml
kubectl apply <span class="nt">-f</span> metallb-config-L2Advertisement.yaml
</code></pre></div></div>

<h1 id="15ingress安装">15.ingress安装</h1>

<h2 id="151执行部署">15.1执行部署</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 添加仓库</span>
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo update

<span class="c"># 拉取仓库</span>
helm pull ingress-nginx/ingress-nginx
<span class="nb">tar </span>xvf ingress-nginx-4.14.1.tgz

<span class="c"># 查看镜像地址，若你的环境无法下载这个镜像，那么就需要你自行找镜像 或者更换镜像地址</span>
<span class="o">[</span>root@k8s-master01 ingress-nginx]# <span class="nb">cat  </span>values.yaml | <span class="nb">grep </span>image | <span class="nb">grep</span> <span class="nt">-v</span> <span class="se">\#</span>
  image:
  image:
    image: ingress-nginx/controller
      image:
        image: ingress-nginx/kube-webhook-certgen
  image:
    image: defaultbackend-amd64
imagePullSecrets: <span class="o">[]</span>
<span class="o">[</span>root@k8s-master01 ingress-nginx]# 

<span class="c"># 我这里的环境可以直接拉取 我直接进行了安装</span>
helm <span class="nb">install </span>ingress-nginx ingress-nginx/ingress-nginx <span class="se">\</span>
  <span class="nt">--namespace</span> ingress <span class="nt">--create-namespace</span>

<span class="c"># 查看完成安装</span>
<span class="o">[</span>root@k8s-master01 ~]# kubectl get all <span class="nt">-n</span> ingress
NAME                                            READY   STATUS    RESTARTS   AGE
pod/ingress-nginx-controller-6996967cbb-fplzt   1/1     Running   0          56s

NAME                                         TYPE           CLUSTER-IP       EXTERNAL-IP    PORT<span class="o">(</span>S<span class="o">)</span>                      AGE
service/ingress-nginx-controller             LoadBalancer   10.109.63.223    192.168.1.71   80:32046/TCP,443:31512/TCP   56s
service/ingress-nginx-controller-admission   ClusterIP      10.110.227.191   &lt;none&gt;         443/TCP                      56s

NAME                                       READY   UP-TO-DATE   AVAILABLE   AGE
deployment.apps/ingress-nginx-controller   1/1     1            1           56s

NAME                                                  DESIRED   CURRENT   READY   AGE
replicaset.apps/ingress-nginx-controller-6996967cbb   1         1         1       56s
<span class="o">[</span>root@k8s-master01 ~]# 
</code></pre></div></div>
<h2 id="152创建测试应用">15.2创建测试应用</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cat</span> <span class="o">&gt;</span> ingress-demo-app.yaml <span class="o">&lt;&lt;</span> <span class="no">EOF</span><span class="sh">
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-server
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-server
  template:
    metadata:
      labels:
        app: hello-server
    spec:
      containers:
      - name: hello-server
        image: registry.cn-hangzhou.aliyuncs.com/lfy_k8s_images/hello-server
        ports:
        - containerPort: 9000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: nginx-demo
  name: nginx-demo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx-demo
  template:
    metadata:
      labels:
        app: nginx-demo
    spec:
      containers:
      - image: nginx
        name: nginx
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-demo
  name: nginx-demo
spec:
  selector:
    app: nginx-demo
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello-server
  name: hello-server
spec:
  selector:
    app: hello-server
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 9000
</span><span class="no">EOF

</span><span class="nb">cat</span> <span class="o">&gt;&gt;</span> ingress-demo-app-ingress.yaml <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: networking.k8s.io/v1
kind: Ingress  
metadata:
  name: ingress-host-bar
spec:
  ingressClassName: nginx
  rules:
  - host: "hello.chenby.cn"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: hello-server
            port:
              number: 8000
  - host: "demo.chenby.cn"
    http:
      paths:
      - pathType: Prefix
        path: "/"  
        backend:
          service:
            name: nginx-demo
            port:
              number: 8000
</span><span class="no">EOF

</span><span class="c"># 等创建完成后在执行：</span>
kubectl  apply <span class="nt">-f</span> ingress-demo-app.yaml 
kubectl  apply <span class="nt">-f</span> ingress-demo-app-ingress.yaml 

kubectl  get ingress
NAME               CLASS   HOSTS                            ADDRESS        PORTS   AGE
ingress-host-bar   nginx   hello.chenby.cn,demo.chenby.cn   192.168.1.71   80      22s

</code></pre></div></div>
<h2 id="153测试访问">15.3测试访问</h2>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 写入hosts</span>
<span class="nb">cat</span> <span class="o">&gt;&gt;</span> /etc/hosts <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
192.168.1.71 hello.chenby.cn
192.168.1.71 demo.chenby.cn
</span><span class="no">EOF


</span><span class="c"># 访问 hello 正常</span>
<span class="o">[</span>root@localhost ~]# curl hello.chenby.cn
Hello World![root@localhost ~]# 
<span class="o">[</span>root@localhost ~]# 

<span class="c"># # 访问 demo 正常</span>
<span class="o">[</span>root@localhost ~]# curl demo.chenby.cn 
&lt;<span class="o">!</span>DOCTYPE html&gt;
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
&lt;style&gt;
html <span class="o">{</span> color-scheme: light dark<span class="p">;</span> <span class="o">}</span>
body <span class="o">{</span> width: 35em<span class="p">;</span> margin: 0 auto<span class="p">;</span>
font-family: Tahoma, Verdana, Arial, sans-serif<span class="p">;</span> <span class="o">}</span>
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;h1&gt;Welcome to nginx!&lt;/h1&gt;
&lt;p&gt;If you see this page, the nginx web server is successfully installed and
working. Further configuration is required.&lt;/p&gt;

&lt;p&gt;For online documentation and support please refer to
&lt;a <span class="nv">href</span><span class="o">=</span><span class="s2">"http://nginx.org/"</span><span class="o">&gt;</span>nginx.org&lt;/a&gt;.&lt;br/&gt;
Commercial support is available at
&lt;a <span class="nv">href</span><span class="o">=</span><span class="s2">"http://nginx.com/"</span><span class="o">&gt;</span>nginx.com&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;em&gt;Thank you <span class="k">for </span>using nginx.&lt;/em&gt;&lt;/p&gt;
&lt;/body&gt;
&lt;/html&gt;
<span class="o">[</span>root@localhost ~]#
</code></pre></div></div>

<h2 id="154测试路径重写">15.4测试路径重写</h2>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 删除刚建的ingress</span>

kubectl  delete <span class="nt">-f</span> ingress-demo-app-ingress.yaml 

<span class="c"># 写入新的ingress</span>
<span class="nb">cat</span> <span class="o">&gt;&gt;</span> ingress-demo-app-ingress-router.yaml <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: networking.k8s.io/v1
kind: Ingress  
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /</span><span class="nv">$2</span><span class="sh">
  name: ingress-host-bar
spec:
  ingressClassName: nginx
  rules:
  - host: "hello.chenby.cn"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: hello-server
            port:
              number: 8000
  - host: "demo.chenby.cn"
    http:
      paths:
      - pathType: Prefix
        path: "/nginx(/|</span><span class="nv">$)</span><span class="sh">(.*)" 
        pathType: ImplementationSpecific
        backend:
          service:
            name: nginx-demo  
            port:
              number: 8000
</span><span class="no">EOF

</span><span class="c"># 执行部署</span>
kubectl  apply <span class="nt">-f</span> ingress-demo-app-ingress-router.yaml 


<span class="c"># 访问 hello 返回正常 </span>
<span class="o">[</span>root@localhost ~]# curl hello.chenby.cn
Hello World![root@localhost ~]# 
<span class="o">[</span>root@localhost ~]# 

<span class="c"># 访问 demo 访问异常</span>
<span class="o">[</span>root@localhost ~]# curl demo.chenby.cn 
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>&lt;title&gt;404 Not Found&lt;/title&gt;&lt;/head&gt;
&lt;body&gt;
&lt;center&gt;&lt;h1&gt;404 Not Found&lt;/h1&gt;&lt;/center&gt;
&lt;hr&gt;&lt;center&gt;nginx&lt;/center&gt;
&lt;/body&gt;
&lt;/html&gt;
<span class="o">[</span>root@localhost ~]# 

<span class="c"># 访问时加上URI 访问正常</span>
<span class="o">[</span>root@localhost ~]# curl demo.chenby.cn/nginx
&lt;<span class="o">!</span>DOCTYPE html&gt;
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>
&lt;title&gt;Welcome to nginx!&lt;/title&gt;
&lt;style&gt;
html <span class="o">{</span> color-scheme: light dark<span class="p">;</span> <span class="o">}</span>
body <span class="o">{</span> width: 35em<span class="p">;</span> margin: 0 auto<span class="p">;</span>
font-family: Tahoma, Verdana, Arial, sans-serif<span class="p">;</span> <span class="o">}</span>
&lt;/style&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;h1&gt;Welcome to nginx!&lt;/h1&gt;
&lt;p&gt;If you see this page, the nginx web server is successfully installed and
working. Further configuration is required.&lt;/p&gt;

&lt;p&gt;For online documentation and support please refer to
&lt;a <span class="nv">href</span><span class="o">=</span><span class="s2">"http://nginx.org/"</span><span class="o">&gt;</span>nginx.org&lt;/a&gt;.&lt;br/&gt;
Commercial support is available at
&lt;a <span class="nv">href</span><span class="o">=</span><span class="s2">"http://nginx.com/"</span><span class="o">&gt;</span>nginx.com&lt;/a&gt;.&lt;/p&gt;

&lt;p&gt;&lt;em&gt;Thank you <span class="k">for </span>using nginx.&lt;/em&gt;&lt;/p&gt;
&lt;/body&gt;
&lt;/html&gt;
<span class="o">[</span>root@localhost ~]# 
</code></pre></div></div>

<h2 id="155测试流量限制">15.5测试流量限制</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="c"># 删除刚建的ingress</span>

kubectl  delete <span class="nt">-f</span> ingress-demo-app-router.yaml 

<span class="c"># 写入新的ingress</span>
<span class="nb">cat</span> <span class="o">&gt;&gt;</span> ingress-demo-app-ingress-limit.yaml <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-limit-rate
  annotations:
    nginx.ingress.kubernetes.io/limit-rps: "1"
spec:
  ingressClassName: nginx
  rules:
  - host: "hello.chenby.cn"
    http:
      paths:
      - pathType: Exact
        path: "/"
        backend:
          service:
            name: hello-server
            port:
              number: 8000
  - host: "demo.chenby.cn"
    http:
      paths:
      - pathType: Prefix
        path: "/nginx(/|</span><span class="nv">$)</span><span class="sh">(.*)" 
        pathType: ImplementationSpecific
        backend:
          service:
            name: nginx-demo  
            port:
              number: 8000
</span><span class="no">EOF

</span><span class="c"># 执行部署</span>
kubectl  apply <span class="nt">-f</span> ingress-demo-app-ingress-limit.yaml 


<span class="c"># 访问过快 会返回 503</span>
<span class="o">[</span>root@localhost ~]# <span class="k">for </span>i <span class="k">in</span> <span class="o">{</span>1..8<span class="o">}</span><span class="p">;</span> <span class="k">do </span>curl hello.chenby.cn<span class="p">;</span> <span class="k">done
</span>Hello World!Hello World!Hello World!Hello World!Hello World!Hello World!&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>&lt;title&gt;503 Service Temporarily Unavailable&lt;/title&gt;&lt;/head&gt;
&lt;body&gt;
&lt;center&gt;&lt;h1&gt;503 Service Temporarily Unavailable&lt;/h1&gt;&lt;/center&gt;
&lt;hr&gt;&lt;center&gt;nginx&lt;/center&gt;
&lt;/body&gt;
&lt;/html&gt;
&lt;html&gt;
&lt;<span class="nb">head</span><span class="o">&gt;</span>&lt;title&gt;503 Service Temporarily Unavailable&lt;/title&gt;&lt;/head&gt;
&lt;body&gt;
&lt;center&gt;&lt;h1&gt;503 Service Temporarily Unavailable&lt;/h1&gt;&lt;/center&gt;
&lt;hr&gt;&lt;center&gt;nginx&lt;/center&gt;
&lt;/body&gt;
&lt;/html&gt;
<span class="o">[</span>root@localhost ~]# 

</code></pre></div></div>
<h2 id="156过滤查看ingress端口">15.6过滤查看ingress端口</h2>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 上面有安装metallb组件，所有ingress有自动获取到地址，我这里就不修改nodeport了</span>
<span class="c"># 各位如果没有metallb组件 就需要修改 nodeport</span>


<span class="c"># 修改为nodeport</span>
kubectl edit svc <span class="nt">-n</span> ingress-nginx   ingress-nginx-controller
<span class="nb">type</span>: NodePort

<span class="o">[</span>root@hello ~/yaml]# kubectl  get svc <span class="nt">-A</span> | <span class="nb">grep </span>ingress
ingress-nginx      ingress-nginx-controller               LoadBalancer   10.110.161.30    192.168.1.71   80:32480/TCP,443:30195/TCP   12m
ingress-nginx      ingress-nginx-controller-admission     ClusterIP      10.105.211.217   &lt;none&gt;         443/TCP                      12m
<span class="o">[</span>root@hello ~/yaml]#

</code></pre></div></div>

<h1 id="16ipv6测试">16.IPv6测试</h1>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#部署应用</span>

<span class="nb">cat</span><span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh"> | kubectl apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: chenby
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chenby
  template:
    metadata:
      labels:
        app: chenby
    spec:
      hostNetwork: true
      containers:
      - name: chenby
        image: docker.io/library/nginx
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: chenby
spec:
  ipFamilyPolicy: PreferDualStack
  ipFamilies:
  - IPv6
  - IPv4
  type: NodePort
  selector:
    app: chenby
  ports:
  - port: 80
    targetPort: 80
</span><span class="no">EOF


</span><span class="c">#查看端口</span>
<span class="o">[</span>root@k8s-master01 ~]# kubectl  get svc
NAME           TYPE        CLUSTER-IP        EXTERNAL-IP   PORT<span class="o">(</span>S<span class="o">)</span>        AGE
chenby         NodePort    fd00:1111::7ef8   &lt;none&gt;        80:32288/TCP   5s

<span class="o">[</span>root@k8s-master01 ~]# 

<span class="c"># 直接访问POD地址</span>
<span class="o">[</span>root@k8s-master01 ~]# curl <span class="nt">-I</span> http://[fd00:1111::7ef8]
HTTP/1.1 200 OK
Server: nginx/1.29.1
Date: Sun, 31 Aug 2025 04:14:38 GMT
Content-Type: text/html
Content-Length: 615
Last-Modified: Wed, 13 Aug 2025 14:33:41 GMT
Connection: keep-alive
ETag: <span class="s2">"689ca245-267"</span>
Accept-Ranges: bytes



<span class="c"># 使用IPv4地址访问测试</span>
<span class="o">[</span>root@k8s-master01 ~]# curl <span class="nt">-I</span> http://192.168.1.31:32651
HTTP/1.1 200 OK
Server: nginx/1.29.1
Date: Sun, 31 Aug 2025 04:15:05 GMT
Content-Type: text/html
Content-Length: 615
Last-Modified: Wed, 13 Aug 2025 14:33:41 GMT
Connection: keep-alive
ETag: <span class="s2">"689ca245-267"</span>
Accept-Ranges: bytes


<span class="c"># 使用主机的内网IPv6地址测试</span>
<span class="o">[</span>root@k8s-master01 ~]# curl <span class="nt">-I</span> http://[fc00::31]:32651
HTTP/1.1 200 OK
Server: nginx/1.29.1
Date: Sun, 31 Aug 2025 04:15:23 GMT
Content-Type: text/html
Content-Length: 615
Last-Modified: Wed, 13 Aug 2025 14:33:41 GMT
Connection: keep-alive
ETag: <span class="s2">"689ca245-267"</span>
Accept-Ranges: bytes


<span class="c"># 使用主机的公网IPv6地址测试</span>
<span class="o">[</span>root@k8s-master01 ~]# curl <span class="nt">-I</span> http://[2409:8a10:6d0:f1::ed1]:32651
HTTP/1.1 200 OK
Server: nginx/1.29.1
Date: Sun, 31 Aug 2025 04:16:16 GMT
Content-Type: text/html
Content-Length: 615
Last-Modified: Wed, 13 Aug 2025 14:33:41 GMT
Connection: keep-alive
ETag: <span class="s2">"689ca245-267"</span>
Accept-Ranges: bytes


</code></pre></div></div>

<h1 id="17安装nfs动态存储">17.安装NFS动态存储</h1>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 安装nfs</span>
<span class="c"># CentOS</span>
yum <span class="nb">install</span> <span class="nt">-y</span> nfs-utils

<span class="c"># Ubuntu</span>
apt <span class="nb">install </span>nfs-kernel-server nfs-common

<span class="c"># 创建共享</span>
<span class="nb">mkdir</span> /nfs

<span class="c"># 编辑共享</span>
vim /etc/exports
/nfs <span class="k">*</span><span class="o">(</span>rw,sync,no_root_squash,no_subtree_check<span class="o">)</span>


<span class="c"># centos </span>
systemctl <span class="nb">enable </span>rpcbind.service
systemctl <span class="nb">enable </span>nfs-server.service
systemctl restart rpcbind.service
systemctl restart nfs-server.service

<span class="c"># ubuntu</span>
systemctl <span class="nb">enable </span>nfs-kernel-server
systemctl restart nfs-kernel-server

<span class="c"># 加载配置</span>
exportfs <span class="nt">-a</span>
exportfs <span class="nt">-r</span>
exportfs


<span class="c"># 添加仓库</span>
helm repo add nfs-subdir-external-provisioner https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner/
<span class="c"># --set nfs.server 填写你的nfs服务器地址</span>
<span class="c"># --set nfs.path 填写你的nfs服务器路径</span>
helm <span class="nb">install</span> <span class="nt">-n</span> kube-system nfs-subdir-external-provisioner nfs-subdir-external-provisioner/nfs-subdir-external-provisioner <span class="se">\</span>
    <span class="nt">--set</span> storageClass.defaultClass<span class="o">=</span><span class="nb">true</span> <span class="se">\</span>
    <span class="nt">--set</span> nfs.server<span class="o">=</span>192.168.1.31 <span class="se">\</span>
    <span class="nt">--set</span> nfs.path<span class="o">=</span>/nfs

<span class="c"># 查看是否为默认存储</span>
<span class="o">[</span>root@k8s-master01 ~]# kubectl get storageclasses.storage.k8s.io
NAME                   PROVISIONER                                     RECLAIMPOLICY   VOLUMEBINDINGMODE   ALLOWVOLUMEEXPANSION   AGE
nfs-client <span class="o">(</span>default<span class="o">)</span>   cluster.local/nfs-subdir-external-provisioner   Delete          Immediate           <span class="nb">true                   </span>123m
<span class="o">[</span>root@k8s-master01 ~]# 

</code></pre></div></div>
<h1 id="171-测试">17.1 测试</h1>
<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 创建PVC需求</span>
<span class="nb">cat</span> <span class="o">&gt;&gt;</span> pvc.yaml <span class="o">&lt;&lt;</span><span class="no">EOF</span><span class="sh">
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: nginx-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 200Mi
</span><span class="no">EOF
</span><span class="c"># 执行部署</span>
kubectl apply <span class="nt">-f</span> pvc.yaml


<span class="c"># 查看pvc</span>
<span class="o">[</span>root@k8s-master-node1 ~/yaml]# kubectl get pvc
NAME        STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
nginx-pvc   Bound    pvc-8a4b6065-904a-4bae-bef9-1f3b5612986c   200Mi      RWX            nfs-storage    4s
<span class="o">[</span>root@k8s-master-node1 ~/yaml]#

<span class="c"># 查看pv</span>
<span class="o">[</span>root@k8s-master-node1 ~/yaml]# kubectl  get pv
NAME                                       CAPACITY   ACCESS MODES   RECLAIM POLICY   STATUS   CLAIM               STORAGECLASS   REASON   AGE
pvc-8a4b6065-904a-4bae-bef9-1f3b5612986c   200Mi      RWX            Delete           Bound    default/nginx-pvc   nfs-storage             103s
<span class="o">[</span>root@k8s-master-node1 ~/yaml]#
</code></pre></div></div>

<h1 id="18污点">18.污点</h1>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 查看当前污点状态</span>
<span class="o">[</span>root@k8s-master01 ~]# kubectl describe node  | <span class="nb">grep </span>Taints
Taints:             &lt;none&gt;
Taints:             &lt;none&gt;
Taints:             &lt;none&gt;
Taints:             &lt;none&gt;
Taints:             &lt;none&gt;

<span class="c"># 设置污点 禁止调度 同时进行驱赶现有的POD</span>
kubectl taint nodes k8s-master01 <span class="nv">key1</span><span class="o">=</span>value1:NoExecute
kubectl taint nodes k8s-master02 <span class="nv">key1</span><span class="o">=</span>value1:NoExecute
kubectl taint nodes k8s-master03 <span class="nv">key1</span><span class="o">=</span>value1:NoExecute

<span class="c"># 取消污点</span>
kubectl taint nodes k8s-master01 <span class="nv">key1</span><span class="o">=</span>value1:NoExecute-
kubectl taint nodes k8s-master02 <span class="nv">key1</span><span class="o">=</span>value1:NoExecute-
kubectl taint nodes k8s-master03 <span class="nv">key1</span><span class="o">=</span>value1:NoExecute-

<span class="c"># 设置污点 禁止调度 不进行驱赶现有的POD</span>
kubectl taint nodes k8s-master01 <span class="nv">key1</span><span class="o">=</span>value1:NoSchedule
kubectl taint nodes k8s-master02 <span class="nv">key1</span><span class="o">=</span>value1:NoSchedule
kubectl taint nodes k8s-master03 <span class="nv">key1</span><span class="o">=</span>value1:NoSchedule

<span class="c"># 取消污点</span>
kubectl taint nodes k8s-master01 <span class="nv">key1</span><span class="o">=</span>value1:NoSchedule-
kubectl taint nodes k8s-master02 <span class="nv">key1</span><span class="o">=</span>value1:NoSchedule-
kubectl taint nodes k8s-master03 <span class="nv">key1</span><span class="o">=</span>value1:NoSchedule-

</code></pre></div></div>

<h1 id="19安装命令行自动补全功能">19.安装命令行自动补全功能</h1>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>yum <span class="nb">install </span>bash-completion <span class="nt">-y</span>
<span class="nb">source</span> /usr/share/bash-completion/bash_completion
<span class="nb">source</span> &lt;<span class="o">(</span>kubectl completion bash<span class="o">)</span>
<span class="nb">echo</span> <span class="s2">"source &lt;(kubectl completion bash)"</span> <span class="o">&gt;&gt;</span> ~/.bashrc
</code></pre></div></div>

<h1 id="附录">附录</h1>

<div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 镜像加速器可以使用DaoCloud仓库，替换规则如下</span>
<span class="c"># docker.io 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull docker.io/nginx:latest
  docker pull docker.io/calico/node:v3.28.0
  <span class="c"># 镜像地址：</span>
  docker pull docker.chenby.cn/library/nginx:latest
  docker pull docker.chenby.cn/calico/node:v3.28.0

<span class="c"># docker.elastic.co 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull docker.elastic.co/apm/apm-server:8.14.0
  <span class="c"># 镜像地址：</span>
  docker pull elastic.chenby.cn/apm/apm-server:8.14.0
  <span class="c"># 阿里云地址：</span>
  docker pull registry.aliyuncs.com/chenby/apm-server:8.14.0

<span class="c"># quay.io 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull quay.io/ceph/ceph:v18.2.1
  <span class="c"># 镜像地址：</span>
  docker pull quay.chenby.cn/ceph/ceph:v18.2.1
  <span class="c"># 阿里云地址：</span>
  docker pull registry.aliyuncs.com/chenby/ceph:v18.2.1

<span class="c"># k8s.gcr.io 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull k8s.gcr.io/kube-state-metrics/kube-state-metrics:v2.8.2
  <span class="c"># 镜像地址：</span>
  docker pull k8s.chenby.cn/kube-state-metrics/kube-state-metrics:v2.8.2
  <span class="c"># 阿里云地址：</span>
  docker pull registry.aliyuncs.com/chenby/kube-state-metrics:v2.8.2

<span class="c"># registry.k8s.io 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull registry.k8s.io/sig-storage/nfsplugin:v4.2.0
  <span class="c"># 镜像地址：</span>
  docker pull k8s.chenby.cn/sig-storage/nfsplugin:v4.2.0
  <span class="c"># 阿里云地址：</span>
  docker pull registry.aliyuncs.com/chenby/nfsplugin:v4.2.0

<span class="c"># gcr.io 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull gcr.io/kaniko-project/executor:v1.23.1
  <span class="c"># 镜像地址：</span>
  docker pull gcr.chenby.cn/kaniko-project/executor:v1.23.1
  <span class="c"># 阿里云地址：</span>
  docker pull registry.aliyuncs.com/chenby/executor:v1.23.1

<span class="c"># ghcr.io 仓库：</span>
  <span class="c"># 官方地址：</span>
  docker pull ghcr.io/coroot/coroot:1.1.0
  <span class="c"># 镜像地址：</span>
  docker pull ghcr.chenby.cn/coroot/coroot:1.1.0
  <span class="c"># 阿里云地址：</span>
  docker pull registry.aliyuncs.com/chenby/coroot:1.1.0




<span class="c"># 镜像版本要自行查看，因为镜像版本是随时更新的，文档无法做到实时更新</span>

<span class="c"># docker pull 镜像</span>

docker pull registry.cn-hangzhou.aliyuncs.com/chenby/cni:master 
docker pull registry.cn-hangzhou.aliyuncs.com/chenby/node:master
docker pull registry.cn-hangzhou.aliyuncs.com/chenby/kube-controllers:master
docker pull registry.cn-hangzhou.aliyuncs.com/chenby/typha:master
docker pull registry.cn-hangzhou.aliyuncs.com/chenby/coredns:v1.10.0
docker pull registry.cn-hangzhou.aliyuncs.com/chenby/pause:3.6
docker pull registry.cn-hangzhou.aliyuncs.com/chenby/metrics-server:v0.5.2
docker pull kubernetesui/dashboard:v2.7.0
docker pull kubernetesui/metrics-scraper:v1.0.8
docker pull quay.io/cilium/cilium:v1.12.6
docker pull quay.io/cilium/certgen:v0.1.8
docker pull quay.io/cilium/hubble-relay:v1.12.6
docker pull quay.io/cilium/hubble-ui-backend:v0.9.2
docker pull quay.io/cilium/hubble-ui:v0.9.2
docker pull quay.io/cilium/cilium-etcd-operator:v2.0.7
docker pull quay.io/cilium/operator:v1.12.6
docker pull quay.io/cilium/clustermesh-apiserver:v1.12.6
docker pull quay.io/coreos/etcd:v3.5.4
docker pull quay.io/cilium/startup-script:d69851597ea019af980891a4628fb36b7880ec26

<span class="c"># docker 保存镜像</span>
docker save registry.cn-hangzhou.aliyuncs.com/chenby/cni:master <span class="nt">-o</span> cni.tar 
docker save registry.cn-hangzhou.aliyuncs.com/chenby/node:master <span class="nt">-o</span> node.tar 
docker save registry.cn-hangzhou.aliyuncs.com/chenby/typha:master <span class="nt">-o</span> typha.tar 
docker save registry.cn-hangzhou.aliyuncs.com/chenby/kube-controllers:master <span class="nt">-o</span> kube-controllers.tar 
docker save registry.cn-hangzhou.aliyuncs.com/chenby/coredns:v1.10.0 <span class="nt">-o</span> coredns.tar 
docker save registry.cn-hangzhou.aliyuncs.com/chenby/pause:3.6 <span class="nt">-o</span> pause.tar 
docker save registry.cn-hangzhou.aliyuncs.com/chenby/metrics-server:v0.5.2 <span class="nt">-o</span> metrics-server.tar 
docker save kubernetesui/dashboard:v2.7.0 <span class="nt">-o</span> dashboard.tar 
docker save kubernetesui/metrics-scraper:v1.0.8 <span class="nt">-o</span> metrics-scraper.tar 
docker save quay.io/cilium/cilium:v1.12.6 <span class="nt">-o</span> cilium.tar 
docker save quay.io/cilium/certgen:v0.1.8 <span class="nt">-o</span> certgen.tar 
docker save quay.io/cilium/hubble-relay:v1.12.6 <span class="nt">-o</span> hubble-relay.tar 
docker save quay.io/cilium/hubble-ui-backend:v0.9.2 <span class="nt">-o</span> hubble-ui-backend.tar 
docker save quay.io/cilium/hubble-ui:v0.9.2 <span class="nt">-o</span> hubble-ui.tar 
docker save quay.io/cilium/cilium-etcd-operator:v2.0.7 <span class="nt">-o</span> cilium-etcd-operator.tar 
docker save quay.io/cilium/operator:v1.12.6 <span class="nt">-o</span> operator.tar 
docker save quay.io/cilium/clustermesh-apiserver:v1.12.6 <span class="nt">-o</span> clustermesh-apiserver.tar 
docker save quay.io/coreos/etcd:v3.5.4 <span class="nt">-o</span> etcd.tar 
docker save quay.io/cilium/startup-script:d69851597ea019af980891a4628fb36b7880ec26 <span class="nt">-o</span> startup-script.tar 

<span class="c"># 传输到各个节点</span>
<span class="k">for </span>NODE <span class="k">in </span>k8s-master01 k8s-master02 k8s-master03 k8s-node01 k8s-node02<span class="p">;</span> <span class="k">do </span>scp <span class="nt">-r</span> images/  <span class="nv">$NODE</span>:/root/ <span class="p">;</span> <span class="k">done</span>

<span class="c"># 创建命名空间</span>
ctr ns create k8s.io

<span class="c"># 导入镜像</span>
ctr <span class="nt">--namespace</span> k8s.io image import images/cni.tar
ctr <span class="nt">--namespace</span> k8s.io image import images/node.tar
ctr <span class="nt">--namespace</span> k8s.io image import images/typha.tar
ctr <span class="nt">--namespace</span> k8s.io image import images/kube-controllers.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/coredns.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/pause.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/metrics-server.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/dashboard.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/metrics-scraper.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/dashboard.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/metrics-scraper.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/cilium.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/certgen.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/hubble-relay.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/hubble-ui-backend.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/hubble-ui.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/cilium-etcd-operator.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/operator.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/clustermesh-apiserver.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/etcd.tar 
ctr <span class="nt">--namespace</span> k8s.io image import images/startup-script.tar 

<span class="c"># pull tar包 解压后</span>
helm pull cilium/cilium

<span class="c"># 查看镜像版本</span>
root@hello:~/cilium# <span class="nb">cat </span>values.yaml| <span class="nb">grep </span>tag: <span class="nt">-C1</span>
  repository: <span class="s2">"quay.io/cilium/cilium"</span>
  tag: <span class="s2">"v1.12.6"</span>
  pullPolicy: <span class="s2">"IfNotPresent"</span>
<span class="nt">--</span>
    repository: <span class="s2">"quay.io/cilium/certgen"</span>
    tag: <span class="s2">"v0.1.8@sha256:4a456552a5f192992a6edcec2febb1c54870d665173a33dc7d876129b199ddbd"</span>
    pullPolicy: <span class="s2">"IfNotPresent"</span>
<span class="nt">--</span>
      repository: <span class="s2">"quay.io/cilium/hubble-relay"</span>
      tag: <span class="s2">"v1.12.6"</span>
       <span class="c"># hubble-relay-digest</span>
<span class="nt">--</span>
        repository: <span class="s2">"quay.io/cilium/hubble-ui-backend"</span>
        tag: <span class="s2">"v0.9.2@sha256:a3ac4d5b87889c9f7cc6323e86d3126b0d382933bd64f44382a92778b0cde5d7"</span>
        pullPolicy: <span class="s2">"IfNotPresent"</span>
<span class="nt">--</span>
        repository: <span class="s2">"quay.io/cilium/hubble-ui"</span>
        tag: <span class="s2">"v0.9.2@sha256:d3596efc94a41c6b772b9afe6fe47c17417658956e04c3e2a28d293f2670663e"</span>
        pullPolicy: <span class="s2">"IfNotPresent"</span>
<span class="nt">--</span>
    repository: <span class="s2">"quay.io/cilium/cilium-etcd-operator"</span>
    tag: <span class="s2">"v2.0.7@sha256:04b8327f7f992693c2cb483b999041ed8f92efc8e14f2a5f3ab95574a65ea2dc"</span>
    pullPolicy: <span class="s2">"IfNotPresent"</span>
<span class="nt">--</span>
    repository: <span class="s2">"quay.io/cilium/operator"</span>
    tag: <span class="s2">"v1.12.6"</span>
    <span class="c"># operator-generic-digest</span>
<span class="nt">--</span>
    repository: <span class="s2">"quay.io/cilium/startup-script"</span>
    tag: <span class="s2">"d69851597ea019af980891a4628fb36b7880ec26"</span>
    pullPolicy: <span class="s2">"IfNotPresent"</span>
<span class="nt">--</span>
    repository: <span class="s2">"quay.io/cilium/cilium"</span>
    tag: <span class="s2">"v1.12.6"</span>
    <span class="c"># cilium-digest</span>
<span class="nt">--</span>
      repository: <span class="s2">"quay.io/cilium/clustermesh-apiserver"</span>
      tag: <span class="s2">"v1.12.6"</span>
      <span class="c"># clustermesh-apiserver-digest</span>
<span class="nt">--</span>
        repository: <span class="s2">"quay.io/coreos/etcd"</span>
        tag: <span class="s2">"v3.5.4@sha256:795d8660c48c439a7c3764c2330ed9222ab5db5bb524d8d0607cac76f7ba82a3"</span>
        pullPolicy: <span class="s2">"IfNotPresent"</span>
</code></pre></div></div>

<p><strong>关于</strong></p>

<p>https://www.oiox.cn/</p>

<p>https://www.oiox.cn/index.php/start-page.html</p>

<p><strong>CSDN、GitHub、知乎、开源中国、思否、掘金、简书、华为云、阿里云、腾讯云、哔哩哔哩、今日头条、新浪微博、个人博客</strong></p>

<p><strong>全网可搜《小陈运维》</strong></p>

<p><strong>文章主要发布于微信公众号：《Linux运维交流社区》</strong></p>]]></content><author><name>博主</name><email>your-email@example.com</email></author><summary type="html"><![CDATA[二进制安装Kubernetes（k8s）v1.35.0]]></summary></entry><entry><title type="html">网络代理配置指南</title><link href="https://westyang.github.io/blog-page/%E6%8A%80%E6%9C%AF/2026/04/16/proxy-configuration.html" rel="alternate" type="text/html" title="网络代理配置指南" /><published>2026-04-16T02:00:00+00:00</published><updated>2026-04-16T02:00:00+00:00</updated><id>https://westyang.github.io/blog-page/%E6%8A%80%E6%9C%AF/2026/04/16/proxy-configuration</id><content type="html" xml:base="https://westyang.github.io/blog-page/%E6%8A%80%E6%9C%AF/2026/04/16/proxy-configuration.html"><![CDATA[<h1 id="网络代理配置指南">网络代理配置指南</h1>

<p>本文介绍在不同环境下配置代理的方法，包括yum、docker和pip的代理设置。</p>

<h2 id="1yum配置代理">1、yum配置代理</h2>

<p>编辑yum配置文件：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># vim /etc/yum.conf</span>
<span class="c"># 加入下面一行代理配置</span>
<span class="nv">proxy</span><span class="o">=</span> http://10.33.49.191:10809
</code></pre></div></div>

<h2 id="2docker-配置代理">2、docker 配置代理</h2>

<p>创建docker服务目录并配置代理：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo mkdir</span> <span class="nt">-p</span> /etc/systemd/system/docker.service.d
vim /etc/systemd/system/docker.service.d/http-proxy.conf    
<span class="c">#添加代理配置</span>
<span class="o">[</span>Service]
<span class="nv">Environment</span><span class="o">=</span><span class="s2">"HTTP_PROXY=http://your-proxy-server:port/"</span>
<span class="nv">Environment</span><span class="o">=</span><span class="s2">"HTTPS_PROXY=https://your-proxy-server:port/"</span>
<span class="nv">Environment</span><span class="o">=</span><span class="s2">"NO_PROXY=localhost,127.0.0.1,.example.com"</span>
</code></pre></div></div>

<h2 id="3pip配置代理和源">3、pip配置代理和源</h2>

<p>设置pip的代理和镜像源：</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip config <span class="nb">set </span>global.proxy http://10.100.28.114:3180
pip config <span class="nb">set </span>global.index-url https://mirrors.ustc.edu.cn/pypi/simple
pip config <span class="nb">set </span>global.trusted-host mirrors.ustc.edu.cn
</code></pre></div></div>

<h2 id="总结">总结</h2>

<p>以上配置方法可以帮助您在不同的环境中设置代理，确保网络访问的顺畅。根据您的实际网络环境，替换相应的代理服务器地址和端口即可。</p>]]></content><author><name>博主</name><email>your-email@example.com</email></author><category term="技术" /><summary type="html"><![CDATA[网络代理配置指南]]></summary></entry><entry><title type="html">Office / Visio 安装与 KMS 激活指南</title><link href="https://westyang.github.io/blog-page/%E6%8A%80%E6%9C%AF%E6%8C%87%E5%8D%97/2026/04/16/office_kms_%E5%AE%89%E8%A3%85%E6%BF%80%E6%B4%BB%E6%8C%87%E5%8D%97.html" rel="alternate" type="text/html" title="Office / Visio 安装与 KMS 激活指南" /><published>2026-04-16T01:00:00+00:00</published><updated>2026-04-16T01:00:00+00:00</updated><id>https://westyang.github.io/blog-page/%E6%8A%80%E6%9C%AF%E6%8C%87%E5%8D%97/2026/04/16/office_kms_%E5%AE%89%E8%A3%85%E6%BF%80%E6%B4%BB%E6%8C%87%E5%8D%97</id><content type="html" xml:base="https://westyang.github.io/blog-page/%E6%8A%80%E6%9C%AF%E6%8C%87%E5%8D%97/2026/04/16/office_kms_%E5%AE%89%E8%A3%85%E6%BF%80%E6%B4%BB%E6%8C%87%E5%8D%97.html"><![CDATA[<h1 id="office--visio-安装与-kms-激活指南">Office / Visio 安装与 KMS 激活指南</h1>

<hr />

<h2 id="一下载镜像">一、下载镜像</h2>

<h3 id="office">Office</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://officecdn.microsoft.com/pr/492350f6-3a01-4f97-b9c0-c7c6ddf67d60/media/zh-cn/ProPlus2021Retail.img
https://officecdn.microsoft.com/pr/492350f6-3a01-4f97-b9c0-c7c6ddf67d60/media/zh-cn/ProPlus2024Retail.img
</code></pre></div></div>

<h3 id="visio">Visio</h3>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>https://officecdn.microsoft.com/pr/492350f6-3a01-4f97-b9c0-c7c6ddf67d60/media/zh-cn/VisioPro2021Retail.img
https://officecdn.microsoft.com/pr/492350f6-3a01-4f97-b9c0-c7c6ddf67d60/media/zh-cn/VisioPro2024Retail.img
</code></pre></div></div>

<hr />

<h2 id="二安装镜像">二、安装镜像</h2>

<p>下载完成后，挂载 <code class="language-plaintext highlighter-rouge">.img</code> 镜像并执行安装程序。</p>

<hr />

<h2 id="三更改-office-license">三、更改 Office License</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> <span class="s2">"C:</span><span class="se">\P</span><span class="s2">rogram Files</span><span class="se">\M</span><span class="s2">icrosoft Office</span><span class="se">\O</span><span class="s2">ffice*"</span>
</code></pre></div></div>

<h3 id="office-1">Office</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> /f %x <span class="k">in</span> <span class="o">(</span><span class="s1">'dir /b ..\root\Licenses16\ProPlus2021VL_KMS*.xrm-ms'</span><span class="o">)</span> <span class="k">do </span>cscript ospp.vbs /inslic:<span class="s2">"..</span><span class="se">\r</span><span class="s2">oot</span><span class="se">\L</span><span class="s2">icenses16</span><span class="se">\%</span><span class="s2">x"</span>

<span class="k">for</span> /f %x <span class="k">in</span> <span class="o">(</span><span class="s1">'dir /b ..\root\Licenses16\ProPlus2024VL_KMS*.xrm-ms'</span><span class="o">)</span> <span class="k">do </span>cscript ospp.vbs /inslic:<span class="s2">"..</span><span class="se">\r</span><span class="s2">oot</span><span class="se">\L</span><span class="s2">icenses16</span><span class="se">\%</span><span class="s2">x"</span>
</code></pre></div></div>

<h3 id="visio-1">Visio</h3>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> /f %x <span class="k">in</span> <span class="o">(</span><span class="s1">'dir /b ..\root\Licenses16\VisioPro2021VL_KMS*.xrm-ms'</span><span class="o">)</span> <span class="k">do </span>cscript ospp.vbs /inslic:<span class="s2">"..</span><span class="se">\r</span><span class="s2">oot</span><span class="se">\L</span><span class="s2">icenses16</span><span class="se">\%</span><span class="s2">x"</span>

<span class="k">for</span> /f %x <span class="k">in</span> <span class="o">(</span><span class="s1">'dir /b ..\root\Licenses16\VisioPro2024VL_KMS*.xrm-ms'</span><span class="o">)</span> <span class="k">do </span>cscript ospp.vbs /inslic:<span class="s2">"..</span><span class="se">\r</span><span class="s2">oot</span><span class="se">\L</span><span class="s2">icenses16</span><span class="se">\%</span><span class="s2">x"</span>
</code></pre></div></div>

<hr />

<h2 id="四kms-命令激活">四、KMS 命令激活</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd</span> <span class="s2">"C:</span><span class="se">\P</span><span class="s2">rogram Files</span><span class="se">\M</span><span class="s2">icrosoft Office</span><span class="se">\O</span><span class="s2">ffice*"</span>

cscript ospp.vbs /inpkey:XJ2XN-FW8RK-P4HMP-DKDBV-GCVGB
cscript ospp.vbs /inpkey:B7TN8-FJ8V3-7QYCP-HQPMV-YY89G

cscript ospp.vbs /sethst:s1.kms.cx
cscript ospp.vbs /act

cscript ospp.vbs /dstatus    <span class="c"># 查看激活状态</span>
</code></pre></div></div>

<hr />

<h2 id="五gvlk-密钥列表">五、GVLK 密钥列表</h2>

<h3 id="office-ltsc-2021">Office LTSC 2021</h3>

<table>
  <thead>
    <tr>
      <th>产品</th>
      <th>Key</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Office LTSC 专业增强版 2021</td>
      <td>FXYTK-NJJ8C-GB6DW-3DYQT-6F7TH</td>
    </tr>
    <tr>
      <td>Office LTSC 标准版 2021</td>
      <td>KDX7X-BNVR8-TXXGX-4Q7Y8-78VT3</td>
    </tr>
    <tr>
      <td>Project Professional 2021</td>
      <td>FTNWT-C6WBT-8HMGF-K9PRX-QV9H8</td>
    </tr>
    <tr>
      <td>Project Standard 2021</td>
      <td>J2JDC-NJCYY-9RGQ4-YXWMH-T3D4T</td>
    </tr>
    <tr>
      <td>Visio LTSC Professional 2021</td>
      <td>KNH8D-FGHT4-T8RK3-CTDYJ-K2HT4</td>
    </tr>
    <tr>
      <td>Visio LTSC Standard 2021</td>
      <td>MJVNY-BYWPY-CWV6J-2RKRT-4M8QG</td>
    </tr>
    <tr>
      <td>Access LTSC 2021</td>
      <td>WM8YG-YNGDD-4JHDC-PG3F4-FC4T4</td>
    </tr>
    <tr>
      <td>Excel LTSC 2021</td>
      <td>NWG3X-87C9K-TC7YY-BC2G7-G6RVC</td>
    </tr>
    <tr>
      <td>Outlook LTSC 2021</td>
      <td>C9FM6-3N72F-HFJXB-TM3V9-T86R9</td>
    </tr>
    <tr>
      <td>PowerPoint LTSC 2021</td>
      <td>TY7XF-NFRBR-KJ44C-G83KF-GX27K</td>
    </tr>
    <tr>
      <td>Publisher LTSC 2021</td>
      <td>2MW9D-N4BXM-9VBPG-Q7W6M-KFBGQ</td>
    </tr>
    <tr>
      <td>Skype for Business LTSC 2021</td>
      <td>HWCXN-K3WBT-WJBKY-R8BD9-XK29P</td>
    </tr>
    <tr>
      <td>Word LTSC 2021</td>
      <td>TN8H9-M34D3-Y64V9-TR72V-X79KV</td>
    </tr>
  </tbody>
</table>

<hr />

<h3 id="office-ltsc-2024">Office LTSC 2024</h3>

<table>
  <thead>
    <tr>
      <th>产品</th>
      <th>Key</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Office LTSC Professional Plus 2024</td>
      <td>XJ2XN-FW8RK-P4HMP-DKDBV-GCVGB</td>
    </tr>
    <tr>
      <td>Office LTSC Standard 2024</td>
      <td>V28N4-JG22K-W66P8-VTMGK-H6HGR</td>
    </tr>
    <tr>
      <td>Project Professional 2024</td>
      <td>FQQ23-N4YCY-73HQ3-FM9WC-76HF4</td>
    </tr>
    <tr>
      <td>Project Standard 2024</td>
      <td>PD3TT-NTHQQ-VC7CY-MFXK3-G87F8</td>
    </tr>
    <tr>
      <td>Visio LTSC Professional 2024</td>
      <td>B7TN8-FJ8V3-7QYCP-HQPMV-YY89G</td>
    </tr>
    <tr>
      <td>Visio LTSC Standard 2024</td>
      <td>JMMVY-XFNQC-KK4HK-9H7R3-WQQTV</td>
    </tr>
    <tr>
      <td>Access LTSC 2024</td>
      <td>82FTR-NCHR7-W3944-MGRHM-JMCWD</td>
    </tr>
    <tr>
      <td>Excel LTSC 2024</td>
      <td>F4DYN-89BP2-WQTWJ-GR8YC-CKGJG</td>
    </tr>
    <tr>
      <td>Outlook LTSC 2024</td>
      <td>D2F8D-N3Q3B-J28PV-X27HD-RJWB9</td>
    </tr>
    <tr>
      <td>PowerPoint LTSC 2024</td>
      <td>CW94N-K6GJH-9CTXY-MG2VC-FYCWP</td>
    </tr>
    <tr>
      <td>Skype for Business LTSC 2024</td>
      <td>4NKHF-9HBQF-Q3B6C-7YV34-F64P3</td>
    </tr>
    <tr>
      <td>Word LTSC 2024</td>
      <td>MQ84N-7VYDM-FXV7C-6K7CC-VFW9J</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="备注">备注</h2>

<ul>
  <li>请确保使用合法授权环境。</li>
  <li>KMS 服务器需可访问，否则激活会失败。</li>
  <li>建议企业环境中使用内部 KMS 服务。</li>
</ul>]]></content><author><name>博主</name><email>your-email@example.com</email></author><category term="技术指南" /><summary type="html"><![CDATA[Office / Visio 安装与 KMS 激活指南]]></summary></entry><entry><title type="html">欢迎来到我的博客</title><link href="https://westyang.github.io/blog-page/%E9%9A%8F%E7%AC%94/2026/04/16/welcome-to-my-blog.html" rel="alternate" type="text/html" title="欢迎来到我的博客" /><published>2026-04-16T01:00:00+00:00</published><updated>2026-04-16T01:00:00+00:00</updated><id>https://westyang.github.io/blog-page/%E9%9A%8F%E7%AC%94/2026/04/16/welcome-to-my-blog</id><content type="html" xml:base="https://westyang.github.io/blog-page/%E9%9A%8F%E7%AC%94/2026/04/16/welcome-to-my-blog.html"><![CDATA[<h1 id="欢迎来到我的博客">欢迎来到我的博客</h1>

<p>这是我使用 GitLab Pages 创建的个人博客网站的第一篇文章。</p>

<h2 id="关于这个博客">关于这个博客</h2>

<p>这个博客将记录我的技术学习、项目经验和个人思考。我希望通过这个平台分享我的知识和见解，同时也作为我个人成长的记录。</p>

<h2 id="技术栈">技术栈</h2>

<ul>
  <li><strong>静态网站生成器</strong>：Jekyll</li>
  <li><strong>部署平台</strong>：GitLab Pages</li>
  <li><strong>主题</strong>：Minima</li>
</ul>

<h2 id="未来计划">未来计划</h2>

<ul>
  <li>分享更多技术文章和教程</li>
  <li>记录项目开发过程和经验</li>
  <li>分享学习心得和资源</li>
</ul>

<p>感谢您的访问！</p>]]></content><author><name>博主</name><email>your-email@example.com</email></author><category term="随笔" /><summary type="html"><![CDATA[欢迎来到我的博客]]></summary></entry></feed>